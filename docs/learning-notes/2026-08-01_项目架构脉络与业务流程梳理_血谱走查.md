# 项目架构脉络与业务流程梳理（系统血谱走查 v1）

> **日期**：2026-08-01 ｜ **依据**：`docs/architecture/系统血谱.md` L0/L1 脉络
> **宪法遵循**：《ENGINEER_CONSTITUTION.md》第一条（输出贴合自然人脑认知规律：先整体后局部、先正常态后异常、每层都有"一句话"锚点）
> **本卷任务**：按血谱四层顺序（agent1-web → Agent1.Api → Agent1 → 存储）逐层走查，理清职责与交互；**底层机制盲点仅登记不深挖**（见第六章），待架构脉络稳固后另行逐项攻坚。
>
> **阅读方法（30 秒建立地图）**：先读「〇、四层全景」，然后任选一章深入。每章开头都有"一句话锚点"，读完章节只需记住锚点即可——细节随时回来查表。

---

## 〇、四层全景（先记住这张表）

| 层 | 组件 | 一句话锚点 | 关键入口 |
|---|---|---|---|
| L0 表现层 | agent1-web（Vue 3 + Vite） | 人机交互窗口：页面 ↔ API 客户端 ↔ 后端 | `src/pages/` + `src/api/` |
| L1 HTTP 层 | Agent1.Api（ASP.NET Core） | 门卫+接线员：中间件管线过滤一切请求，15 个 Controller 按域接线 | `Program.cs` + `Controllers/` |
| L2 核心层 | Agent1（SK Agent + 服务库） | 大脑：6 步流水线编排意图 → LLM 推理 → 工具检索 → 双通道渲染 | `Services/Dialog/AgentDialog.cs` |
| L3 存储层 | PostgreSQL + SQLite + 静态字典 | 三层记忆：PG 主存（向量/文档/审计）、SQLite 权威（确定性数据）、C# 字典兜底 | `DatabaseService` / `ChemicalDatabaseService` |

数据流总动脉：**前端页面 → axios → 中间件 → Controller → AgentDialog/Services → 工具/检索 → LLM(llama.cpp) → 双通道渲染 → 响应回前端**

---

## 一、L0 表现层：agent1-web

### 1.1 技术栈与模式

Vue 3 + Vite + TypeScript + Pinia（`stores/auth.ts`）+ Vue Router（`router/index.ts`）+ axios 封装（`lib/axios.ts`，含 JWT 拦截器）。**双模式运行**：`mocks/` 下 MSW 拦截（纯前端演示）⇄ 真实后端 API，由 Vite 代理切换。

### 1.2 页面与后端 Controller 映射（一页一业务域）

| 前端页面 | 对应 API 模块 | 后端 Controller |
|---|---|---|
| `DashboardPage` 仪表盘 | `api/dashboard.ts` | DashboardController |
| `ComplianceCheckPage` 合规自查 | `api/compliance.ts` | ComplianceController |
| `HazardQueryPage` 危化品查询 | `api/compliance.ts` | ComplianceController（hazard/query） |
| `StorageCompatibilityPage` 储存兼容 | `api/compliance.ts` | ComplianceController（storage/compatibility） |
| `EmergencyPage` 应急响应 | `api/emergency.ts` | EmergencyController |
| `KnowledgeBasePage` 知识库 | `api/knowledgeBase.ts` | KnowledgeBaseController |
| `KnowledgeGraphPage` 知识图谱 | `api/graph.ts` | KnowledgeGraphController |
| `RegulatoryAuditPage` 监管核查 | — | RegulatoryController |
| `MultimodalPage` 视觉分析 | — | MultimodalController |
| `AssetsPage` / `AssetDetailPage` 资产台账 | `api/dashboard.ts` | DashboardController |
| `InspectionPlans/Rounds/Report*` 巡检 | `api/inspection.ts` | InspectionController |
| `TicketList/Detail` 整改工单 | `api/tickets.ts` | TicketsController |
| `AuditPage` 审计日志 | `api/audit.ts` | AuditController |
| `EvalPage` 合规评测 | `api/eval.ts` | EvalController |
| `DiagnosticsPage` 诊断 | — | DiagnosticsController |
| `DatabaseDiagnosticsPage` DB 诊断 | `api/admin.ts` | AdminController |
| `SystemMonitorPage` 系统监控 | `api/dashboard.ts` | DashboardController（+健康端点） |
| `SettingsPage` 设置 | `api/auth.ts` | AuthController |
| `LoginPage` 登录 | `api/auth.ts` | AuthController |
| `AIChatPage` 对话 | — | —（前端直连 AgentDialog 演示通道） |

---

## 二、L1 HTTP 层：Agent1.Api

### 2.1 中间件管线（按注册顺序，10 个节点）

```
请求进入
 ① HTTPS 重定向 + HSTS（仅生产）
 ② GlobalExceptionMiddleware      ← 最外层兜底：捕获下游一切未处理异常
 ③ RequestIdMiddleware            ← 请求 ID 透传（全链路追踪锚点）
 ④ RateLimitingMiddleware         ← 100 次/分钟/IP+端点 组合限流
 ⑤ RequestMetricsMiddleware       ← 指标采集（MetricsCollector）
 ⑥ Swagger / SwaggerUI
 ⑦ CORS（默认 localhost:3000/5173，生产由 CORS_ORIGINS 控制）
 ⑧ Authentication                 ← JWT Bearer 认证（MapInboundClaims=false）
 ⑨ TokenBlacklistMiddleware       ← 黑名单检查（必须在认证后、授权前）
 ⑩ Authorization                  ← 三策略：Admin / Auditor / Viewer
 → MapControllers
```

**权限三策略**：`Admin`=admin 仅；`Auditor`=admin+auditor；`Viewer`=admin+auditor+viewer。查询类多为 Viewer，**写操作（审核/扫描/更新/评测/工单流转）一律 Auditor+**。

### 2.2 15 个 Controller 职责分工（按业务域分组）

**认证与账号**
| Controller | 职责 | 权限 | 关键端点 |
|---|---|---|---|
| AuthController | JWT 签发/刷新/登出；BCrypt 密码；设备指纹；Refresh Token 落库（`refresh_tokens` 表） | AllowAnonymous | POST login / refresh / logout |

**合规业务主战场**
| Controller | 职责 | 权限 | 关键端点 |
|---|---|---|---|
| ComplianceController | ①合规审核（完整管线）②危化品信息查询 ③储存兼容性检查；缓存 + `_llmGate` 并发闸门 + ConclusionVerifier 法规验证 | check=Auditor，其余 Viewer | POST check / hazard/query / storage/compatibility |
| EmergencyController | 泄漏/火灾/爆炸/中毒四类应急响应方案生成（LLM + 知识库 + 审计留痕） | Viewer | POST response |
| RegulatoryController | 监管核查：核查清单按行拆分，逐条法规评估 | audit=Auditor | POST audit |
| KnowledgeGraphController | 化学品-法规-事故关联网查询（KnowledgeGraphFactory 门面） | Viewer | POST query |

**知识库与检索**
| Controller | 职责 | 权限 | 关键端点 |
|---|---|---|---|
| KnowledgeBaseController | 检索模式切换（Bm25/Vector/Hybrid）、RAG 检索测试、增量更新触发（409 互斥） | incremental=Auditor | GET/PUT search-mode, POST rag-test, POST incremental-load |
| MultimodalController | GHS 标签/储罐场景/自定义图像分析（调 llama.cpp 视觉模型） | analyze=Auditor | POST analyze（20MB 限） |

**巡检与整改闭环（P0 业务闭环交付卡口）**
| Controller | 职责 | 权限 | 关键端点 |
|---|---|---|---|
| InspectionController | 巡检计划 CRUD + 轮次执行 + 合规率统计（InspectionOrchestrator 编排） | 写=Auditor | POST/GET plans, POST run, GET rounds |
| TicketsController | 整改工单查询 + 状态机流转（accept→start→complete→verify→close/reject）+ LLM 工单跟进 | 写=Auditor | GET, PUT {id}/status, POST followup |
| DashboardController | 仪表盘总览/资产台账/合规扫描（异步+进度轮询）/发现列表/危化品报告 | scan=Auditor | GET overview/assets/findings/history, POST scan, GET scan/status |

**审计与运维**
| Controller | 职责 | 权限 | 关键端点 |
|---|---|---|---|
| AuditController | 审计日志查询 + SHA256 哈希链完整性验证 + 一键修复断链 + 报告导出 | Admin | GET logs/integrity/export/stats, POST repair-chain |
| AdminController | 数据库信息/连接验证（运维（参照等保控制点）） | Admin | GET db/info, db/validate |
| AlertsController | 告警邮件通道测试（SMTP） | test=Auditor | POST test |

**评测与诊断**
| Controller | 职责 | 权限 | 关键端点 |
|---|---|---|---|
| EvalController | 50 条业务评测集异步跑批（TaskStore 进度）+ 取消 | run=Auditor | POST run, GET status/{id}, DELETE status/{id} |
| DiagnosticsController | 5 条预置用例验证 SK Function Calling 是否触发 | Viewer | POST tool-calling |

**Program.cs 内置端点**（非 Controller）：`/health`（DB+LLM+KB 三检，降级 503）、`/health/ready|live`、`/metrics`（Prometheus）、`/cache/stats|clear`、`/knowledgebase/incremental-update`、`/memory/*`（短/长期记忆统计与清理）、`/admin/config/decoupled-architecture`（双通道热切换）、`/alert/test`。

---

## 三、L2 核心层：Agent1（大脑）

### 3.1 AgentDialog：6 步统一线性流水线

```
用户输入
 ┌─ [1/6] 预处理          PreprocessAsync：Trim 清洗
 ├─ 安全闸门               SafetyGuardService.ValidateInput：Prompt 注入检测（拦截即 Blocked）
 ├─ [2/6] 意图路由         IntentRouter.Route：关键词匹配，记录 LastMatchedKeyword（可审计）
 ├─ [3/6] 上下文加载       LoadContextAsync：会话历史(10条) + 短期记忆关键事实 + 用户画像
 ├─ [4/6] 业务执行         ExecuteBusinessWithResultAsync（见 3.2）
 │                          └─ 输出末端：OutputSanitizer → OutputValidator 法规白名单 → 输出安全检测
 ├─ [5/6] 会话保存         SaveSessionAsync：写入 sessions 表
 └─ [6/6] 输出格式化       FormatOutput：展示文本
 审计留痕：ChemicalCompliance 意图 → audit_logs + ComplianceAuditLogger
```

**意图路由决策表**（优先级从上到下，命中即停）：

| 优先级 | 意图 | 关键词示例 |
|---|---|---|
| 1 | Emergency 应急 | 应急、泄漏、火灾、爆炸、疏散、PPE、急救、事故 |
| 2 | RegulatoryAudit 法规审计 | 审计、核查、合规检查、监管 |
| 3 | KnowledgeGraph 知识图谱 | 知识图谱、图谱查询、实体 |
| 4 | ChemicalCompliance 合规 | 法规、GB、GHS、危险、易燃、储存、间距、同库、类别… |
| 5 | SimpleChat 闲聊 | 你好、谢谢、再见… |

### 3.2 业务执行层（第 4 步展开）

**前置记忆层**（MemoryCoordinator 预推理）：
- 缓存/记忆命中 → 仅当"来源=真实工具调用"才跳过 LLM（防兜底文本被缓存误答）
- 长期记忆命中 → 注入上下文（跨会话参考）；短期记忆命中 → 注入但不跳过推理

**ChemicalCompliance 分支（SK Auto Function Calling）**：

```
提示词组装（system + 历史 + 问题 + 输出模板）
 → LlmService.InvokeStreamWithRetryAsync（流式）
    ├─ 熔断器检查：3 连败 → 30 秒冷却 → 规则引擎降级
    ├─ FunctionChoiceBehavior.Required()：强制工具调用
    └─ 流式输出 + 死循环检测（重复行>8 / 级联字符>12 / 5000 字符硬截断）
 → LLM 失败 / 零工具调用 → TryFallbackToRuleEngine（确定性规则引擎兜底）
 → 双通道解耦管线（开关控制，见 3.5）
 → OutputValidator 法规白名单硬校验（非库内法规号标注"待核实"）
 → 记忆后推理（PostInferenceAsync 异步不阻塞）
 → 审计留痕
```

**SimpleChat 分支**：通用对话提示词 + SK 系统工具（GetCurrentTime/Calculate）收集。

### 3.3 LlmService 与 llama.cpp 的交互模式

| 维度 | 机制 |
|---|---|
| 协议 | Semantic Kernel `AddOpenAIChatCompletion(ModelId, Endpoint)`（`LlmService.cs#L67`）→ llama.cpp OpenAI 兼容端点 `/v1/chat/completions`（ModelConfig.Endpoint） |
| 工具 | `ChemicalComplianceTools` 插件注册（13 个 `[KernelFunction]`），SK Auto FC 由 LLM 自主决定调用 |
| 推理并发 | `SemaphoreSlim(2,2)` 单例闸门（API 层）+ 熔断器（进程内 3 连败/30 秒） |
| Thinking | Qwen3 思考模式开关：`EnableThinking`（默认 false，主流程临时切 true；OllamaThinkingHandler 反射注入 SK HttpClient） |
| 重试 | `MaxRetries`/`RetryDelayMs` 配置驱动；2 分钟超时兜底 |
| 防呆 | 流式死循环检测（重复行/括号级联/硬截断）；think 标签过滤（仅 DeepSeek-R1） |
| Embedding | llama.cpp `/v1/embeddings`，**批量数组请求**（一次多句），并发受 `MaxConcurrentEmbeddings` 控制 |
| 诊断 | `FunctionCallDiagnosticsFilter` 拦截器 → `LastFunctionCalls`（供评测/审计/验证层） |

**13 个工具（ChemicalComplianceTools）**：CheckHazardCategory、CheckStorageCompatibility、GetSafetyDistance、LookupChemicalProperties、GetMajorHazardThreshold、CheckRegulationVersion、LookupHazardLabel（多模态）+ 别名归一化辅助 + GetCurrentTime/Calculate 等系统工具。每个工具内部都是**三级降级**（见 3.6）。

### 3.4 ChemicalRAG：知识加载与混合检索

**文档入库管道**（全格式）：

```
knowledgebase/ 目录扫描
 → PdfExtractor / DocExtractor（PDF/DOC/DOCX/TXT 提取）
 → TextCleaner（清洗）
 → 乱码闸门（GarbledTextDetector 拒收乱码块）
 → SemanticChunker（语义分块）
 → 双存储：PostgreSQL（knowledge_documents + knowledge_chunks + 向量）+ 内存索引（BM25）
 扫描件 PDF（文本层过薄）→ PdfOcrService 视觉 OCR 回退（llama.cpp 视觉模型，可配置开关）
```

**增量更新**（Bug-038/039 修复后的正确形态）：`_incrementalGate`（static 信号量）`WaitAsync(0)` 拿不到 → `IncrementalAlreadyRunningException` → 409；绑定 `ApplicationStopping` 停机取消；文件追踪器（`LoadFileTracker`）只处理新增/修改/删除。

**检索三步**（`SearchAsync`）：
1. `RetrieveAsync(query, topK*3)`：HybridKnowledgeBaseService 内 查询缓存 → 查询扩展（ExpandQuery）→ 按模式（Bm25 / Vector / Hybrid-RRF 融合）检索
2. **优先级重排序**（核心修复）：`RegulationType` 加权——国标+3000 / 化工专业条例+2800 / 园区规则+2000 / 历史案例+1000 / 企业制度+500
3. 取 topK 返回

### 3.5 双通道解耦管线（Citation 准确率整改核心）

```
LLM 原始输出 + 工具调用记录
 → ComplianceFactExtractor：从工具返回提取结构化事实（法规号等）
 → OutputSanitizer：从 LLM 输出中删除 LLM 自编的法规引用（LLM 只负责专业解读）
 → FactAssembler：用工具事实确定性渲染法规引用段落
 → ResponseMerger：合并为最终答案
```

开关：`/admin/config/decoupled-architecture` 热切换；`UseDecoupledArchitecture=true` 时 LLM 用消毒后的输出模板（`OutputTemplateDecoupled`）。

### 3.6 确定性知识三级降级（工具内部）

```
Level 1: SQLite 结构化数据库（ChemicalDatabaseService —— 权威数据源，C# 种子 + 002_*.sql 双源同构）
Level 2: ChemicalSubstanceDatabase 静态字典（知识图谱门面注入）
Level 3: 硬编码字典（评测集对齐兜底）
末级:    诚实声明（"未找到"而非编造）
```

### 3.7 记忆系统（三层）

| 层 | 服务 | 要点 |
|---|---|---|
| 短期记忆 | MemoryService（会话内） | KeyFacts 提取/注入；`/memory/stats`、`/memory/clear` |
| 长期记忆 | LongTermMemoryService（跨会话） | 关键词检索、180 天清理；落库 `long_term_memories` 表 |
| 协调器 | MemoryCoordinator | 预推理（缓存判定）+ 后推理（异步写入）；与 ResponseCacheService 联动 |

---

## 四、L3 存储层

### 4.1 PostgreSQL（主存储，8 张表）

| 表 | 职责 | 备注 |
|---|---|---|
| `chemical_documents` | 化学品文档 + 向量（pgvector） | 动脉 A 双存储之一 |
| `knowledge_documents` | 知识库文档登记 | `source_path UNIQUE`（增量互斥的约束基础） |
| `knowledge_chunks` | 分块内容 + 向量 | `document_id` 外键级联删除 |
| `sessions` | 会话 | UUID 主键 |
| `audit_logs` | 操作审计 | 参照等保审计控制点 + SHA256 哈希链 |
| `search_logs` | 检索日志 | 查询/结果数/耗时 |
| `long_term_memories` | 长期记忆 | user_id + memory_type |
| `refresh_tokens` | 刷新令牌 | token_hash 主键 + 过期时间 |

### 4.2 SQLite + 静态字典（确定性数据通道）

- **SQLite**（`ChemicalDatabaseService`）：substances / 兼容性规则 / 安全距离规则等——**确定性数据 100% 走此路径**，C# 种子与 PG migration 002 同源
- **C# 静态字典**（`ChemicalSubstanceDatabase`）：内存兜底，启动时由 `ChemicalKnowledgeGraph` 门面注入（`Program.cs#L331`）

### 4.3 同源拷贝对照（血谱登记核对）

| 数据 | 权威源 | 拷贝 | 风险 |
|---|---|---|---|
| 危险类别/安全距离 | SQLite（ChemicalDatabaseService） | ChemicalSubstanceDatabase 静态字典、ChemicalComplianceTools 硬编码字典 | 已通过评测集三方对齐（#3 FIX 注释） |

---

## 五、四条核心业务线完整链路

### 业务线 1：合规自查（最高频）

```
ComplianceCheckPage
 → POST /api/compliance/check {query}
 → [中间件] JWT认证 → 授权(Auditor)
 → ComplianceController.CheckCompliance
    ├─ SafetyGuardService.ValidateInput（Prompt 注入拦截）
    ├─ ResponseCacheService.Get（命中直接返回）
    ├─ _llmGate.WaitAsync(5s)（超限 503+retryAfter:10）
    ├─ AgentDialog.ExecuteEvalFastAsync（评测快速通道：跳过流水线装饰，直调 LLM）
    │    └─ LlmService：SK FC Required → llama.cpp 推理
    │         └─ 工具调用：CheckHazardCategory 等 → SQLite→字典→RAG（HybridKnowledgeBaseService）
    │              └─ 双通道渲染（FactExtractor→Sanitizer→FactAssembler→Merger）
    ├─ ConclusionVerifier.VerifyAsync（法规号验证：库内白名单/幻觉标注）
    ├─ SafetyGuardService.ValidateOutput（输出高危断言检测）
    ├─ IntegrationService 外部系统查询（未接入显式降级，不阻断主流程）
    ├─ 审计留痕（audit_logs）
    └─ _cache.Set（写缓存）
 → 200 {response, toolsUsed, verifiedRegulations, warnings}
```

### 业务线 2：应急响应

```
EmergencyPage（化学品 + 事故类型 leak/fire/explosion/poisoning）
 → POST /api/emergency/response
 → EmergencyController（Viewer 权限）
    ├─ 英文枚举 → 中文事故类型映射
    ├─ 组装 EmergencyScenario（默认 100kg）
    ├─ EmergencyResponseService.GeneratePlanAsync
    │    ├─ 知识库检索（化学品属性：CAS/UN/危险类别/闪点）
    │    ├─ LLM 生成方案（疏散隔离/PPE/应急处置措施）
    │    └─ 审计留痕
    └─ FormatEmergencyPlan 文本格式化（隔离半径/防护行动区/PPE 等级…）
 → 200 {output}
```

### 业务线 3：知识库查询（RAG 链路）

```
KnowledgeBasePage
 → POST /api/knowledgebase/rag-test {query, topK}
 → KnowledgeBaseController（Viewer）
    ├─ ChemicalRAG.SearchAsync(query, topK)
    │    ├─ HybridKnowledgeBaseService.RetrieveAsync（缓存 → 查询扩展 → 模式检索）
    │    └─ RegulationType 优先级重排序（国标+3000…）
    ├─ 元数据规范键读取（MetadataKeys.SourceFile / Priority，Bug-043 修复）
 → 200 {results[{content, score, rank, retrievalMethod, source}]}
```

### 业务线 4：巡检 → 整改闭环（P0 业务闭环）

```
InspectionPlansPage 创建计划（Auditor）
 → InspectionController POST /api/inspection/plans
 → InspectionOrchestrator.CreatePlan（存 JSON 仓库）
执行轮次 → InspectionController POST /api/inspection/plans/{id}/run
 → Orchestrator 逐项：DeterministicRuleEngine / ComplianceRuleEngine 判定 → 产出 Findings + Tickets
DashboardPage 一键扫描 → POST /api/dashboard/scan（互斥 + 异步 + /scan/status 轮询）
TicketListPage 流转 → TicketsController PUT /api/tickets/{id}/status
 → 状态机：accept → start → complete → verify → close（或 reject）
 → InspectionRepository.SaveRound 持久化
```

---

## 六、盲点登记（本次仅记录，不深挖）

> 按第二宪法流程：架构脉络稳固后，逐项从「正常基准 → 反直觉坑点」深挖，每篇产出一份四要素笔记归档至 `docs/learning-notes/`。

| # | 盲点 | 涉及代码 | 状态 |
|---|---|---|---|
| 1 | SemaphoreSlim 并发闸门机制 | `Program.cs#L107` + ComplianceController | ✅ **已归档**：`2026-08-01_并发锁与信号量机制吃透笔记.md` |
| 2 | DI 生命周期与共享状态 | Program.cs 全量注册 | ✅ 已归档（同上一篇） |
| 3 | SK FunctionChoiceBehavior 与 llama.cpp FC JSON 交互协议（Required/Auto/None 的上下文膨胀风险） | `LlmService.cs#L121-148` | ⏳ 待深挖（关联 Bug-015/016） |
| 4 | RRF 融合公式与参数（Hybrid 模式） | `HybridKnowledgeBaseService.HybridRetrieveAsync` | ⏳ 待深挖 |
| 5 | 查询扩展 ExpandQuery 的机制（同义词/拆词？） | `HybridKnowledgeBaseService` | ⏳ 待深挖 |
| 6 | 向量嵌入：EmbeddingModelId 服务、维度、批量请求时序 | `LlmService.cs#L1008-1016` | ⏳ 待深挖 |
| 7 | 记忆协调器：缓存命中来源可信度判定与长期记忆写入时机 | `MemoryCoordinator.cs` | ⏳ 待深挖 |
| 8 | ReflectionVerifier 反思机制的推理链 | `ReflectionVerifier.cs`（617 行） | ⏳ 待深挖 |
| 9 | TokenBudgetManager 令牌预算分配 | `AI/TokenBudgetManager.cs` | ⏳ 待深挖 |
| 10 | MSW 前端 Mock 的契约维护 | `agent1-web/src/mocks/handlers.ts`（1559 行） | ⏳ 待深挖 |
| 11 | SHA256 哈希链完整性算法细节 | `AuditService.cs` | ⏳ 待深挖 |
| 12 | DeterministicRuleEngine vs ComplianceRuleEngine 分工边界 | `Orchestration/` 两引擎 | ⏳ 待深挖 |

---

## 七、与系统血谱的对应（本次走查的新增认知）

| 血谱条目 | 本次确认/补充 |
|---|---|
| L0 动脉 A（非结构化条文→RAG） | 确认完整管道：PdfExtractor→TextCleaner→乱码闸门→SemanticChunker→PG 双存储；补充：OCR 回退为可选支线 |
| L0 动脉 B（SQL 迁移→知识图谱） | 补充：`Program.cs#L331` 门面注入是图谱初始化点；SQLite 种子与 migration 002 同源 |
| L0 动脉 C（C#种子→SQLite兜底） | 确认三级降级链：SQLite → 静态字典 → 硬编码字典 → 诚实声明 |
| L0 动脉 D（配置与评测） | 补充：/admin/config 热切换端点 + EvalController 异步跑批 + TaskStore |
| L1 静脉 1（三级降级瀑布） | 工具内降级 + 熔断器 + 规则引擎兜底，三层均确认 |
| L1 静脉 3（记忆缓存旁路） | MemoryCoordinator 预推理/后推理 + 来源可信度判定（Bug 修复点）确认 |
| L1 静脉 4（推理防御链） | 双通道解耦管线 + 法规白名单 + 输出安全检测，全链确认 |
| 病灶登记 | Bug-038/039（增量互斥）、Bug-015/016（FC 死亡螺旋）、Bug-043（元数据键）均已在新架构中看到修复形态 |

---

## 附：速查索引（人脑记忆锚点）

- 一层一锚点：前端=页面↔API 映射；API=10 中间件 + 15 控制器；核心=6 步流水线 + 3 级降级；存储=PG 8 表 + SQLite 权威
- 六步口诀：**预 路 上 业 存 出**（预处理 → 意图路由 → 上下文加载 → 业务执行 → 会话保存 → 输出格式化）
- 三闸门：输入安全闸（Prompt 注入）→ 并发闸（_llmGate 2 并发）→ 输出闸（白名单 + 安全检测）
- 三降级：LLM 熔断 → 规则引擎；工具内 SQLite → 字典 → 硬编码；外部系统未接入 → 显式降级不阻断
