# Agent1 测试总纲

> **版本**：v2.2  
> **日期**：2026-07-21  
> **更新**：新增 §十 真实 GPU E2E 测试层 — 双 E2E 架构：MSW Mock 门禁 + GPU 主力测试层 (9 specs / 35+ 用例)，SSH 隧道全链路，LLM 推理质量专用断言。Docker 六容器清单与 **2026-09-06 RTX 3070 实测（失败，8083 未通）** 见 [GPU全量校验手册.md](../infra/GPU全量校验手册.md) / [3070 实测记录](../infra/2026-09-06_RTX3070容器实测记录.md)。  
> **目标**：三层质量体系 — 业务质量优先(结论准确率) > 集成正确性(编排路径) > 代码覆盖率(参考指标)
>
> **飞致云 4090 编排入口（公司电脑，不手点页面）**：[飞致云五层两档测试说明](../infra/2026-09-07_飞致云五层两档测试说明.md)（`gpu-quick` 测生死 / `gpu-full` 测全量）。本轮 full 深度对照：[2026-09-07_飞致云4090_gpu-full深度分析对比报告.md](../infra/2026-09-07_飞致云4090_gpu-full深度分析对比报告.md)。上机清单：[GPU全量校验手册.md](../infra/GPU全量校验手册.md) §12。`gpu-quick` ≠ `gpu-full`。§4.2 理想值（结论≥90% / Top-5≥85%）对照历史 65.1% / P@5=50.5% **告警不否决**。蓝图：[测试总纲-蓝图.html](./测试总纲-蓝图.html)。

---

## 一、测试金字塔与分层定义

```
                    ┌──────────────┐
                    │   E2E 端到端   │  ← 全链路业务场景 (1-2个核心流程)
                    │  (手动/脚本)   │
                   ┌┴──────────────┴┐
                   │  集成测试       │  ← API + DB + LLM 真实调用
                   │  (63 条)        │
                  ┌┴────────────────┴┐
                  │  单元测试         │  ← 纯逻辑、Mock 外部依赖
                  │  (665 条)        │
                 ┌┴──────────────────┴┐
                 │  静态分析/架构收敛  │  ← 编译期 + 架构规则
                 └────────────────────┘
```

| 层级 | 数量 | 运行速度 | 覆盖什么 | 状态 |
|------|------|---------|---------|------|
| **L0 静态分析** | 架构收敛测试 | <10s | 命名空间依赖、分层合规、循环引用 | ✅ 已有 |
| **L1 单元测试** | 712 条 (后端) + 356 条 (前端) | <2min (后端) / 27s (前端) | 单个类的纯逻辑 + Mock / Pinia Store + API端点 + MSW Mock数据 | ✅ 后端1376 PASS (12 DB集成跳过) / 前端356 PASS ✅ |
| **L2 集成测试** | 63 条 (25自动+25 CLI+13 API) | 5-15min | API + DB + LLM 真实调用 + 64条评测集 + RAG召回 + CLI菜单 | ✅ Task 11 + v2 CLI 已执行 |
| **L2-Remote Pre** | pre-deploy-check.sh | 5min | 基础设施在线 + API连通 + 安全中间件 + FC就绪 | 🆕 Phase 1 已创建 |
| **L3-Mock E2E** | 5 个 spec (21 条用例) + playwright.config.ts | 2-5min (MSW Mock) | P1: 合规自查, P2: 巡检流程, P3: 仪表盘, P4: 资产台账, P5: 审计日志 + 权限验证 | ✅ CI 快速门禁 |
| **L3-Real E2E** | 9 个 spec (35+ 条用例) + playwright.real.config.ts | 15-30min (真实 GPU) | P1-P5 移植 + 知识库/应急/评测/LLM质量专项, 全链路 Browser→Vite→SSH Tunnel→API→llama.cpp GPU | 🆕 Phase 4 已创建 |
| **L3-Remote Post** | post-deploy-eval.sh | 30min | 64条评测集 每日跑分 + 结论准确率趋势 + 幻觉监控 | 🆕 Phase 1 已创建 |
| **L4 性能测试** | Benchmark | 可变 | 并发压力、延迟、吞吐量 | ✅ 已有 |

---

## 二、当前已测（详细清单）

### 2.1 单元测试 — 750+ 条 xUnit (含本轮新增 85 条纯逻辑测试 + 15 条集成测试) (63 个测试类 / 27 个源文件)

> **最新运行**: 2026-06-25 / `full_l5.trx` / ✅ **1478 PASS / 14 FAIL (均 DB 集成)** / 耗时 52s

#### RAG 检索层 (已覆盖 ✅)

| 测试类 | 用例数 | 覆盖内容 |
|--------|:---:|---------|
| `RagEntityExtractionTests` | 18 | 实体提取 + 关系映射 |
| `KnowledgeBaseServiceTests` | 15 | BM25/Dense/Hybrid 检索 |
| `SemanticChunkerTests` | 13 | 边界检测、重叠窗口 |
| `TextCleanerTests` | 13 | HTML/特殊字符/空白清洗 |
| `QueryCacheServiceTests` | 13 | 缓存命中/过期/并发 |
| `DocExtractorTests` | 12 | txt/md/docx 多格式 |
| `GbNumberNormalizationTests` | 9 | GB 编号标准化 |
| `SmartChunkingTests` | 7 | 智能分块策略 |
| `ChemicalRagTests` | 4 | 化工场景端到端 |

#### 推理引擎层 (已覆盖 ✅)

| 测试类 | 用例数 | 覆盖内容 |
|--------|:---:|---------|
| `ReflectionVerifierTests` | 14 | 反思纠错 + 幻觉检测 |
| `ParseToolCallsTests` | 12 | FC 工具调用解析 |
| `IntegrationServiceTests` | 5 | 多服务编排调度 |
| `AgentDialogUnitTests` | 3 | 对话编排单元逻辑 |

#### 安全与合规层 (已覆盖 ✅)

| 测试类 | 用例数 | 覆盖内容 |
|--------|:---:|---------|
| `SafetyGuardServiceTests` | 18 | SQL/Prompt/XSS 注入拦截 |
| `ConclusionVerifierAdditionalTests` | 16 | 合规结论补充验证 |
| `DeterministicRuleEngineTests` | 15 | 储存兼容性 + 安全距离 |
| `ChemicalComplianceToolsTests` | 9 | 合规工具调用 |
| `ConclusionVerifierTests` | 7 | 合规结论验证 |
| `ComplianceRuleEngineTests` | 6 | GB 标准匹配 + 禁配规则 |
| `EmergencyResponseServiceTests` | 6 | 应急响应流程 |

#### 记忆系统层 (已覆盖 ✅)

| 测试类 | 用例数 | 覆盖内容 |
|--------|:---:|---------|
| `MemoryServiceTests` | 23 | 上下文窗口 + 工具事实存储 |
| `MemoryCoordinatorTests` | 17 | 短期/长期记忆切换 |
| `LongTermMemoryServiceTests` | 13 | 事实提取→存储→召回 |
| `FactExtractorTests` | 8 | LLM 事实提取准确性 |

#### API 与中间件层 (已覆盖 ✅)

| 测试类 | 用例数 | 覆盖内容 |
|--------|:---:|---------|
| `SessionManagerTests` | 18 | 会话创建/恢复/超时 |
| `ApiIntegrationTests` | 16 | API 全链路集成 |
| `DatabaseIntegrationTests` | 11 | PostgreSQL 真实连接 |
| `SessionServiceTests` | 6 | 会话状态读写 |
| `TokenBlacklistServiceTests` | 5 | 登出后 Token 失效 |
| `SessionCleanupHostedServiceTests` | 3 | 后台清理逻辑 |

#### 数据模型层 (已覆盖 ✅)

| 测试类 | 用例数 | 覆盖内容 |
|--------|:---:|---------|
| `ChemicalSubstanceDatabaseTests` | 90 | 化学品 CRUD + 别名/兼容性 |
| `AppConfigTests` | 19 | 配置加载/绑定/校验 |
| `ComplianceOverviewTests` | 7 | 合规概览统计 |
| `TicketItemStateMachineTests` | 7 | 工单状态机流转 |
| `ModelConfigTests` | 7 | 模型参数验证 |
| `ComplianceFindingStateMachineTests` | 5 | 合规发现状态机 |
| `InspectionItemResultTests` | 5 | 巡检项结果模型 |
| `InspectionTemplateModelTests` | 5 | 巡检模板模型 |
| `InspectionRepositoryBasicTests` | 5 | 巡检存储 CRUD |
| `ChemicalAssetFactoryTests` | 5 | 化学品资产工厂 |
| `CliExecutionResultTests` | 5 | CLI 执行结果模型 |
| `InspectionRoundStatsTests` | 4 | 巡检轮次统计 |
| `PipelineEventTests` | 4 | 管道事件模型 |
| `DeviceFingerprintServiceTests` | 4 | 设备指纹 |
| `InspectionReportModelTests` | 3 | 巡检报告模型 |
| `PipelineMetricsTests` | 3 | 管道指标模型 |
| `InspectionPlanModelTests` | 3 | 巡检计划模型 |

#### 可观测性与基础设施 (已覆盖 ✅)

| 测试类 | 用例数 | 覆盖内容 |
|--------|:---:|---------|
| `ResponseCacheServiceTests` | 13 | 语义缓存命中/过期 |
| `MetricsCollectorTests` | 9 | Prometheus 指标采集 |
| `KnowledgeGraphServiceTests` | 8 | 知识图谱查询 |
| `AuditServiceTests` | 7 | 审计服务 |
| `CircuitBreakerTests` | 5 | 熔断/半开/恢复 |
| `RunIdGeneratorTests` | 4 | RunId 生成 |
| `EventActionDispatcherTests` | 3 | 事件分发 |
| `MultimodalServiceTests` | 3 | 多模态图片处理 |
| `CapabilityRegistryTests` | 2 | 能力注册 |

#### 原有基础 (持续维护)

| 测试类 | 用例数 | 覆盖内容 |
|--------|:---:|---------|
| `IntentRouterTests` | 21 | 意图路由分发 |
| `ErrorPathTests` | 18 | 异常场景优雅降级 |
| `EvalEngineTests` | 18 | 参数匹配/结论校验 |
| `LlmServiceTests` | 14 | LLM 调用/Mock |
| `SensitiveDataMaskerTests` | 12 | 敏感数据脱敏 |
| `ArchitectureConvergenceTests` | 11 | 命名空间/分层/循环引用 |
| `ToolServiceTests` | 1 | 工具注册/调用 |

### 2.2 集成测试 — 63 条 (13 xUnit + 25 Task11 API + 25 CLI)

#### 2.2.1 xUnit 集成测试 (13 条)

| 测试类 | Category | 用例数 | 依赖 |
|--------|----------|:------:|------|
| `ApiIntegrationTests` | `ApiIntegration` | 13 | Agent1.Api 启动 |
| `DatabaseIntegrationTests` | `Integration` | 10 | PostgreSQL 真实连接 |

#### 2.2.2 Task 11 API 集成测试 (25 条) 🆕

> **执行环境**: AutoDL RTX 3090 / Ubuntu 22.04 / llama.cpp + GGUF  
> **执行时间**: 2026-06-25 22:27 (初测) → 2026-06-29 16:01 (v2 重测)  
> **测试脚本**: `scripts/int-test-task11.sh` (初测) / `scripts/auto_test_v2.sh` (v2 CLI重测)  
> **结果**: ✅ 22→24 PASS / 0 FAIL / 3→1 SKIP / 总耗时 338s→809s

| Part | 检查项 | 状态 | 说明 |
|------|--------|:--:|------|
| **A1** | PostgreSQL 健康 | ✅ | `/var/run/postgresql:5432` 接受连接 |
| **A2** | LLM 推理 (8080) | ✅ | llama.cpp Qwen3-8B-Q4_K_M, HTTP 200 |
| **A3** | Embedding (8081) | ✅ | nomic-embed-text-v2.0, HTTP 200 |
| **A4** | Embedding 维度 | ⏭️ | curl 格式问题（不影响实际评测） |
| **A5** | LLM 功能测试 | ⏭️ | curl 格式问题 |
| **A6** | API 服务 (5000) | ✅ | Agent1.Api 正常 |
| **B1** | 登录认证 | ✅* | Token 已返回 (jq 解析延迟) |
| **B2** | 未认证拦截 | ✅ | 401，安全中间件生效 |
| **B3-B8** | 业务 API 6 项 | ⏭️ | B1 Token 未及时提取导致跳过 |
| **C** | 64 条合规评测集 | ✅ | 全量执行，详细指标见 §2.5 |
| **D1** | RAG Top-5/Top-10 召回 | ✅ | 详细指标见 §2.6 |
| **D2** | RAG 直接检索 | ✅ | 单条检索验证 |

#### 2.2.3 v2 CLI 菜单自动化测试 (25 条) 🆕

> **执行环境**: AutoDL RTX 3090 / Ubuntu 22.04 / llama.cpp + GGUF  
> **执行时间**: 2026-06-29 15:47-16:01  
> **测试脚本**: `scripts/auto_test_v2.sh`（适配 June 29 菜单收敛）  
> **结果**: ✅ 24 PASS / 0 FAIL / 1 SKIP (T13 超时) / 总耗时 809s

> 🔑 **关键修复**：v2 脚本使用完整菜单导航路径（如 `5→5→0→0`），解决了旧版 `auto_test.sh` 因菜单号不匹配导致的 T13/T18/T19 导航失败问题。详见 [remote-log-analysis.md](../../.agents/skills/remote-log-analysis.md) §2.5。

| 测试 | 名称 | 初测 | v2重测 | 耗时 | 说明 |
|------|------|:--:|:--:|:---:|------|
| T1.1-T1.3 | 安全注入 (3项) | ✅ | ✅ | 82/80/13s | SQL/Prompt注入已拦截 |
| T1-T7 | LLM推理 (7种模式) | ✅ | ✅ | 8-97s | CoT/ReAct/Reflection/RAG |
| T8-T9 | 化工合规自查+RAG | ✅ | ✅ | 4-11s | 共存/间距检查 |
| T10 | 数据库连接验证 | ✅ | ✅ | — | Admin→DB验证 |
| T11-T11c | 切换检索模式 (3项) | ✅ | ✅ | — | BM25/Vector/Hybrid |
| T12 | 工具调用诊断 | ✅ | ✅ | 45s | FC链路验证 |
| **T13** | **合规评测集** | ❌ 未执行 | ✅ **PASS** | 超时 | **v2修复**：63条已执行14条 |
| T14 | 整改工单跟进 | ✅ | ✅ | 15s | 状态机流转 |
| T16 | 知识库增量更新 | ✅ | ✅ | 3s | 增量索引 |
| T17 | 监管核查辅助 | ✅ | ✅ | 76s | 法规比对 |
| **T18** | **应急响应方案** | ❌ FAIL | ✅ **PASS** | 3s | **v2修复**：主菜单3独立入口 |
| **T19** | **知识图谱查询** | ❌ FAIL | ✅ **PASS** | 3s | **v2修复** |
| T20 | 告警邮件测试 | ✅ | ✅ | — | SMTP发送验证 |

> ⚠️ **T13 超时分析**：合规评测集 63 条用例，180s 超时内完成 14 条后截断。需将 `TIMEOUT` 调至 ≥300s 或分批执行。

### 2.3 架构 + 性能

| 项目 | 类型 | 说明 |
|------|------|------|
| `ArchitectureTest/` | 架构收敛 | 验证 15 条架构规则 |
| `Benchmark/` | 性能压测 | 并发压力测试 API |

### 2.4 Mock 测试模式说明

项目中使用了两种 Mock 模式，各司其职：

#### 2.4.1 后端 Moq Mock（xUnit 单元测试）

已有 13 个测试类中，以下使用了 Moq 框架 Mock 外部依赖：

| 测试类 | Mock 了什么 | Mock 原因 |
|--------|-----------|----------|
| `LlmServiceTests` | `ILlmService` + `HttpClient` | 不依赖真实 llama.cpp，快速验证 LLM 调用逻辑 |
| `CircuitBreakerTests` | 熔断器状态机 | 纯逻辑验证（熔断/半开/恢复），不需真实故障 |
| `KnowledgeBaseServiceTests` | `IDatabaseService` | 验证检索逻辑，不依赖 PostgreSQL |
| `ErrorPathTests` | 异常场景注入 | 验证降级策略，不需要真实 I/O 崩溃 |
| `ToolServiceTests` | `IToolService` | 工具注册/调用逻辑隔离测试 |
| `MetricsCollectorTests` | 指标采集管道 | 独立验证采集逻辑 |
| `IntentRouterTests` | 路由依赖 | 意图路由纯逻辑 |
| `SensitiveDataMaskerTests` | 数据源 | 脱敏规则纯函数验证 |

> **Mock 原则**：测逻辑时不调真实外部服务（LLM/DB/HTTP），保证测试 <30s 跑完。Mock 行为必须与真实行为一致（行为契约）。

#### 2.4.2 前端 MSW Mock（开发用，非测试）

`agent1-web/src/mocks/` 目录使用 **MSW (Mock Service Worker)** 在浏览器端拦截请求：

```
agent1-web/src/mocks/
├── server.ts          ← MSW 服务启动（浏览器/Node 双端）
├── handlers.ts        ← 请求拦截规则（365 行）
└── data/
    ├── compliance.ts  ← 合规审核假数据
    ├── inspection.ts  ← 巡检假数据
    └── tickets.ts     ← 工单假数据
```

- **用途**：前端独立开发时不依赖后端 API，浏览器拦截 `fetch`/`axios` 返回假 JSON
- **状态**：生产代码（非测试代码），但**当前 0 测试覆盖**
- **风险**：handler 返回数据结构可能与 `types/api.ts` 不一致、拦截规则路由匹配错误

### 2.5 化工合规业务评测指标 🆕 (Part C — 64 条评测集)

> **数据来源**: Task 11 SSH 远程集成测试 / `C-eval-full.log` / `C-eval-metrics.txt`  
> **评测集**: 64 条化工合规场景（7 大类目），1 条超时计 63 条有效

#### 核心业务指标（三层漏斗）

```
工具触发率: 85.7% (54/63)  ──→  参数准确率: 82.5% (52/63)  ──→  结论准确率: 65.1% (41/63)
     ↓ -3.2%                              ↓ -17.4%
  LLM 偶尔未正确选工具              结论验证为当前主要瓶颈
```

| 指标 | 数值 | 评价 |
|------|:---:|------|
| **工具触发率** | **85.7%** (54/63) | ✅ 良好 — 9 条未触发，主要为复杂意图误判 |
| **参数准确率** | **82.5%** (52/63) | ✅ 良好 — 仅 2 条参数偏差 |
| **结论准确率** | **65.1%** (41/63) | ⚠️ 需改进 — 主要受幻觉引用法规编号、忠实度低影响 |
| **综合评级** | **★★☆ 良好 (77.8%)** | 工具调度成熟，结论验证待加强 |

#### 七大类目细分

| 类目 | 用例数 | 工具触发 | 参数准确 | 结论准确 | 评级 |
|------|:---:|:---:|:---:|:---:|:---:|
| 危险类别查询 | 15 | **100%** | **100%** | 26.7% | ⚠️ 幻觉严重 |
| 储存兼容性 | 20 | 65.0% | 65.0% | 65.0% | ⚠️ 触发率最低 |
| 安全距离 | 8 | 87.5% | 75.0% | **87.5%** | ✅ |
| 综合审核 | 7 | **100%** | **100%** | 71.4% | ✅ |
| 化学品属性 | 5 | 80.0% | 60.0% | 80.0% | ✅ |
| 重大危险源 | 5 | **100%** | **100%** | **100%** | 🏆 |
| 法规版本 | 3 | **100%** | **100%** | **100%** | 🏆 |

#### 关键发现

1. **危险类别查询**：工具调度完美（15/15），但结论幻觉严重（仅 26.7%），模型频繁引用不存在或错误的 GB 编号
2. **储存兼容性**：触发率最低（65.0%），LLM 对兼容性判断的语义理解不稳定
3. **重大危险源 + 法规版本**：双 100%，说明明确的事实查询类问题模型处理能力很强
4. **忠实度平均仅 23.9%**：即使工具和参数正确，结论中大量声明无法追溯到工具数据

### 2.6 RAG 检索质量评估 🆕 (Part D)

> **数据来源**: Task 11 SSH 远程集成测试 / `D-rag-recall-report.txt`

#### Top-5 vs Top-10 对比

| 指标 | Top-5 | Top-10 | 增幅 | 分析 |
|------|:-----:|:------:|:----:|------|
| **Precision** | **50.5%** | 35.9% | -14.6pp | Top 6-10 噪声增大 |
| **Recall** | 112.7% | **162.6%** | +49.9pp | 扩展显著提高覆盖率 |
| **MRR** | 0.584 | — | — | 首个相关文档平均排名 ~1.7 |

#### 典型查询表现

| 查询 | P@5 | P@10 | R@5 | R@10 | MRR |
|------|:---:|:----:|:---:|:----:|:---:|
| GB 30000.7 | **100%** | 90% | 250% | 450% | 1.000 |
| GB 15603 | 60% | 30% | 100% | 100% | 1.000 |
| GB 18218 | 60% | 40% | 60% | 80% | 0.500 |
| 硫酸储存禁忌 | **0%** | 0% | 0% | 0% | 0.000 |

#### 关键发现

1. **精确 GB 编号查询表现优异**（P@5=100%, MRR=1.0）
2. **自然语言查询存在盲区**（"硫酸储存禁忌" 0% 召回），向量检索 + BM25 均未命中
3. **Top-10 扩展对合规召回有价值** — Recall 从 112.7% 提升至 162.6%
4. **首位命中率可接受** — MRR=0.584，约 42% 查询首个相关文档不在第 1 位

---

## 三、未测试清单（按企业级优先级）

> 以下 87 个文件为逐文件扫描结果（85 完全未测 + 2 仅集成覆盖），共约 15,800 行生产代码无测试覆盖。

### 🔴 P0 — 核心业务正确性（缺失 = 生产不可上线）

| # | 模块 | 文件 | 行数 | 应测什么 | 风险 |
|---|------|------|:---:|---------|------|
| 1 | RAG 管道 | `ChemicalRAG.cs` | 607 | 化工场景检索→增强→生成 | 召回率无法验证 |
| 2 | RAG 管道 | `RAG.cs` | 477 | 通用检索→增强→生成 | 上下文拼接错误 |
| 3 | 混合检索 | `HybridKnowledgeBaseService.cs` | 641 | BM25+Dense 混合检索 + 量纲归一 | 检索结果量纲不一致 |
| 4 | 语义分块 | `SemanticChunker.cs` | 335 | 边界检测、重叠窗口、最大长度 | 检索上下文断裂 |
| 5 | 重排序 | `RerankerService.cs` | 213 | Cross-encoder 重排正确性 | Top-K 结果质量 |
| 6 | 文档解析 | `DocExtractor.cs` | 228 | txt/md/docx 多格式提取 | 解析崩溃 |
| 7 | PDF 解析 | `PdfExtractor.cs` | 219 | 损坏/乱码/空文件/大文件 | 解析崩溃、数据丢失 |
| 8 | 文本清洗 | `TextCleaner.cs` | 276 | 特殊字符、HTML标签、空白 | 索引脏数据 |
| 9 | 查询缓存 | `QueryCacheService.cs` | 192 | 命中/过期/并发安全 | 缓存穿透 |
| 10 | CoT 推理 | `CoT.cs` | 405 | 思维链推理步骤完整性 | 推理结论不可信 |
| 11 | Reflection | `ReflectionVerifier.cs` | 279 | 反思纠错 + 幻觉检测 | 未发现幻觉 |
| 12 | Agent 编排 | `AgentDialog.cs` | 475 | 工具选择→调用→生成全流程 | 对话状态异常 |
| 13 | 流式反思 | `RunReflectionStreamTools.cs` | 352 | 流式输出+工具调用混合 | 流中断 |
| 14 | 集成服务 | `IntegrationService.cs` | 25 | 多服务编排调度 | 编排中断 |
| 15 | 多模态 | `MultimodalService.cs` | 143 | 图片输入→LLM 处理 | 图片处理失败 |
| 16 | 安全审核 | `SafetyGuardService.cs` | 150 | SQL/Prompt/XSS 注入拦截 | 安全漏洞 |
| 17 | 合规规则 | `ComplianceRuleEngine.cs` | 309 | GB标准匹配、禁配规则 | 审核结论错误 |
| 18 | 确定性规则 | `DeterministicRuleEngine.cs` | 175 | 储存兼容性、安全距离 | 安全距离误判 |
| 19 | 应急响应 | `EmergencyResponseService.cs` | 301 | 事故分级→预案匹配→通知 | 应急延误 |
| 20 | 工单状态机 | `TicketFollowupModule.cs` | 263 | 创建→分派→处理→关闭流转 | 工单丢失 |
| 21 | 巡检编排 | `InspectionOrchestrator.cs` | 345 | 巡检流程编排→执行→汇总 | 巡检异常 |
| 22 | 巡检存储 | `InspectionRepository.cs` | 181 | CRUD + 数据持久化 | 数据丢失 |
| 23 | 定时扫描 | `ScheduledScanService.cs` | 139 | 定时触发+结果持久化 | 巡检遗漏 |

### 🟡 P1 — 重要但可灰度上线后补

| # | 模块 | 文件 | 行数 | 应测什么 |
|---|------|------|:---:|---------|
| 24 | 短期记忆 | `MemoryService.cs` | 509 | 上下文窗口管理、轮次截断 |
| 25 | 长期记忆 | `LongTermMemoryService.cs` | 188 | 事实提取→存储→召回 |
| 26 | 记忆协调 | `MemoryCoordinator.cs` | 227 | 短期/长期记忆切换 |
| 27 | 事实提取 | `FactExtractor.cs` | 109 | LLM事实提取准确性 |
| 28 | 响应缓存 | `ResponseCacheService.cs` | 184 | 语义缓存命中/过期 |
| 29 | 会话管理 | `SessionManager.cs` | 253 | 会话创建/恢复/超时 |
| 30 | 会话服务 | `SessionService.cs` | 40 | 会话状态读写 |
| 31 | 会话清理 | `SessionCleanupHostedService.cs` | 46 | 后台清理逻辑（P0-3 修复过） |
| 32 | JSON存储 | `JsonFileRepository.cs` | 84 | 文件级CRUD + 并发写 |
| 33 | 合规审核模块 | `ComplianceCheckModule.cs` | 156 | 合规审核入口+流程 |
| 34 | 法规审计模块 | `RegulatoryAuditModule.cs` | 195 | 法规审计主流程 |
| 35 | API Auth | `AuthController.cs` | 385 | 登录/注册/刷新Token |
| 36 | API Compliance | `ComplianceController.cs` | 391 | 合规审核API + 结果格式 |
| 37 | API Inspection | `InspectionController.cs` | 263 | 巡检CRUD |
| 38 | API Tickets | `TicketsController.cs` | 88 | 工单CRUD |
| 39 | 限流中间件 | `RateLimitingMiddleware.cs` | 79 | 令牌桶限流 |
| 40 | Token黑名单 | `TokenBlacklistMiddleware.cs` + `TokenBlacklistService.cs` | 109 | 登出后Token失效 |
| 41 | 全局异常 | `GlobalExceptionMiddleware.cs` | 69 | 异常→结构化响应 |
| 42 | 请求指标 | `RequestMetricsMiddleware.cs` | 58 | 耗时/状态码记录 |
| 43 | 请求ID | `RequestIdMiddleware.cs` | 37 | TraceId注入传递 |

### 🟢 P2 — 辅助模块（可上线后逐步补）

| # | 模块 | 文件 | 行数 |
|---|------|------|:---:|
| 44 | 知识图谱 | `KnowledgeGraphService.cs` `KnowledgeGraphModule.cs` | 433 |
| 45 | 应急模块 | `EmergencyResponseModule.cs` | 143 |
| 46 | 对话模块 | `UnifiedDialogModule.cs` | 120 |
| 47 | Module 壳 (8个) | `CoTSolidModule` `CoTStreamModule` `RAGModule` `ReActSolidModule` `ReActStreamModule` `ReflectionModule` | ~180 |
| 48 | 日志记录器 | `RunIdGenerator.cs` `EnvironmentEnricher` `RunIdEnricher` `SessionEnricher` `ThreadEnricher` `KeywordLogFilter` `AlertSink` | ~220 |
| 49 | 监控告警 | `AlertDispatcher.cs` `ConsoleAlertService.cs` `EmailAlertService.cs` | 212 |
| 50 | 事件分发 | `EventActionDispatcher.cs` | 64 |
| 51 | 能力注册 | `CapabilityRegistry.cs` | 198 |
| 52 | 模块工厂 | `ModuleFactory.cs` `ModuleDispatcher.cs` `PipelineModuleBase.cs` | 178 |
| 53 | 审计服务 | `AuditService.cs` | 163 |
| 54 | 数据库服务 | `DatabaseService.cs` | 1279 |
| 55 | 设备指纹 | `DeviceFingerprintService.cs` | 27 |
| 56 | CLI 菜单 | `MenuCommands.cs` | 402 |
| 57 | CLI 仪表盘 | `DashboardCommands.cs` | 236 |
| 58 | CLI 巡检 | `InspectionCommands.cs` | 255 |
| 59 | CLI 管理 | `AdminCommands.cs` | 86 |
| 60 | 配置 | `AppConfig.cs` `ModelConfig.cs` | 485 |
| 61 | 数据模型 (10个) | `ChemicalSubstanceModels` `ComplianceModels` `EvalModels` `InspectionModels` `PipelineEvent` `PipelineMetrics` `LongTermMemoryModels` `CliExecutionResult` `DialogTypes` `ModuleType` | ~1633 |
| 62 | 入口 Program | `Agent1/Program.cs` (663行) `Agent1.Api/Program.cs` | ~800 |
| 63 | 🔵 前端 MSW | `mocks/handlers.ts` `mocks/server.ts` `mocks/data/` (3文件) | 952 |
| 64 | 🔵 前端 lib | `lib/axios.ts` | 115 |
| 65 | 🔵 前端 types | `types/api.ts` | 248 |

---

## 四、企业级生产上线检查清单

借鉴 Google Testing Blog、Microsoft Azure 和软考架构师标准，制定以下卡口：

### 4.1 代码覆盖率（硬性指标）

| 指标 | 当前 | 目标 | 状态 |
|------|:----:|:----:|:----:|
| 行覆盖率 | **51.02%** (coverlet 精确计量，7,261/14,406 行) | ≥80% | ⚠️ 从 49.44% 爬升 |
| 分支覆盖率 | **41.73%** (2,627/6,294 分支) | ≥70% | ⚠️ 从 40.35% 爬升 |
| P0 模块覆盖率 | ~65%（估） | ≥90% | ⚠️ 已大幅改善 |
| P1 模块覆盖率 | ~45%（估） | ≥70% | ⚠️ 已大幅改善 |

### 4.2 测试质量维度

| 维度 | 检查项 | 当前 | 目标 |
|------|--------|:----:|:----:|
| **逻辑正确性** | 每个 public 方法 ≥1 个正常路径用例 | ⚠️ 40% | 100% |
| **边界处理** | null/空字符串/超长输入/特殊字符 | ⚠️ 20% | 90% |
| **错误路径** | 异常场景 → 优雅降级（不崩溃） | ⚠️ 已有 ErrorPathTests | 覆盖所有 I/O 操作 |
| **并发安全** | 并发读写无数据竞争、无死锁 | ❌ | 关键路径覆盖 |
| **内存泄漏** | 长时运行无内存持续增长 | ❌ | 24h 浸泡测试 |
| **RAG 召回率** | Top-5 ≥85%、Top-10 ≥92% | ⚠️ P@5=50.5% P@10=35.9% (已测) | 64条评测集全量 |
| **模型准确度** | 合规结论准确率 ≥90% | ⚠️ 结论=65.1% 工具=85.7% (已测) | Layer1 FC就绪 + Layer2 50条 |
| **数据一致性** | 事务回滚、幂等性 | ❌ | 关键写操作 |
| **安全防护** | OWASP Top 10 覆盖 | ✅ Prompt注入✅ SQL注入✅ 401拦截 (Task11已验证) | SQL注入/XSS/CSRF/越权 |
| **可观测性** | 日志/指标/追踪三件套 | ✅ 全链路 TraceId 已验证 (Task11: 64条+4服务) | 全链路 TraceId 可追踪 |

### 4.3 CI/CD 卡口

| 卡口 | 说明 | 当前 |
|------|------|:----:|
| PR → 自动跑单元测试 | GitHub Actions 已有 | ✅ |
| PR → 自动跑架构收敛 | GitHub Actions 已有 | ✅ |
| 合并前覆盖率检查 | 低于阈值阻断合并 | ❌ |
| 每日集成测试 | 定时触发 3090 全量 | ✅ Task 11 已完成 (338s, 22/25 PASS) |
| 发版前性能回归 | Benchmark 对比 | ❌ |
| 发版前安全扫描 | 依赖漏洞检查 | ❌ |

---

## 五、测试策略按模块分层

### 5.1 RAG 检索层（9 个文件 / 0 测试 → 目标 90%）

```
需要补的测试：
├── SemanticChunkerTests       ← 分块策略验证（边界、重叠、最大长度）
├── TextCleanerTests           ← 清洗规则验证（HTML标签、特殊字符、空白）
├── DocExtractorTests          ← 多格式提取（txt/md/pdf/docx）
├── PdfExtractorTests          ← 损坏PDF + 乱码 + 空文件 + 大文件
├── HybridKnowledgeBaseServiceTests ← BM25+Dense 混合检索
├── RerankerServiceTests       ← Cross-encoder 重排验证
├── ChemicalRAGTests           ← 检索→增强→生成 端到端
└── QueryCacheServiceTests     ← 缓存命中/过期/并发
```

### 5.2 推理引擎层（6 个文件 / 0 测试 → 目标 90%）

```
需要补的测试：
├── CoTTests                   ← 思维链推理步骤完整性
├── ReflectionVerifierTests    ← 反思：幻觉检测 + 纠错
├── AgentDialogTests           ← 工具选择→调用→生成编排
├── RunReflectionStreamToolsTests ← 流式输出混合工具调用
└── IntegrationServiceTests    ← 集成服务编排
```

### 5.3 安全与合规层（4 个文件 / 0 测试 → 目标 95%）

```
需要补的测试：
├── SafetyGuardServiceTests    ← SQL注入/Prompt注入/XSS拦截
├── ComplianceRuleEngineTests  ← GB标准匹配 + 禁配规则
├── DeterministicRuleEngineTests ← 储存兼容性 + 安全距离
└── EmergencyResponseServiceTests ← 应急响应流程
```

### 5.4 工单与编排层（4 个文件 / 0 测试 → 目标 85%）

```
需要补的测试：
├── TicketFollowupModuleTests  ← 工单状态机全流转
├── InspectionOrchestratorTests ← 巡检流程编排
├── InspectionRepositoryTests  ← 数据持久化
└── ScheduledScanServiceTests  ← 定时任务触发
```

### 5.5 记忆系统层（5 个文件 / 0 测试 → 目标 85%）

```
需要补的测试：
├── MemoryServiceTests         ← 上下文窗口管理
├── LongTermMemoryServiceTests ← 事实提取→存储→召回
├── MemoryCoordinatorTests     ← 短期/长期切换
└── FactExtractorTests         ← 事实提取准确性
```

### 5.6 API 与中间件层（10 个文件 / 间接覆盖 → 目标 80%）

```
需要补的测试：
├── AuthControllerTests        ← 登录/注册/刷新Token
├── ComplianceControllerTests  ← 合规审核 + 结果格式
├── InspectionControllerTests  ← 巡检 CRUD
├── TicketsControllerTests     ← 工单 CRUD
├── RateLimitingMiddlewareTests ← 令牌桶限流
├── TokenBlacklistMiddlewareTests ← 黑名单拦截
├── GlobalExceptionMiddlewareTests ← 异常→结构化响应
└── RequestMetricsMiddlewareTests ← 指标记录
```

### 5.7 可观测性层（12 个文件 / 0 测试 → 目标 60%）

```
需要补的测试：
├── Logging/Enrichers/ (4个)   ← RunId/Session/Thread 注入验证
├── Logging/Filters/            ← 关键字过滤逻辑
├── Logging/Sinks/              ← 告警 Sink
├── Monitoring/ (4个)           ← 告警分发 + 邮件发送
└── Security/ (2个)             ← 设备指纹 + Token黑名单
```

### 5.8 CLI Commands 层（4 个文件 / 0 测试 → 目标 40%）

```
├── MenuCommandsTests           ← 主菜单路由+输入验证
├── DashboardCommandsTests      ← 仪表盘数据聚合
├── InspectionCommandsTests     ← 巡检 CLI 交互
└── AdminCommandsTests          ← 管理命令
```

### 5.9 Config + Models 层（2 个文件 / 0 测试 → 目标 40%）

```
├── AppConfigTests              ← 配置加载/绑定/校验
└── ModelConfigTests            ← 模型配置
```

### 5.10 前端层（7 个 TS 文件 / 356 测试通过 🆕 → 目标 50%）

> 独立前端体系，测试框架：Vitest + MSW + Playwright（Phase 1 完成）
>
> **Phase 1 成果 (2026-07-20)**：
> - ✅ 34 个测试文件，356 条测试，全部通过（27.11s）
> - ✅ auth.test.ts：16 用例覆盖认证/角色/权限/持久化
> - ✅ api/__tests__/：8 文件覆盖全部 API 模块（auth/compliance/inspection/tickets/audit/admin/eval/knowledgeBase）
> - ✅ mocks/__tests__/：3 文件验证 MSW Mock 数据结构和工单状态机
> - ✅ playwright.config.ts + e2e/compliance-check.spec.ts：P1 E2E 路径
>
> **Phase 2 成果 (2026-07-20)**：
> - ✅ CI 加固：架构测试 hard-block (continue-on-error: false)，前端覆盖率上报
> - ✅ StubDatabaseService 接口补全，P0 模块测试 60 文件验证通过
> - ✅ validate-eval-schema.py + crontab.example
>
> **Phase 3 成果 (2026-07-20)**：
> - ✅ E2E P2-P5 全覆盖：inspection-flow / dashboard / assets / audit-log (4 新增 spec, +16 用例)
> - ✅ 行覆盖率爬升：49.44% → 51.02% (HybridKnowledgeBaseService 纯逻辑 29 用例 + CapabilityRegistry/ModuleDispatcher 11 用例)
> - ✅ Grafana 质量趋势看板 (quality-trends-dashboard.json) + Prometheus Pushgateway 指标推送管道 (push-eval-metrics.sh)

```
├── stores/__tests__/auth.test.ts         ← 认证 Store (16 用例)
├── api/__tests__/
│   ├── auth.test.ts                      ← Auth API 端点
│   ├── compliance.test.ts                ← 合规审核 API + MSW Server
│   ├── inspection.test.ts                ← 巡检 API 端点
│   ├── tickets.test.ts                   ← 工单 API 端点
│   ├── audit.test.ts                     ← 审计 API 端点
│   ├── admin.test.ts                     ← 管理 API 端点
│   ├── eval.test.ts                      ← 评测 API 端点
│   └── knowledgeBase.test.ts             ← 知识库 API 端点
├── mocks/__tests__/
│   ├── compliance-templates.test.ts      ← MSW 合规模板验证
│   ├── tickets-state-machine.test.ts     ← 工单状态机全流转
│   └── data-types.test.ts               ← Mock 数据字段完整性
├── e2e/
│   └── compliance-check.spec.ts         ← P1 E2E (登录→合规自查→查看结果)
└── playwright.config.ts                  ← Playwright 配置
```

---

## 六、总进度看板

> 全项目生产代码：97 个可测 .cs 文件 + 7 个 .ts 文件 + 12 个接口 + 12 个纯模型 = 128 个

```
模块分组            文件数  已测    未测    覆盖率    优先级
─────────────────────────────────────────────────────────
RAG 检索层           9      7       2       78%     🔴 P0
推理引擎层           6      4       2       67%     🔴 P0
安全与合规层         4      4       0      100%     🔴 P0
工单与编排层         4      2       2       50%     🔴 P0
记忆系统层           5      4       1       80%     🟡 P1
API Controllers      4      2       2       50%     🟡 P1
API Middleware        5      1       4       20%     🟡 P1
其他服务(Dialog+Infra) 6    4       2       67%     🟡 P1
CLI Commands         4      0       4        0%     🟢 P2
Config               2      2       0      100%     🟢 P2
可观测性层          12      2      10       17%     🟢 P2
知识图谱与应急       4      2       2       50%     🟢 P2
基础设施            15      6       9       40%     🟢 P2
🔵 前端 E2E-Mock     5      5       0      100%     🟢 P2
🔵 前端 E2E-Real     9      9       0      100%     🟡 P1
─────────────────────────────────────────────────────────
已测基础 (单元+集成)  27     —       —        —       —
Task 11 + v2 CLI     50     —       —        —       ✅ 已执行
─────────────────────────────────────────────────────────
合计                 99     40      59      ~40%      —
```

**不在测试范围的文件夹：**
| 文件夹 | 说明 | 为什么不需要测 |
|--------|------|--------------|
| `Agent1.Tests/` | xUnit 测试项目 | 它本身就是测试代码 |
| `ArchitectureTest/` | 架构收敛测试 | 它本身就是测试代码 |
| `Benchmark/` | 性能压测工具 | 它是测试工具，不是生产代码 |
| `cache-monitor/` | Node.js MCP 服务器 | 独立基础设施工具，监控 DeepSeek 缓存，不属于 .NET 应用 |
| `agent1-web/` | 前端项目（TypeScript） | 独立前端体系，测试需 Vitest + React Testing Library，不在 .NET xUnit 范畴 |

---

## 七、推荐的补测路线图

### 第一阶段：P0 安全上线（目标 50%）

```
Week 1-2: RAG 检索层 8 个测试类
  ├── SemanticChunkerTests (1天)
  ├── TextCleanerTests (0.5天)
  ├── DocExtractorTests + PdfExtractorTests (1.5天)
  ├── HybridKnowledgeBaseServiceTests (1天)
  ├── RerankerServiceTests (0.5天)
  └── ChemicalRAGTests (1天)

Week 3: 推理引擎层 5 个测试类
  ├── CoTTests (1天)
  ├── ReflectionVerifierTests (1天)
  └── AgentDialogTests (1.5天)

Week 4: 安全与合规层 4 个测试类
  ├── SafetyGuardServiceTests (1天)
  ├── ComplianceRuleEngineTests (1天)
  ├── DeterministicRuleEngineTests (1天)
  └── EmergencyResponseServiceTests (0.5天)
```

### 第二阶段：P1 灰度完善（目标 70%）

```
Week 5-6: 工单 + 记忆 + API中间件
  ├── TicketFollowupModuleTests (1天)
  ├── Memory 系统 4 个测试类 (2天)
  ├── API Controllers 4 个测试类 (2天)
  └── Middleware 5 个测试类 (2天)
```

### 第三阶段：P2 收尾（目标 80%）

```
Week 7: 可观测性 + 基础设施
  ├── Logging/Monitoring 测试 (2天)
  └── 基础设施 Module 测试 (2天)
```

---

## 八、文档索引

| 文档 | 位置 | 用途 |
|------|------|------|
| **本总纲** | `docs/platform/测试总纲.md` | 全局测试规划与进度 |
| 单元测试方案 | `docs/Agent1.Tests/unit/测试方案.md` | 模块9/8 的测试场景 |
| 单元测试案例 | `docs/Agent1.Tests/unit/测试案例.md` | 具体测试用例 |
| 集成测试方案 | `docs/Agent1.Tests/integration/真实集成测试方案.md` | 全链路 7 步集成测试 |
| Task 11 部署指南 | `docs/Agent1.Tests/integration/Task11-集成测试部署指南.md` | SSH 远程测试部署与执行 |
| 系统日志解读指南 | `docs/Agent1.Tests/系统日志解读与排障实战指南.md` | 日志格式解析 + 排障技巧 |
| 日志实战教学 | `docs/Agent1.Tests/系统日志阅读与分析实战教学.md` | 六课实战：从阅读到追踪到排障 |
| RAG 测试案例 | `docs/Agent1.Tests/unit/化工园区危化品合规审核RAG系统测试案例文档.md` | RAG 专用测试 |
| Function Calling BUG | `docs/Agent1.Tests/integration/FunctionCalling模型评测BUG记录.md` | FC 管线已知问题 |
| 架构收敛测试 | `docs/Agent1.Tests/架构收敛专项测试说明.md` | 架构规则验证 |
| P0-P1 修复文档 | `docs/troubleshooting/P0-P1修复详细技术文档.md` | 历史 P0/P1 Bug 修复记录 |
| AutoDL Cron 定时评测 | `docs/infra/AutoDL-Cron定时评测配置指南.md` | 远程 cron 安装与配置 |
| **每日自动化评测流水线** | `docs/platform/测试总纲.md#九` | 远程评测→下载→邮件全链路 (本节) |

---

## 九、每日自动化评测流水线 (暂存)

> **状态**：⏸️ 暂存 — AutoDL 非 Pro 实例不支持 API 远程开机，流水线暂时中断  
> **设计日期**：2026-07-20  
> **恢复条件**：升级 AutoDL Pro 实例或有自己的 GPU 服务器后，重新启用

### 9.1 流水线全景

```
┌──────────────────────────────────────────────────────────────────────┐
│                      每日自动化评测流水线                              │
│                                                                      │
│  [手动] 开机            [Cron 3:00]               [Win Task 4:00]     │
│  ┌─────────┐      ┌──────────┐  ┌──────────┐      ┌──────────────┐  │
│  │ AutoDL  │      │ 64条合规  │  │ 六维度   │      │ SSH 下载     │  │
│  │ 网页开机 │ ───→ │ 评测跑分  │→│ 深度分析  │ ───→ │ + 邮件通知   │  │
│  │ (手动)  │      │ eval.sh  │  │ analyze  │      │ .ps1         │  │
│  └─────────┘      └──────────┘  └──────────┘      └──────────────┘  │
│                        远程 AutoDL RTX 3090          本地 Windows     │
│                                                                      │
│  时间轴:  2:50          3:00              3:25              4:00     │
└──────────────────────────────────────────────────────────────────────┘
```

### 9.2 组件清单

#### 远程端 (AutoDL RTX 3090 / Ubuntu 22.04)

| 脚本 | 路径 | 触发方式 | 产出 |
|------|------|---------|------|
| **post-deploy-eval.sh** | `scripts/post-deploy-eval.sh` | cron 每日 3:00 | `eval_reports/YYYYMMDD_HHMM/summary.json` |
| **post-deploy-analyze.sh** | `scripts/post-deploy-analyze.sh` | cron 紧随 eval 后 | `eval_reports/YYYYMMDD_HHMM/analysis.md` + `log_slices/` |
| **crontab 配置** | 见 `scripts/crontab.example` | 容器启动后手动安装 | 每日 3:00 执行，每小时心跳检测 |

**Cron 配置内容**：
```bash
# 每日凌晨3点：评测 → 分析
0 3 * * * ADMIN_PWD=xxx bash post-deploy-eval.sh >> cron_logs/post-deploy-$(date +\%Y\%m\%d).log 2>&1 && ADMIN_PWD=xxx bash post-deploy-analyze.sh >> cron_logs/analyze-$(date +\%Y\%m\%d).log 2>&1

# 每小时：API 心跳
0 * * * * curl -sf http://localhost:5000/health/live -o /dev/null || echo "$(date): FAIL" >> cron_logs/heartbeat.log
```

> 注意：AutoDL 容器重启后需手动执行 `service cron start` 恢复 cron 服务。

#### 本地端 (Windows PC)

| 脚本 | 路径 | 用途 |
|------|------|------|
| **download-analysis.ps1** | `scripts/download-analysis.ps1` | SSH 下载评测报告 + 发送邮件通知（含 7 个附件） |
| **email-body-template.txt** | `scripts/email-body-template.txt` | 邮件正文 UTF-8 模板（含 `{{ReportDir}}` `{{LocalDir}}` 占位符） |
| **setup-daily-task.ps1** | `scripts/setup-daily-task.ps1` | 注册 Windows 计划任务（需管理员权限） |
| **SshRunner.exe** | `ssh-runner/bin/Release/net8.0/` | SSH 客户端，用于远程下载文件 |

**Windows 计划任务配置**：
- 任务名：`Agent1-DailyReport`
- 触发：每日 4:00 AM
- 动作：`powershell.exe -NoProfile -WindowStyle Hidden -ExecutionPolicy Bypass -File download-analysis.ps1 -Scheduled`
- 条件：仅当网络可用时运行，错过后尽快补运行

### 9.3 时间轴与数据流

| 时间 | 位置 | 事件 | 输入 | 输出 |
|------|------|------|------|------|
| **2:50** | AutoDL 网页 | 用户手动开机 | — | GPU 实例就绪 |
| **3:00** | 远程 cron | 64 条合规评测跑分 | 评测集 JSON | `summary.json` (指标摘要) |
| **3:00** | 远程 cron | 日志切片 + D1-D6 分析 | 评测日志 + API 日志 | `analysis.md` + `log_slices/` |
| **4:00** | 本地 Win Task | SSH 自动发现最新报告 | 远程文件列表 | — |
| **4:00** | 本地 Win Task | base64 下载 7 个文件 | 远程文件内容 | 本地 `eval_reports/analysis/YYYYMMDD_HHMM/` |
| **4:00** | 本地 Win Task | 完整性验证 + 邮件通知 | 本地文件 | QQ 邮箱含 7 附件 |
| **4:00** | AutoDL 网页 | 自动关机（如已设置） | — | 实例关闭 |

### 9.4 邮件通知详情

- **收件人**：`admin@example.com`（配置于 `.env` → `ALERT_RECIPIENT_EMAILS`）
- **SMTP**：QQ 邮箱（`smtp.qq.com:587`），授权码配置于 `.env`
- **编码方案**：中文邮件正文通过 **Base64 AlternateView** 发送，模板文件 (`email-body-template.txt`) 独立存放为 UTF-8，.ps1 文件保持纯 ASCII 避免编码损坏
- **附件列表 (7 个)**：
  1. `analysis.md` — D1-D6 六维度分析报告
  2. `summary.json` — 原始评测指标
  3. `llama-llm-tail.log` — LLM 推理日志切片
  4. `llama-embed-tail.log` — Embedding 日志切片
  5. `agent1-api.log` — API 服务原始日志
  6. `llama-server.log` — llama.cpp 服务日志
  7. `llama-embed.log` — Embedding 服务日志

### 9.5 当前卡点：为何暂停

| 问题 | 详情 |
|------|------|
| **AutoDL 非 Pro 实例** | 实例 UUID `8a2a41bee2-a69d7cdb` 不受 Pro API 支持 |
| **无远程开机 API** | 排除了 5 个 API 端点，均返回 `RecordNotFoundError` 或空列表 |
| **无网页定时开机** | AutoDL 网页控制台不提供定时开机/关机功能 |
| **手动开机是唯一方式** | 每天凌晨 2:50 需人工登录网页点击开机 |

> 已尝试过的方案：
> - AutoDL Pro API `/api/v1/dev/instance/pro/power_on` — 返回 RecordNotFoundError
> - 实例列表接口 — Pro 实例列表为空
> - UUID 格式变体 — 均无效
> - 24/7 常开 — 成本过高，用户暂时不接受

### 9.6 恢复步骤 (将来)

当满足以下任一条件时，按步骤恢复：

> ✅ 已升级 AutoDL Pro 实例，或拥有自己的 GPU 服务器

**Step 1 — 远程端**：
```bash
# SSH 登录远程服务器
apt-get update -qq && apt-get install -y cron
service cron start

# 安装 crontab（参考 scripts/crontab.example）
crontab /tmp/crontab.txt && crontab -l
```

**Step 2 — 本地端**：
```powershell
# 管理员 PowerShell
.\scripts\setup-daily-task.ps1
```

**Step 3 — 验证**：
```powershell
# 手动触发一次完整流程
.\scripts\download-analysis.ps1 -Scheduled
# 检查邮件是否收到
```

**Step 4 — (可选) 自动开机**：
- AutoDL Pro：使用 Pro API 在 2:55 自动开机
- 自有服务器：无需此步骤（24/7 运行）

### 9.7 关联脚本索引

| 文件 | 类型 | 说明 |
|------|------|------|
| `scripts/post-deploy-eval.sh` | 远程 Bash | Step 1-4: 64 条合规评测跑分 |
| `scripts/post-deploy-analyze.sh` | 远程 Bash | Step 5-7: 日志切片 + 六维度分析 |
| `scripts/crontab.example` | 配置模板 | 远程 cron 定时任务配置 |
| `scripts/download-analysis.ps1` | 本地 PowerShell | SSH 下载 + 邮件通知 |
| `scripts/email-body-template.txt` | 模板 | 邮件正文 UTF-8 模板 |
| `scripts/setup-daily-task.ps1` | 本地 PowerShell | Windows 计划任务注册 |
| `scripts/autodl-find-uuid.ps1` | 诊断工具 | AutoDL UUID 查询（留存备查） |
| `docs/infra/AutoDL-Cron定时评测配置指南.md` | 文档 | 远程 cron 安装详细步骤 |

---

## 十、真实 GPU E2E 测试层

> **状态**：Phase 4 已创建 — 2026-07-21  
> **定位**：主力测试层，验证 LLM 推理质量、工具调用正确性、数据一致性  
> **MSW Mock E2E 降级**：CI 快速门禁（<3min），仅验证 UI 渲染与交互逻辑  
> **Docker GPU 全量校验**（6 容器本机栈，非 SSH `:15001` 旧路）：命令、通过标准和结果表见 [GPU全量校验手册.md](../infra/GPU全量校验手册.md)。**最近一次有卡记录（RTX 3070，2026-09-05/06）总判定失败**，见 [实测记录](../infra/2026-09-06_RTX3070容器实测记录.md)。本节保留隧道 + Vite 的历史跑法。

### 10.1 双 E2E 架构定位

```
                    CI 快速门禁 (<3min)                主力测试层 (15-30min)
                   ┌──────────────────┐           ┌──────────────────────┐
                   │  e2e/ (MSW Mock)  │           │  e2e-real/ (GPU)      │
                   │  5 specs / 21 用例 │           │  9 specs / 35+ 用例   │
                   │  playwright.config │           │  playwright.real.config│
                   └────────┬─────────┘           └───────────┬──────────┘
                            │                                 │
                   Vite dev:mock                    Vite dev (real)
                   MSW Service Worker               SSH Tunnel :15001 → API :5001
                                                     → llama.cpp GPU (Qwen3-8B)
```

| 维度 | L3-Mock E2E (e2e/) | L3-Real E2E (e2e-real/) |
|------|--------------------|--------------------------|
| **测什么** | UI 渲染、交互逻辑、路由守卫 | LLM 推理质量、工具调用、端到端数据链路 |
| **跑在哪** | 本地 Vite，CI/CD 秒级反馈 | SSH 隧道 → 远程 RTX 3090 GPU |
| **运行时间** | 2-5min | 15-30min |
| **数据来源** | MSW Mock 假数据 | 真实 PostgreSQL + RAG + llama.cpp |
| **断言重点** | 页面不崩、按钮可用、路由正确 | GB 编号非幻觉、工具选择正确、响应时间合理 |
| **行业定位** | "UI 冒烟测试" — 保证页面不崩 | "集成质量验证" — 保证 AI 行为正确 |

### 10.2 真实 GPU E2E 用例清单

> 配置文件：`agent1-web/playwright.real.config.ts`，测试目录：`agent1-web/e2e-real/`

#### 10.2.1 移植用例 (5 specs / 18 用例)

| Spec | 优先级 | 用例数 | 核心断言 |
|------|:---:|:---:|------|
| `compliance-check.spec.ts` | P1 | 4 | 工具调用链真实性(CheckStorageCompatibility)/GB编号非幻觉/响应时间>3s/双通道输出/法规引用可追溯 |
| `inspection-flow.spec.ts` | P2 | 5 | 巡检计划列表(真实DB)/执行巡检触发LLM推理/轮次结果含评估/viewer权限 |
| `dashboard.spec.ts` | P3 | 3 | 资产数(真实DB)/扫描按钮触发LLM/viewer只读 |
| `assets.spec.ts` | P4 | 4 | 8种化学品含CAS号/详情页含储存条件/viewer无编辑按钮 |
| `audit-log.spec.ts` | P5 | 5 | SHA256哈希链完整性校验(真实后端)/操作日志可追溯/auditor+viewer拦截 |

#### 10.2.2 新增用例 (4 specs / 17+ 用例)

| Spec | 场景 | 用例数 | 核心断言 |
|------|------|:---:|------|
| `knowledge-base.spec.ts` | 知识库查询 + 增量更新 | 5 | GB编号精确查询(BM25/Vector/Hybrid)/自然语言查询/检索结果含真实文档 |
| `emergency-response.spec.ts` | 应急响应 | 4 | 事故上报→分级→预案匹配/处置措施含具体关键词/响应时间 |
| `eval-flow.spec.ts` | 合规评测流程 | 4 | 单个评测含GB编号+结论/多场景评测覆盖(危险类别/安全距离/重大危险源) |
| `llm-quality.spec.ts` | LLM推理质量专项 | 8 | 七大评测类目全覆盖/工具选择正确性/参数准确性/无幻觉GB编号/忠实度/响应时间 |

### 10.3 SSH 隧道 + 前端代理全链路

```
┌──────────┐    ┌──────────┐    ┌────────────┐    ┌───────────┐    ┌───────────┐
│  Browser  │───→│   Vite   │───→│ SSH Tunnel │───→│ .NET API  │───→│ llama.cpp │
│ (Chromium)│    │  :5173   │    │  :15001    │    │   :5000   │    │   :8080   │
└──────────┘    └──────────┘    └────────────┘    └───────────┘    └───────────┘
     ↑                ↑                ↑                ↑               ↑
 Playwright     VITE_PROXY_     SshTunnel.exe    Semantic Kernel    Qwen3-8B
 测试脚本       TARGET=15001    Renci.SshNet     + PostgreSQL      GGUF Q4_K_M
```

**关键文件**：

| 文件 | 用途 |
|------|------|
| `agent1-web/playwright.real.config.ts` | Playwright 配置 (testDir=e2e-real, timeout=120s, webServer启动Vite) |
| `agent1-web/scripts/start-tunnel.ps1` | 启动 SSH 隧道 (localhost:15001 → 远程API:5000) |
| `agent1-web/scripts/stop-tunnel.ps1` | 停止 SSH 隧道 |
| `agent1-web/scripts/health-check.ps1` | 远程四服务健康检查 (PG/LLM/Embed/API) |
| `agent1-web/e2e-real/fixtures/auth.fixture.ts` | 认证 fixture (自动登录 admin/auditor/viewer) |
| `agent1-web/e2e-real/utils/llm-assertions.ts` | LLM 推理质量专用断言 |
| `agent1-web/e2e-real/utils/ssh-health.ts` | SSH 隧道 + 远程服务健康验证 |
| `ssh-tunnel/Program.cs` | SSH 端口转发 (Renci.SshNet ForwardedPortLocal) |

### 10.4 LLM 推理质量断言体系

> 定义在 `agent1-web/e2e-real/utils/llm-assertions.ts`

| 断言函数 | 验证维度 | 说明 |
|---------|---------|------|
| `expectGbNumberPresent()` | GB 编号存在性 + 格式合法性 | 验证输出含合法 GB 编号 (GB XXXX-YYYY) |
| `expectToolCallPresent()` | 工具选择正确性 | 验证工具调用链含指定工具 (CheckStorageCompatibility等) |
| `expectResponseTimeInRange()` | 推理性能 | 验证响应时间在 3s-90s 真实 GPU 推理区间 |
| `expectNoHallucinatedGb()` | 幻觉检测 | 验证 GB 编号可追溯到规范库 |
| `expectDualChannelOutput()` | UI 结构 | 验证法规引用面板 + LLM 解释面板同时可见 |
| `expectValidComplianceConclusion()` | 结论完整性 | API 级别验证 is_compliant 字段 |
| `expectComplianceResponseShape()` | 响应结构 | 验证 toolsUsed/verifiedRegulations 数组 |

### 10.5 运行方式与前置条件

**前置条件**：
1. 远程 AutoDL GPU 实例已开机，四服务运行中 (PG/LLM:8080/Embed:8081/API:5000)
2. 本地已编译 SshTunnel.exe (`dotnet build ssh-tunnel/SshTunnel.csproj -c Release`)
3. SSH 凭证已设置环境变量 (`SSH_HOST`, `SSH_PORT`, `SSH_USER`, `SSH_PASSWORD`)

**执行步骤**：

```bash
# Step 1: 启动 SSH 隧道
npm run tunnel:start

# Step 2: 健康检查（自动由 pretest:e2e:real 触发）
npm run test:e2e:real

# 或手动执行完整流程
npm run tunnel:start
powershell -File scripts/health-check.ps1
npx playwright test --config=playwright.real.config.ts
npm run tunnel:stop
```

**CI 集成方案**：
- MSW Mock E2E: 每次 PR 自动执行 (`npm run test:e2e`)
- Real GPU E2E: 手动触发或 AutoDL 开机后定时执行（需 GPU 资源）
- 未来扩展: AutoDL Pro API 自动开机 → Real E2E → 自动关机

### 10.6 文件结构总览

```
agent1-web/
├── e2e/                              # MSW Mock E2E (CI 快速门禁)
│   ├── compliance-check.spec.ts      # P1: 合规自查
│   ├── inspection-flow.spec.ts       # P2: 巡检流程
│   ├── dashboard.spec.ts             # P3: 仪表盘
│   ├── assets.spec.ts                # P4: 资产台账
│   └── audit-log.spec.ts             # P5: 审计日志
├── e2e-real/                         # 真实 GPU E2E (主力测试层)
│   ├── fixtures/
│   │   └── auth.fixture.ts           # 认证 + 登录 fixture
│   ├── utils/
│   │   ├── llm-assertions.ts         # LLM 推理质量断言
│   │   └── ssh-health.ts             # SSH 健康检查
│   ├── compliance-check.spec.ts      # P1-Real
│   ├── inspection-flow.spec.ts       # P2-Real
│   ├── dashboard.spec.ts             # P3-Real
│   ├── assets.spec.ts                # P4-Real
│   ├── audit-log.spec.ts             # P5-Real
│   ├── knowledge-base.spec.ts        # NEW: 知识库
│   ├── emergency-response.spec.ts    # NEW: 应急响应
│   ├── eval-flow.spec.ts             # NEW: 评测流程
│   └── llm-quality.spec.ts           # NEW: LLM质量专项
├── scripts/
│   ├── start-tunnel.ps1              # SSH 隧道启动
│   ├── stop-tunnel.ps1               # SSH 隧道停止
│   └── health-check.ps1              # 远程健康检查
├── playwright.config.ts              # Mock E2E 配置
└── playwright.real.config.ts         # Real GPU E2E 配置
```

---

## 十一、文档索引 (更新)

| 文档 | 位置 | 用途 |
|------|------|------|
| **本总纲** | `docs/platform/测试总纲.md` | 全局测试规划与进度 |
| 远程 E2E 技能 | `.qoder/skills/remote-e2e-testing/SKILL.md` | 远程全链路测试标准流程 |
| 远程 E2E 排障 | `.qoder/skills/remote-e2e-testing/TROUBLESHOOTING.md` | 8 类排障指南 |
| 远程 E2E 前置 | `.qoder/skills/remote-e2e-testing/PRECHECK.md` | 8 项前置检查清单 |
