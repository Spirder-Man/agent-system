# Agent1 前端与六层后端架构深度对齐方案

> **版本**: v1.0 | **日期**: 2026-07-20
> **定位**: 本文档以前端视角深度解析与后端六层架构的对接关系。补充 [Agent1前端架构设计方案](../agent1-web/Agent1前端架构设计方案.md) 中未覆盖的架构级对齐细节。

---

## 一、全局架构映射：前端三层 vs 后端六层

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          AGENT1 全栈架构对齐                                 │
├─────────────────────────────────────┬───────────────────────────────────────┤
│         前端 agent1-web             │         后端 .NET 8                    │
│        (Vue 3 + Vite 5)             │   (ASP.NET Core + Semantic Kernel)    │
├─────────────────────────────────────┼───────────────────────────────────────┤
│                                     │                                       │
│  [视图层] pages/  17页面            │  [第1层] 表现层 (Controller)          │
│     ↓ 组件渲染                     │     ↑ JSON Response                   │
│  [组件层] components/  40+组件      │  [第2层] 模块调度层 (ModuleDispatcher) │
│                                     │                                       │
│  [状态管理层] stores/  8 Pinia      │  [第3层] 业务编排层 (AgentDialog)     │
│     ↓ mutation/query               │     ↓                                 │
│  [数据获取层] @tanstack/vue-query   │  [第4层] 核心业务层 (LlmService)      │
│     ↓ axios                        │     ↓                                 │
│  [HTTP 层] lib/axios.ts + JWT      │  [第5层] 基础设施层 (Database)         │
│     ↓ HTTPS                        │     ↓                                 │
│  [Mock 层] MSW handlers (开发模式)  │  [第6层] 外部系统层 (llama.cpp等)     │
│                                     │                                       │
└─────────────────────────────────────┴───────────────────────────────────────┘
```

### 1.1 关键架构契约

| # | 前端层 | 后端层 | 契约内容 | 当前状态 |
|---|--------|--------|---------|:--------:|
| 1 | **路由守卫** `router.beforeEach` | **第1层** Middleware `[Authorize(Policy)]` | 前端先过滤角色，后端二次验证 JWT | ✅ |
| 2 | **页面路由** `/compliance` | **第2层** ModuleDispatcher → `ComplianceCheck(8)` | 路由路径与 ModuleType 一一对应 | ✅ |
| 3 | **Pinia action** `checkCompliance()` | **第3层** AgentDialog.ExecuteEvalFastAsync | Store 调用链 = AgentDialog 6步流水线 | ✅ |
| 4 | **axios POST** `/api/Compliance/check` | **第4层** LlmService.InvokeNonStreamingWithRetryAsync | FC调用链：prompt → SK Kernel → llama.cpp | ✅ |
| 5 | **缓存策略** `staleTime: 30s` | **第5层** ResponseCacheService (TTL 5min) | 前端短缓存 + 后端长缓存双层 | ✅ |
| 6 | **健康检查** `useQuery(['health'])` | **第6层** llama.cpp + PostgreSQL + Embedding | 前端显示服务就绪状态 | ✅ |

---

## 二、第1层对接：表现层 (Presentation Layer)

### 2.1 三通道入口统一认证

```
┌─────────────────────────────────────────────────────────────────┐
│  前端入口: LoginPage.vue                                         │
│                                                                  │
│  POST /api/Auth/login { username, password }                     │
│         ↓                                                        │
│  [后端第1层] AuthController.Login()                               │
│         ↓                                                        │
│  JWT Token 生成 (密钥: DB_PASSWORD 环境变量)                      │
│         ↓                                                        │
│  Response: { token, refreshToken, username, role, expiresAt }    │
│         ↓                                                        │
│  [前端] authStore.login() → 存储 token → 路由守卫放行             │
│         ↓                                                        │
│  [axios拦截器] 每次请求自动添加 Authorization: Bearer {token}     │
│         ↓                                                        │
│  [后端 Middleware] JWT Bearer 验证 → [Authorize(Policy)] 角色检查  │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 前端角色权限 ↔ 后端 Policy 对齐表

| 前端角色 | 后端 Policy | 可访问路由 | 后端中间件行为 |
|----------|------------|-----------|--------------|
| `admin` | `Admin` + `Auditor` | 全部 17 页面 + 审计日志 + 系统管理 | 所有 `[Authorize]` 通过 |
| `auditor` | `Auditor` | 除审计日志/系统管理外全部 | GET/POST 业务API通过 |
| `viewer` | `Viewer` (预留) | 仅 `/login` + `/403` | 当前: 全部 API 返回 401 |

### 2.3 中间件对齐

| 前端处理 | 后端 Middleware | HTTP状态码 | 前端行为 |
|----------|---------------|:---------:|---------|
| axios 响应拦截器 | GlobalExceptionMiddleware | 500 | `ElMessage.error('服务异常')` |
| axios 响应拦截器 | RateLimitingMiddleware | 429 | `ElNotification` 含倒计时 |
| axios 响应拦截器 | TokenBlacklistMiddleware | 401 | 自动刷新 token → 失败跳登录 |
| 路由守卫 `beforeEach` | `[Authorize]` Attribute | 403 | 重定向 `/403` |
| — | RequestIdMiddleware | — | 响应头 `X-Request-Id` (用于日志溯源) |
| — | RequestMetricsMiddleware | — | Prometheus 指标采集 |

---

## 三、第2层对接：模块调度层 (Module Dispatch Layer)

### 3.1 前端路由 → Backend ModuleType 完整映射

```
前端路由                         后端调度
══════════                      ══════════
/compliance  ─────────────────→ ComplianceController.Check()
                                    ↓
                               ModuleDispatcher.ExecuteModuleAsync(
                                   ModuleType.ComplianceCheck (8))

/inspection  ─────────────────→ InspectionController
/inspection/create                 ↓
/inspection/:id/execute        ModuleDispatcher.ExecuteModuleAsync(
/inspection/rounds/:id             ModuleType.ComplianceCheck (8))
/inspection/reports/:id             (巡检执行 = 合规自查的扩展)

/tickets  ────────────────────→ TicketsController
/tickets/:id                       ↓
                               ModuleDispatcher.ExecuteModuleAsync(
                                   ModuleType.TicketFollowup (9))

/assets  ─────────────────────→ InspectionController.GetAssets()
                                   ↓
                               ModuleDispatcher.ExecuteModuleAsync(
                                   ModuleType.ComplianceCheck (8))

/audit  ──────────────────────→ AuditController (admin only)
                                   ↓
                               AuditService (第5层直接，不走ModuleDispatcher)
```

### 3.2 ModuleType 前端感知矩阵

| ModuleType | 枚举值 | 前端感知方式 | 可观察组件 |
|:----------:|:------:|------------|-----------|
| CoTSolid (1) | 1 | Console CLI only | 不直接暴露前端 |
| CoTStream (2) | 2 | Console CLI only | 不直接暴露前端 |
| ReActSolid (3) | 3 | Console CLI only | 不直接暴露前端 |
| ReActStream (4) | 4 | Console CLI only | 不直接暴露前端 |
| Reflection (5) | 5 | [内部] 对前端透明 | ComplianceResultCard 警告区 |
| RAG (6) | 6 | [内部] 对前端透明 | ComplianceResultCard 法规引用角标 |
| UnifiedDialog (7) | 7 | Console CLI only | 不直接暴露前端 |
| **ComplianceCheck (8)** | 8 | **合规自查 / 巡检 / 资产** | ComplianceResultCard, ToolCallChain |
| **TicketFollowup (9)** | 9 | **工单列表/详情** | TicketStatusFlow, TicketActionButtons |
| **RegulatoryAudit (10)** | 10 | Console CLI only | 不直接暴露前端 |
| EmergencyResponse (11) | 11 | [预留] | 应急响应页面（待实现） |
| KnowledgeGraph (12) | 12 | [预留] | 知识图谱可视化（待实现） |

---

## 四、第3层对接：业务编排层 (Orchestration Layer)

### 4.1 AgentDialog 6步流水线 → 前端加载状态同步

```
后端 AgentDialog (6步)          前端 UI 状态
══════════════════════          ═══════════════
[1] PreprocessAsync             → loading = true, progress = 10%
    输入清理 + 安全拦截              (安全拦截: 红色提示)

[2] RouteIntent                 → progress = 30%
    意图分类 → ModuleType          (判断使用哪个 Controller)

[3] LoadContextAsync             → progress = 40%
    会话 + 记忆 + 用户画像加载       (无直接UI变化)

[4] ExecuteBusinessAsync         → progress = 50-80%
    LlmService.InvokeNonStreaming  "推理中...预计 8s"
    → SK Auto FC → 工具调用         ToolCallChain 逐步点亮
    → 工具结果返回                  RegulationRefPanel 逐步填充

[5] SaveSessionAsync             → progress = 90%
    会话保存 → AuditService 记录    (无直接UI变化)

[6] FormatOutput                 → progress = 100%, loading = false
    双通道结果:                     ├─ RegulationRefPanel (结构化)
    法规引用 + LLM解释               └─ LlmExplanationPanel (Markdown)
```

### 4.2 EvalEngine 评测链路 → 前端（Console 专用，API不暴露）

```
EvalEngine.RunComplianceEvalAsync()
  Layer1: FC就绪性检查 (5条诊断用例)
    → 结果不入前端
  Layer2: 逐条业务评测 (63条)
    → eval_reportXXX.json
    → 前端仪表盘可读取 eval_report 统计展示
```

---

## 五、第4层对接：核心业务层 (Core Business Layer)

### 5.1 7个 KernelFunction → 前端组件映射

| KernelFunction | 前端触发组件 | 展示组件 | 调用方式 |
|---------------|------------|---------|---------|
| `CheckHazardCategory` | ComplianceInput | RegulationRefPanel (GB 30000引用) | POST /api/Compliance/hazard/query |
| `CheckStorageCompatibility` | QuickTemplateBar ["储存兼容性"] | RegulationRefPanel (GB 15603引用) | POST /api/Compliance/storage/compatibility |
| `GetSafetyDistance` | QuickTemplateBar ["安全距离"] | RegulationRefPanel (GB 50160引用) | POST /api/Compliance/check |
| `LookupChemicalProperties` | ChemicalSelector | AssetDetailCard | GET /api/Inspection/assets/:id |
| `GetMajorHazardThreshold` | DashboardPage | StatCard (预警) | POST /api/Compliance/check |
| `CheckRegulationVersion` | [内部校验] | HallucinatedRegulations 警告 | POST /api/Compliance/check |
| `GetCurrentTime` | [辅助] | — | 自动调用 |

### 5.2 双通道输出架构（前端核心设计）

```
后端 ComplianceResponse                    前端双面板展示
══════════════════════                     ════════════════
{
  "response": "markdown 格式的 LLM 解释",  →  LlmExplanationPanel
  "verifiedRegulations": [                    (markdown-it 渲染)
    { "number": "GB 15603-2022",              ↓
      "excerpt": "§4.2.2 ..." },          →  RegulationRefPanel
    ...                                       (结构化列表 + 绿色勾)
  ],
  "hallucinatedRegulations": [             →  RegulationRefPanel
    { "number": "GB XXXX-XXXX" }              (红色警告 ⚠️)
  ],
  "warnings": [                            →  ComplianceResultCard 警告区
    "丙酮存量超临界量80%"
  ],
  "toolsUsed": [                           →  ToolCallChain
    "CheckStorageCompatibility",               (时间线 + 图标)
    "CheckHazardCategory"
  ],
  "cacheHit": true                         →  "⚡ 缓存结果" 标签
}
```

### 5.3 安全护栏：确定性检查层

```
前端不参与推理判断 —— 所有安全结论由后端确定性规则引擎生成：

[用户输入] → SafetyGuardService.ValidateInput()    ← 第4层 (Prompt注入拦截)
                ↓
          LLM 推理 (Qwen3-8B, Temp=0.3, FC=Required)
                ↓
          SafetyGuardService.ValidateOutput()        ← 第4层 (高危断言拦截)
                ↓
          ConclusionVerifier.VerifyAsync()           ← 第4层 (法规幻觉检测)
                ↓
          ReflectionVerifier.VerifyBusinessFacts()   ← 第4层 (事实核查)
                ↓
          [前端仅负责展示] ← 确定性结果 + ⚠️警告列表
```

---

## 六、第5-6层对接：基础设施层与外部系统层

### 6.1 前端可观测性对接

| 后端端点 | 前端用途 | 调用频率 | 组件 |
|---------|---------|:-------:|------|
| `GET /health/live` | 系统存活状态 | 每10s | SystemStatusPage |
| `GET /health/ready` | 服务就绪（含DB+LLM） | 启动时 | SystemStatusPage |
| `GET /metrics` | Prometheus 指标 (运维用) | — | Grafana Dashboard |
| `GET /api/cache/stats` | 缓存命中率 + 大小 | 手动刷新 | SystemStatusPage |
| `POST /api/cache/clear` | 清除缓存 (admin) | 手动触发 | SystemStatusPage |
| `GET /api/memory/stats` | 记忆系统状态 | 手动刷新 | SystemStatusPage |

### 6.2 前端对第6层的间接感知

```
前端不直接访问第6层（llama.cpp / PostgreSQL / Embedding），
但通过以下方式间接感知：

llama.cpp GPU推理状态:
  ├─ 推理耗时: 后端返回 latencyMs → 前端显示 "查询耗时 Xs"
  ├─ 模型就绪: /health/ready → 前端 SystemStatusPage 绿灯/红灯
  └─ 熔断器: 后端 3次失败→30s冷却 → 前端显示 "服务繁忙，请稍后重试"

PostgreSQL 状态:
  ├─ 数据回显: 巡检计划/工单列表/资产台账 均来自 DB 查询
  └─ 审计哈希链: /audit → 前端显示 "哈希链状态: ✅ 完整 (42条)"

Embedding 服务状态:
  └─ RAG检索质量: 后端返回 ragQuality → 前端合规自查页角标
```

---

## 七、前端测试金字塔与后端测试体系对齐

### 7.1 全栈测试矩阵

```
┌──────────────────────────────────────────────────────────────────┐
│                        全栈测试体系                                │
├─────────────────────────────┬────────────────────────────────────┤
│      前端测试 (agent1-web)   │       后端测试 (Agent1.Tests)       │
├─────────────────────────────┼────────────────────────────────────┤
│                              │                                     │
│  L3: Playwright E2E          │  L3: CLI 菜单自动化 (25条)          │
│  ┌───────────────────────┐   │  ┌──────────────────────────────┐   │
│  │ P1 登录→合规自查→结果 │   │  │ T1-T20: 安全注入/LLM推理/    │   │
│  │ P2 巡检→工单流转      │   │  │ 合规评测/RAG召回/CLI菜单     │   │
│  │ P3 仪表盘数据聚合     │   │  │                              │   │
│  │ P4 资产→自动扫描      │   │  │ 状态: ✅ 已执行 (远程GPU)    │   │
│  │ P5 审计日志查看       │   │  └──────────────────────────────┘   │
│  │                       │   │                                     │
│  │ 状态: 🔴 未实现       │   │  L2: 集成测试 (63条)                │
│  └───────────────────────┘   │  ┌──────────────────────────────┐   │
│                              │  │ ApiIntegration (13条·HTTP)    │   │
│  L2: 组件测试 (Vitest)       │  │ DatabaseIntegration (13条)    │   │
│  ┌───────────────────────┐   │  │ Task11 API (25条·远程)       │   │
│  │ ComplianceResultCard   │   │  │ v2 CLI (25条·远程)          │   │
│  │ TicketStatusBadge      │   │  │                              │   │
│  │ TicketActionButtons    │   │  │ 状态: ✅ CI中自动运行PG容器  │   │
│  │ StatCard               │   │  └──────────────────────────────┘   │
│  │ ChemicalSelector       │   │                                     │
│  │ ErrorBoundary          │   │  L1: 单元测试 (665条)               │
│  │                       │   │  ┌──────────────────────────────┐   │
│  │ 状态: 🔴 未实现       │   │  │ xUnit + Moq + FluentAssert   │   │
│  └───────────────────────┘   │  │ 63个测试类 覆盖27个源文件     │   │
│                              │  │ 全部通过 ✅                   │   │
│  L1: 单元测试 (Vitest)       │  │                              │   │
│  ┌───────────────────────┐   │  │ 覆盖率: ~21% (目标≥80%) 🔴   │   │
│  │ authStore / ticketStore│   │  └──────────────────────────────┘   │
│  │ complianceStore        │   │                                     │
│  │ inspectionStore        │   │  L0: 架构收敛测试                    │
│  │ constants / format     │   │  ┌──────────────────────────────┐   │
│  │                       │   │  │ ArchitectureTest (7验证点)    │   │
│  │ 状态: 🔴 未实现       │   │  │ dotnet run (CI: continue-on-  │   │
│  └───────────────────────┘   │  │   error=true → 待修复)        │   │
│                              │  └──────────────────────────────┘   │
└─────────────────────────────┴────────────────────────────────────┘
```

### 7.2 测试数据流对齐

```
前端测试数据源                    后端测试数据源
══════════════                    ══════════════
E2E (Playwright):
  MSW handlers.ts Mock数据   ←→ 不依赖后端

组件测试 (Vitest):
  MSW + Mock Store            ←→ 不依赖后端

单元测试 (Vitest):
  纯函数 + Mock Pinia         ←→ 不依赖后端
                                  ↓
                              单元测试 (xUnit):
                                纯逻辑 + Moq Mock

                              集成测试 (xUnit):
                                PostgreSQL Docker容器 (CI自动)
                                + 真实 HTTP 请求 (WebApplicationFactory)

                              架构收敛测试:
                                反射扫描 .cs 文件 → 验证架构规则
```

---

## 八、架构级的实施建议与优化方向

### 8.1 🔴 P0 紧急：让前端测试不再空跑

```typescript
// 第一步：创建 src/__tests__/stores/auth.test.ts
import { describe, it, expect, beforeEach } from 'vitest';
import { setActivePinia, createPinia } from 'pinia';
import { useAuthStore } from '@/stores/auth';

describe('authStore', () => {
  beforeEach(() => { setActivePinia(createPinia()); });

  it('login should set token and role', async () => {
    const store = useAuthStore();
    // Mock axios POST /api/Auth/login
    await store.login('admin', 'password');
    expect(store.isAuthenticated).toBe(true);
    expect(store.role).toBe('admin');
  });

  it('hasPermission should respect role hierarchy', () => {
    const store = useAuthStore();
    store.role = 'admin';
    expect(store.isAdmin).toBe(true);
    expect(store.isAuditor).toBe(true);
  });
});
```

### 8.2 🟡 P1 重要：CI 架构卡口硬化

```yaml
# .github/workflows/ci.yml 修改点
- name: Architecture Convergence Test
  run: dotnet run --project ArchitectureTest --configuration Release
  continue-on-error: false  # ← 改为 false，架构退化必须阻断
```

### 8.3 🟡 P1 重要：前端测试启用覆盖率

```yaml
# ci.yml frontend-test Job 修改
- name: '🧪 前端单元测试 (Vitest)'
  run: npx vitest run --coverage
- uses: actions/upload-artifact@v4
  if: always()
  with:
    name: frontend-coverage
    path: agent1-web/coverage/
```

### 8.4 🟢 P2 优化：实现 Swagger → TypeScript 类型自动生成

利用后端的 `swagger.json` 自动生成前端 `types/api.ts`：

```bash
# scripts/generate-api.ts (Node.js 开发者工具)
npx openapi-typescript http://localhost:5000/swagger/v1/swagger.json \
  -o src/types/api.generated.ts
```

### 8.5 🟢 P2 优化：前端架构收敛检查自动化

在 ArchitectureTest 项目中增加前端文件结构验证：

```csharp
// ArchitectureTest/Program.cs 新增验证点
// [验证点 10] 前端-后端架构收敛
VerifyFrontendConvergence() {
    AssertFileExists("agent1-web/src/types/api.ts");
    AssertFileExists("agent1-web/src/lib/axios.ts");
    AssertFileExists("agent1-web/src/stores/auth.ts");
    AssertRouteCount("agent1-web/src/router/index.ts", minRoutes: 14);
    // 验证 Vite 代理端口 = 后端 launchSettings.json 端口
    AssertViteProxyPort("agent1-web/vite.config.ts", expectedPort: 5000);
}
```

---

## 九、总结

本文档建立了前端 agent1-web 与后端六层架构之间完整的**架构契约矩阵**：

| 契约维度 | 对齐状态 | 关键交付物 |
|----------|:--------:|----------|
| 认证体系 | ✅ | JWT + 3级 RBAC 前后端一致 |
| 路由→ModuleType 映射 | ✅ | 17条前端路由 ↔ 12种 ModuleType |
| 6步流水线→UI状态 | ✅ | AgentDialog 步骤 ↔ loading/progress/progress bar |
| 7个 KernelFunction | ✅ | 工具调用 ↔ 前端组件展示 |
| 双通道输出 | ✅ | ComplianceResponse ↔ 双面板渲染 |
| 安全护栏 | ✅ | 4层确定性检查 + 前端仅展示 |
| 测试对齐 | 🔴 | 前端0测试 vs 后端665+63测试 |
| CI/CD卡口 | 🟡 | 架构收敛 continue-on-error 待修复 |

**核心原则**：前端是后端确定性能力的**展示层**，不参与合规推理判断。所有安全结论由后端第4层的确定性规则引擎（SafetyGuardService + ConclusionVerifier + ReflectionVerifier）生成，前端仅负责结构化展示和用户交互。
