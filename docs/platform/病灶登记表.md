# 病灶登记表

> 本文档记录系统所有已知陷阱、Bug修复历史、反直觉行为。每条含四要素：现象、根因、状态、影响范围。
>
> 配合 `系统血谱.md` 使用——血谱画血管，本文档记录血管上的伤疤。
>
> 最后更新：2026-08-13（登记 Bug-048 集成测试整表清理误删知识库；Bug-045 FindEnvFile off-by-one、Bug-046 测试环境变量删除污染、Bug-047 静态门面测试初始化缺陷；Bug-043 远程 PG 同步完成）

---

## 一、活跃病灶（未修复，阅读/修改时警惕）

### 病灶 1：快速模式使存量乱码块常驻

| 项目 | 内容 |
|------|------|
| **位置** | 动脉 A，PostgreSQL `knowledge_chunks` 表，约 60 条分块 |
| **现象** | 系统启动时发现 PG 中已有分块（count > 0），走快速模式跳过文件扫描直接充血。早期入库的乱码块（GarbledTextDetector 上线前产生的）因此常驻内存索引。 |
| **根因** | `GarbledTextDetector` 乱码闸门只拦截增量入库，不回溯清理存量。 |
| **状态** | ⚠️ 活跃。2026-07-29 启动全量 OCR 重灌进行中，完成后销记。 |
| **影响** | 约 60 条乱码块可能污染检索结果。 |

### 病灶 2：过时注释"静态内存字典"误导读者

| 项目 | 内容 |
|------|------|
| **位置** | `ChemicalComplianceTools.cs#L707` |
| **现象** | 代码注释写"静态内存字典"，但实际查询走的是 `ChemicalSubstanceDatabase`（PG 门面），不是内存字典。 |
| **根因** | 注释未随架构演进而更新——早期确实是内存字典，后来改为 PG 门面但忘记改注释。 |
| **状态** | ⚠️ 活跃。未修——调用面广，担心改了注释引入新的误解。 |
| **影响** | 阅读代码时被误导以为此处有独立内存缓存。 |

### 病灶 3：同名类命名陷阱

| 项目 | 内容 |
|------|------|
| **位置** | `Services/Knowledge/ChemicalDatabaseService.cs`（SQLite）vs `Services/Knowledge/ChemicalSubstanceDatabase.cs`（PG 门面） |
| **现象** | 两个类名高度相似——`ChemicalDatabaseService` 操作 SQLite，`ChemicalSubstanceDatabase` 操作 PG 图谱。不仔细看文件内容极易混淆。 |
| **根因** | 历史命名遗留。SQLite 服务先创建，命名为 "Database" 合理；后来加 PG 图谱时新建了 "SubstanceDatabase" 但没重构旧名。 |
| **状态** | ⚠️ 活跃。不修——调用面广，重命名需全仓联动。 |
| **影响** | 新人/自己隔一段时间回来改代码时，可能误认为两者是同一个东西。血谱动脉 B/C 已分别标注。 |

### 病灶 4：老 SQLite 库不吃新种子

| 项目 | 内容 |
|------|------|
| **位置** | 动脉 C，`ChemicalDatabaseService.InitializeAsync` 第 66-68 行 |
| **现象** | 种子写入条件 `COUNT(*) == 0`——只要 SQLite 库中已有任意数据，跳过全部种子写入。后续新增的种子数据不会自动补入已有库。 |
| **根因** | 设计如此——种子是全量一步写入的快照，不支持增量追加。 |
| **状态** | ⚠️ 活跃（设计如此，非 Bug）。 |
| **影响** | 补种子数据需要删库重建（`Data/chemical_substances.db` 删除后重启）。 |

### 病灶 5：CheckRegulation 笔误（候选 Bug）

| 项目 | 内容 |
|------|------|
| **位置** | `EvalEngine.cs#L174` + `LlmService.cs#L611-615` |
| **现象** | `compliance_judgment` 意图的工具白名单中写了 `"CheckRegulation"`，但插件的实际函数名是 `"CheckRegulationVersion"`。名称不匹配导致临时 Kernel 过滤后实际注册 4/9（而非预期的 5/9），法规版本查询工具在合规判断类评测用例中永远不可用。 |
| **根因** | 白名单手写时笔误，缺少 "Version" 后缀。 |
| **状态** | ⏳ 候选 Bug（2026-08-01 血谱走查发现），待远程评测验证 toolMatch 后销记或修复。 |
| **影响** | 所有 `compliance_judgment` 评测用例缺少法规版本查询能力——大模型即使想查法规版本也看不到这个工具。 |

---

## 二、已修复病灶（按时间倒序，保留思维链路备查）

> 遵循工程宪法第二条：每次 Bug 修复保留可复盘的思维链路。状态标记 ✅ + 修复日期。

---

### Bug-048：集成测试整表清理误删知识库

| 项目 | 内容 |
|------|------|
| **修复日期** | 2026-08-13 |
| **位置** | 测试基础设施，`Agent1.Tests/DatabaseIntegrationTests.cs` `DisposeAsync` |
| **现象** | 运行 DatabaseIntegrationTests 后，本地 PG 的 `knowledge_documents` 与 `knowledge_chunks` 被整表清空（34/55 → 0/0）；台账 #10/#14 的存量数据在验证批次 1 后“消失”，与报告状态矛盾。 |
| **根因** | `DisposeAsync` 固定执行 `DELETE FROM knowledge_chunks; DELETE FROM knowledge_documents;`——把“测试后清理”写成了“清空生产/开发库”，任何一次集成测试都会误删真实知识数据。 |
| **修复方案** | ① 本类所有写入数据统一携带 `TestMarker = "codex-itest-20260813"` 标记；② `DisposeAsync` 改为仅按标记删除（`source_path/file_name LIKE marker`，chunks 由 `ON DELETE CASCADE` 级联清理，旧表 chemical_documents 按标记删除）；③ 从 `db/backups/dump_20260811.dump` 选择性恢复 `knowledge_documents` + `knowledge_chunks`（34/55 行，序列同步）。 |
| **思维链路** | 1. 核对批次 1 状态时发现 knowledge 两表 0 行（台账声称 34 文档/55 分块）→ 2. 查测试代码发现 DisposeAsync 整表 DELETE → 3. 回溯确认是集成测试运行造成，非批次 1 功能改动 → 4. 先恢复备份 → 再修测试清理逻辑（标记化）→ 5. 重跑 DatabaseIntegrationTests 13/13 通过，知识库 34/55 保持完好。 |
| **影响** | 此前任意一次集成测试都可能把本地开发库的知识底座清空；修复后测试只清理自身标记数据。同类隐患排查：`GetChemicalDocumentCount_InitiallyZero_AfterCleanup` 仍调用全量 Clear API，建议后续仅在独立测试库执行。 |

---

### Bug-045：FindEnvFile off-by-one 致 .env 加载失效

| 项目 | 内容 |
|------|------|
| **修复日期** | 2026-08-13 |
| **位置** | 测试基础设施，`Agent1.Tests/ModuleInitializer.cs` `FindEnvFile()` |
| **现象** | 测试进程内 DB_PASSWORD 等环境变量为 null（probe 验证），集成测试用硬编码默认密码 changeme 连本地 PG 报 28P01；本地 PG 实际密码已改为 .env 的 12345678。 |
| **根因** | `AppContext.BaseDirectory` 带尾反斜杠（如 `...\bin\Debug\net8.0\`），`Path.GetDirectoryName` 首轮只去掉尾分隔符返回同目录 → 首轮迭代被浪费；循环上限 5 层只到达 `Agent1.Tests\`，永远够不到项目根 .env。 |
| **修复方案** | 循环前用 `Path.TrimEndingDirectorySeparator(AppContext.BaseDirectory)` 去掉尾分隔符，5 层上限恰好覆盖到项目根。 |
| **思维链路** | 1. 现象：串行全量 203 失败，大部分是 28P01/静态门面 null → 2. 怀疑 .env 未加载 → 3. 临时 probe 测试打印环境变量与逐层 .env 探测 → 4. 发现 level0/level1 指向同一目录（尾反斜杠吃掉一层）→ 5. 修复后 probe 验证 DB_PASSWORD=12345678 ✅。 |
| **影响** | 历史会话“集成测试通过”多依赖 shell 手动注入 DB_PASSWORD；修复后 .env 自动加载恢复设计意图。 |

---

### Bug-046：测试环境变量删除污染（4 处 finally 设 null）

| 项目 | 内容 |
|------|------|
| **修复日期** | 2026-08-13 |
| **位置** | `Agent1.Tests/AppConfigTests.cs` 环境变量覆盖测试族 |
| **现象** | `Load_WithDbPasswordEnvVar_ShouldOverrideConfig` 等 4 个测试在 finally 用 `SetEnvironmentVariable(key, null)` **删除**环境变量而非恢复原值；DB_PASSWORD 被删后 `CustomApiWebApplicationFactory` 见空值注入错误密码 `test_pwd_ci` → WebApplicationFactory 启动失败 → 后续所有依赖 SetGraph 副作用的测试连锁失败。 |
| **根因** | 测试的“清理”写成“删除”，破坏 ModuleInitializer 加载 .env 注入的进程级状态；测试间环境变量状态耦合未隔离。 |
| **修复方案** | 4 处 finally 改为保存原值→恢复原值；另修复 2 个下游测试的环境变量免疫（`Validate_BadEndpointFormat` 清空 LLM_ENDPOINT、`ChemicalKnowledgeBasePath` 清空 KNOWLEDGE_BASE_PATH）；`GetComplianceSummary` 2 处改用隔离路径 `InspectionRepository(tempPath)` 防残留文件污染；`CircuitBreakerTests` TotalMs 断言放宽为 ≥0（ElapsedMilliseconds <1ms 时为 0，flaky）。 |
| **思维链路** | 1. 全量测试 198 失败，28P01 指向错误密码 test_pwd_ci → 2. grep 定位唯一注入点 CustomApiWebApplicationFactory → 3. 其触发条件是 DB_PASSWORD 为空 → 4. 定位删除源 AppConfigTests finally null → 5. 修复后暴露更深层：FindEnvFile off-by-one（Bug-045）与静态门面初始化缺陷（Bug-047）。 |
| **影响** | 本地全量测试从 198+ 失败降至门面类 85 失败（Bug-047 范畴）；剩余问题为测试架构级，独立登记。 |

---

### Bug-047：静态门面测试初始化缺陷（ChemicalSubstanceDatabase 等）

| 项目 | 内容 |
|------|------|
| **发现日期** | 2026-08-13 |
| **位置** | 测试架构，`ChemicalSubstanceDatabaseTests` / `KnowledgeGraphServiceTests` / `EmergencyResponseServiceTests` 等（约 85 个用例） |
| **现象** | 这些测试直接调用静态门面 `ChemicalSubstanceDatabase.Lookup` 等，但测试内无人调用 `SetGraph`；`_graph` 唯一初始化点是两个 `Program.cs` 的 DI 启动副作用。全量跑时依赖“ApiIntegrationTests 的 WebApplicationFactory 恰好先跑”的脆弱顺序耦合——元数据顺序一变即批量失败（NotBeNull/28P01）。 |
| **根因** | 静态门面 + DI 启动副作用初始化，测试缺乏显式装配（AssemblyFixture/IClassFixture）；CI Job 1（Category!=Integration，无 PG）同样存在此隐患。 |
| **状态** | ⏳ 未修（测试架构级改造，需产品决策：门面懒加载 vs 测试装配初始化 vs 标记 Integration 类别） |
| **影响** | 本地全量测试 85 个用例的顺序耦合失败；串行/并行下结果不稳定（曾出现 4~203 失败摆动）。 |

---

### Bug-044：审计哈希链验证/修复被 LIMIT 1000 截断盲区

| 项目 | 内容 |
|------|------|
| **修复日期** | 2026-08-13 |
| **位置** | 审计链，`DatabaseService.GetAuditLogsAsync` L1344 + `AuditService.VerifyIntegrityAsync / RepairChainAsync` |
| **现象** | audit_logs 实际 1192 条，但 VerifyIntegrityAsync / RepairChainAsync 只读回最近 1000 条（`LIMIT 1000`），链验证/修复窗口被截断；窗口首条被 GENESIS 重锚，与真实链头错位——验证“自洽但不完整”，最早 192 条记录永远不在验证范围内。 |
| **根因** | 验证/修复复用面向报告导出的 `GetAuditLogsAsync`（LIMIT 1000 防爆内存设计），未区分“窗口查询”与“全链验证”两种语义。 |
| **修复方案** | ① IDatabaseService 新增 `GetAllAuditLogsAsync()`（无 LIMIT，按 id 排序），VerifyIntegrityAsync / RepairChainAsync 切换到全量读回（带降级路径）；② RepairChainAsync 全链重算回写 1192 条（含 4 条 NULL 空洞），VerifyIntegrityAsync 通过；③ pgcrypto 递归 CTE 独立复刻 C# 算法全链比对 match=1192 / mismatch=0 / null=0 交叉验证。 |
| **思维链路** | 1. 修复历史 4 条 NULL 时用 pgcrypto 递归复刻哈希链比对 → 2. 发现 mismatch=786 与 C# 验证通过矛盾 → 3. 定位：`id+1` 递推在 id 空洞断链后改用 row_number，仍 mismatch → 4. 发现真凶 LIMIT 1000（C# 侧只验证 1000 条窗口）→ 5. 修复：新增全量读回方法 → 6. 终验：SQL 复刻与 C# 算法全链等价（mismatch=0）。 |
| **影响** | 任何 audit_logs 超 1000 条的环境，审计防篡改验证都存在覆盖盲区；修复后全链验证 + 独立 SQL 复刻可交叉审计。 |

---

### Bug-043：王水错误（硝酸↔盐酸标兼容）三拷贝点

| 项目 | 内容 |
|------|------|
| **修复日期** | 2026-08-13 |
| **位置** | 动脉 B 禁忌边，同源拷贝组 1：002 种子 L667 + SQLite 种子 `ChemicalDatabaseService.SeedCompatibilityRulesAsync` L875 + PG 实际数据 |
| **现象** | 硝酸↔盐酸边 is_compatible=true，reason="两种强酸可同库分区存放"——用户问“硝酸和盐酸能一起存吗”会得到“可以同库”的错误安全建议（全库唯一“答错要出事故”的配伍答错）。 |
| **根因** | 002 种子人工编写 21 条规则时未复核 GB 15603 配存表——硝酸+盐酸=王水（混合放热、产生氯气/亚硝酰氯剧毒气体），实为禁配。 |
| **修复方案** | 三拷贝点同步：002 L667 → FALSE + 王水 reason + regulation_ref=GB 15603；SQLite 种子 L875 同步；本地 PG `UPDATE ... WHERE 硝酸↔盐酸`（UPDATE 1）并核销回查 `is_compatible=f` ✅。 |
| **思维链路** | 1. 台账 #25 记录（P1）→ 2. 血谱勘测确认错误存在 4 拷贝组中 3 处（硬编码字典/测试桩无此边）→ 3. 先改数据（本地 PG UPDATE 1）→ 再改代码（002 迁移 + SQLite 种子）→ 4. 核销 SQL 回查 f ✅。 |
| **影响** | 远程 PG（AutoDL）已于 2026-08-13 经 `remote_fix_25c.ps1`（base64 文件传输）同步修复 UPDATE 1，核销回查 `is_compatible=false + GB 15603 + 王水 reason` ✅；#26 克劳斯反应（硫化氢↔二氧化硫）两拷贝点待化学专家结论后同法修复。 |

---

### Bug-042：不可证伪锚点致探针永远满分盲区（强标记正则族缺驼峰类名）

| 项目 | 内容 |
|------|------|
| **修复日期** | 2026-08-12 |
| **位置** | DriftMonitor 测量链路，`DriftMatcher.cs` 强标记正则族 |
| **现象** | 探针 probe_a4（乱码闸门锚点）无论 LLM 回答正确/错误/未作答，漂移量恒为 0——探针强制断言打在没有刻度（不可证伪）的锚点上，永远满分，测不出认知漂移。 |
| **根因** | `ExtractStrongTokens` 强标记正则族只有 4 类（PortToken/PathToken/UpperToken/SnakeToken），不含驼峰类名；锚点 canonical_value 含类名 `GarbledTextDetector` 时提取不到任何标记，`MatchScore` 的 `tokens.Count == 0` 分支返回 1.0（"不制造噪音"设计）→ 探针强制断言无刻度锚点 = 永远满分盲区。 |
| **修复方案** | ① DriftMatcher 新增第 5 类强标记 CamelToken 正则 `\b[A-Z][a-z]+[A-Z][A-Za-z0-9]*\b`（排除场景：全大写前缀 SQLite/SHA256 归 UpperToken 管、无第二大写 Vite/Level1、数字嵌首 Agent1.Api——避免产品名/通用词/版本号误伤）；② 新增 5 条单测（驼峰提取/产品名防误伤/可证伪性/答对答错区分）；③ 远程真实 LLM 三态验证：正确回答 drift=0、错误回答 drift=1、未作答 drift=1，全部通过。 |
| **思维链路** | 1. 发现：q8 E2E 中 probe_a4 报"锚点不可证伪"→ 2. 定位：GarbledTextDetector 类名不匹配任何强标记正则 → 3. 分析：MatchScore 无标记默认 1.0 是"不制造噪音"设计，但与探针强制断言组合成盲区 → 4. 方案三选（扩展评分器/停用替换/暂不处理）→ 用户选根治：扩展评分器 → 5. 修复：CamelToken 正则 + 防误伤排除 → 6. 验证：50 条锚点中 ~10 条从不可证伪变可证伪，probe_a4 三态全部通过；顺带实证探针幂等覆盖（turn=4 被末次 POST 覆盖）。 |
| **影响** | 影响面：50 条锚点中 15 条含驼峰、~10 条可证伪性升级（前端器官/主存储/图谱门面/命名陷阱/配置入口/出口闸门链/双通道验证/FC策略/FC契约/乱码闸门），被动抽取将新增断言；既有测试核对安全（SQLite/Level1 不匹配新正则，无回归）。 |

---

### Bug-041：OCR 失败固化为永久薄文本降级

| 项目 | 内容 |
|------|------|
| **修复日期** | 2026-07-29 |
| **位置** | 动脉 A，`ProcessPdfFileAsync` |
| **现象** | 扫描件 PDF OCR 瞬时失败后，薄文本结果仍被写入 PG 和 file_tracker.json。下次增量更新时 tracker 认为"已处理"，跳过重试，导致 OCR 故障永久固化为低质量入库。 |
| **根因** | 缺少 OCR 质量门——失败时没有阻断入库流程。 |
| **修复方案** | 增加质量门：扫描件（`ExtractionMethod == OCR_NEEDED`）OCR 彻底失败或 `PagesOcred / PagesTotal < 0.5`（或质量 failed）→ `ProcessPdfFileAsync` 返回 false → 不入库、不记 tracker → 下轮增量自动重试。 |
| **思维链路** | 1. 发现：增量更新后搜索质量下降 → 2. 定位：某扫描件 OCR 当天服务抖动失败 → 3. 分析：tracker 记录了"已处理"所以不再重试 → 4. 修复：增加质量门，不合格不入 tracker → 5. 验证：重启 OCR 服务后增量自动重灌。 |

---

### Bug-039：增量加载端点可重入 + 优雅取消缺陷

| 项目 | 内容 |
|------|------|
| **修复日期** | 2026-07-29 |
| **位置** | 动脉 A 增量支线，`ChemicalRAG.cs` |
| **现象** | 用户快速连续调用增量加载接口 → 两个并发同时写 PG 和 file_tracker.json → 数据重复、tracker 状态混乱。服务收到 SIGTERM 时增量循环不感知取消信号 → 写了一半的文件留下僵尸行。 |
| **根因** | 缺少互斥锁 + 取消令牌未绑定。 |
| **修复方案** | ① `_incrementalGate = SemaphoreSlim(1,1)` + `WaitAsync(0)`（已被占立即抛异常，不排队）→ 两个调用方（KnowledgeBaseController + Program.cs）catch → HTTP 409「已在运行」。② 增量循环绑定 `ApplicationStopping` 令牌 → SIGTERM 后在当前文件边界收手。③ 插入前无条件防御性删除（不由内存 tracker 推断 DB 残留）→ 即使上次异常退出，本次也能从干净状态开始。 |
| **思维链路** | 1. 发现：两次快速点击加载按钮 → DB 中出现重复分块 → 2. 定位：没有互斥机制 → 3. 设计：用 SemaphoreSlim 零等待模式（已在跑就拒绝，不排队）→ 4. 加固：增加取消令牌 + 防御性删除 → 5. 残留：SaveFileTracker 仍批末落盘（逐文件落盘未实施），但优雅取消已在文件边界保进度。 |

---

### Bug-037：nomic-embed 向量嵌入 ubatch-size 缺失导致静默降级

| 项目 | 内容 |
|------|------|
| **修复日期** | 2026-07-29 |
| **位置** | 动脉 A，向量嵌入服务 :8081 启动参数 |
| **现象** | 600+ token 的条款块做向量嵌入时返回 HTTP 500，系统静默降级为 BM25-only 模式（只用关键词检索，不用向量相似度）。搜索结果质量下降但无告警。 |
| **根因** | nomic-embed 使用非因果注意力（non-causal attention），需要整段文本进入单个物理批次（ubatch）。默认 `-ub 512` 的物理批次上限只有 512 token——遇到 600+ token 的块超出上限，服务报错。启动脚本中只写了 `--batch-size 2048`（逻辑批次），缺少 `-ub 2048`（物理批次）。 |
| **修复方案** | 启动参数同时设置 `-b 2048 -ub 2048`（逻辑批次和物理批次都设为 2048），确保系统中最长的条款块（~2000 token）也能单批次处理。两处必须同调：`start_services.sh` + `scripts/zh-diag.sh#L242`。 |
| **思维链路** | 1. 发现：搜索结果部分问题查不到 → 2. 定位：BM25-only 日志 → 3. 深挖：nomic-embed 的 attention 机制需要整段进 ubatch → 4. 对比：llama.cpp 的 `-b` vs `-ub` 参数含义 → 5. 修复：双参数对齐 2048。 |

---

### Bug-036：化工专业条例切块质量三连坑

| 项目 | 内容 |
|------|------|
| **修复日期** | 2026-07-29（分 ①②③ 三次修复） |
| **位置** | 动脉 A，`SemanticChunker` + `PdfOcrService` |

**Bug-036 ①：条款切分规则不适配化工专业条例**

| 子项 | 内容 |
|------|------|
| **现象** | 化工专业条例按"第X条"编号，但切分器按通用规则切，导致一条完整的法规条款被切成多块或和多条合并。 |
| **修复** | 新增 `ChunkByClause` 专用切分策略——识别"第X条"模式进行条款级切分。 |

**Bug-036 ②：超长段落无兜底切分**

| 子项 | 内容 |
|------|------|
| **现象** | 部分文档段落超过 chunk 上限，切分器拒绝处理导致丢块。 |
| **修复** | 新增 `ChunkByParagraph` 强制切分兜底——超长段落按固定长度强制切开，保证不丢内容。 |

**Bug-036 ③：OCR 源头未归一化**

| 子项 | 内容 |
|------|------|
| **现象** | PDF OCR 产生的文本含目录点线（`…·`）、表格线（`│├└`）等干扰字符，污染下游检索。 |
| **修复** | `NormalizeOcrText` 源头归一化——目录点线/表格线替换为白名单字符（空格/换行）。 |

---

### Bug-035 / W24：SQLite 种子脚本被 Split(';') 截断

| 项目 | 内容 |
|------|------|
| **修复日期** | 2026-07 中旬 |
| **位置** | 动脉 C，`ChemicalDatabaseService.InitializeAsync` |
| **现象** | SQL 种子脚本含多语句（CREATE TABLE + 多个 INSERT），原代码用 `Split(';')` 拆分后逐条执行。SQL 语句内部的字符串值含分号时（如 `"浓度≥50%;"`）被误拆，导致 SQL 语法错误、种子写入失败。 |
| **根因** | `Split(';')` 是最粗糙的 SQL 语句分割方式——不理解 SQL 字符串字面量边界。 |
| **修复方案** | 改为整段 `ExecuteNonQuery`——SQLite 引擎原生支持多语句执行，不需要应用层拆分。 |
| **思维链路** | 1. 发现：SQLite 种子数据不完整 → 2. 查日志：SQL 语法错误 → 3. 定位：分号在字符串值中被误拆 → 4. 修复：去掉 Split 逻辑，整段执行。 |

---

### Bug-032 v2：FC=Required 违约时 LLM 输出仍被使用

| 项目 | 内容 |
|------|------|
| **修复日期** | 2026-07 中旬 |
| **位置** | 静脉 2，`AgentDialog.cs#L888` |
| **现象** | FC=Required 模式下，大模型有时不调用工具直接生成回答（toolCalls == 0）。旧逻辑在检测到违约后仍使用 LLM 的文本输出，导致回答基于"大模型瞎编"而非"工具检索的真实数据"。 |
| **根因** | FC 违约检查存在但未阻断——LLM 的裸输出被当作有效回答继续走管线。 |
| **修复方案** | v2：toolCalls == 0 → 完全丢弃 LLM 输出 → 走 `FactAssembler.BuildNoResult()` 生成"无法确定"回答。⚠️ 耦合声明：此修复依赖 FC=Required 契约。如果未来改为 FC=Auto 或 FC=None，必须同步移除此闸门，否则所有正常回答都会被误判为违约。 |
| **思维链路** | 1. 发现：合规检查偶尔输出明显编造的内容 → 2. 查日志：toolCalls=0 但回答非空 → 3. 定位：违约检查存在但未阻断 → 4. 修复：v1 阻断 LLM 输出 → v2 同步修复 FactAssembler 兜底。 |

---

### #1-#9 工程修复速记（已在对应 Bug 条目中详细说明的不重复）

| 编号 | 修复内容 | 日期 | 位置 |
|------|---------|------|------|
| #1 | `_initialized` 仅成功后置位，失败下次启动重试 | 早期 | 动脉 C `InitializeAsync` |
| #2 | `OutputValidator` 库外 GB 号标记【待核实】而非删除（幂等保护） | 早期 | 静脉 2 CLI 路径 |
| #3 | 硬编码字典添加 FIX 注释自证与种子/迁移脚本同源 | 2026-07 | 静脉 1 Level 3 |
| #4 | Dashboard 扫描 202 + 轮询机制 | 早期 | 静脉 5 |
| #5 | `GarbledTextDetector` 三规则确定性拒收乱码块 | 早期 | 动脉 A 增量入口 |
| #6 | （编号空缺，历史原因） | — | — |
| #7 | `WarmupFromEvalSet` 双格式兼容（v1.0 + v1.1 wrapper） | 2026-07 | 静脉 3 预热路径 |
| #8 | `quality-rules.json` 经 `.csproj Content` 配置构建时拷贝 | 2026-07 | 动脉 D |
| #9 | `AgentDialog` 内部状态改为私有字段 `_lastToolResults/_lastToolPlan` | 2026-07 | AgentDialog 内部 |

---

## 三、反直觉陷阱（不是 Bug，但容易踩坑）

> 遵循工程宪法第二条流程 2：将违背直觉的底层陷阱固化为长期知识。

### 陷阱 1：Controller 是瞬时态，内部 new SemaphoreSlim 完全失效

| 项目 | 内容 |
|------|------|
| **来源** | 工程宪法 第二条流程 2 示例 |
| **现象** | `new SemaphoreSlim(1,1)` 写在 Controller 方法内 → 每次请求 new 一个新锁 → 并发控制完全失效。 |
| **正确做法** | 锁必须是单例（注入 Singleton 服务或 static 字段）。 |

### 陷阱 2：SemaphoreSlim 不可重入，漏写 finally.Release 锁永久耗尽

| 项目 | 内容 |
|------|------|
| **来源** | 工程宪法 第二条流程 2 示例 |
| **现象** | 异步方法中 `await semaphore.WaitAsync()` 后抛异常 → 未执行 `Release()` → 锁计数永久 -1 → 服务假死。 |
| **正确做法** | `await semaphore.WaitAsync(); try { ... } finally { semaphore.Release(); }` |

### 陷阱 3：单进程信号量在多实例 Docker 部署下完全失去互斥效果

| 项目 | 内容 |
|------|------|
| **来源** | 工程宪法 第二条流程 2 示例 |
| **现象** | Docker Compose 起 3 个 API 实例，每个实例有自己的 SemaphoreSlim → 全局最多 3 个并发，而非预期的 1 个。 |
| **正确做法** | 跨实例互斥必须用分布式锁（如 PostgreSQL advisory lock 或 Redis）。 |
