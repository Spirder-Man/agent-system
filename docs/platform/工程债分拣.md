# 工程债分拣

只标期，不修复、不重写病灶表和台账正文。第一阶段不改业务代码；运维辅助脚本只改默认 URL/监听口。

总闸：[工程治理.md](工程治理.md)。伤疤原文：[病灶登记表.md](病灶登记表.md)。库表原文：[全量问题台账](../db/数据库化石/全量问题台账.md)。

期次：

- **冻结**：本阶段不改；第三阶段若动相关门面再考虑
- **设计如此**：不是 Bug，扩数据时按手册处理
- **第二阶段**：规则引擎 / 种子 / 无 GPU 结论
- **第三阶段**：双通道、可追溯、表结构与页面链路
- **另开**：需要语料、OCR 或运行库，不绑在这三期
- **已核销**：不翻案

---

## 病灶（活跃 5 条）

| 来源 | 摘要 | 第一阶段 | 交给 |
|------|------|----------|------|
| 病灶 1 | 快速模式使存量乱码块常驻 | 只记账 | 另开（有语料与 OCR 再重灌） |
| 病灶 2 | 注释仍写「静态内存字典」 | 已知陷阱，禁止本阶段改注释以外的代码；本阶段连注释也不改（避免动 `Agent1/**`） | 第三阶段若动知识门面一并改注释 |
| 病灶 3 | `ChemicalDatabaseService`（SQLite）与 `ChemicalSubstanceDatabase`（PG）类名相近 | 禁止本阶段重命名 | 第三阶段若动知识门面再考虑 |
| 病灶 4 | 旧 SQLite 库 `COUNT(*)==0` 才灌种子 | 标为设计如此 | 第二阶段扩规则时按联动手册删库重建 |
| 病灶 5 | 评测白名单 `CheckRegulation` ≠ 工具名 `CheckRegulationVersion` | 本阶段不改评测白名单 | 第二/三阶段动评测或双通道时再改 |

---

## 台账未核销 / 验证中

已核销（#3/#5/#17/#18/#20/#25/#27/#30/#31/#36）保持已核销，下表不列。

| # | 摘要 | 第一阶段 | 交给 |
|---|------|----------|------|
| 1 | `audit_logs.module` 全空 | 不改表、不改接口 | 第三阶段可追溯 |
| 2 | `audit_logs.ip_address` 多为空 | 不改 | 第三阶段可追溯 |
| 4 | `audit_logs.detail` 自由格式 | 不改 | 第三阶段可追溯 |
| 6–8 | `chemical_documents` 空表化石 / 索引 / 兜底路径 | 不改 | 第三阶段（退役表须先拆代码） |
| 9 | `chemical_documents` 退役三步走未执行 | 不改 | 第三阶段 |
| 10 | `knowledge_chunks` 水印垃圾块 | 只记账，不重灌 | 另开（OCR 重灌） |
| 11 | 分块溯源完整率 0%（章节+条款+页码） | 不改 | 第三阶段可追溯 |
| 12 | 未实现 `UNIQUE(file_hash, chunk_index)` | 不改 schema | 第三阶段 |
| 13 | `regulation_type` 分类不一致 | 不改 | 第三阶段 |
| 14 | `content_hash` 全空，增量检测未生效 | 不改 | 第三阶段 |
| 15 | `regulation_title` / `parent_category` 幽灵字段 | 不改 | 第三阶段 |
| 16 | `total_chunks` 语义=切块数≠入库数 | 不改 | 第三阶段 |
| 19 | 长期记忆 `source_session_id` 孤儿引用 | 不改 | 第三阶段 |
| 21 | 物质覆盖缺口（35 vs 重点监管清单） | 不扩种子 | 第二阶段规则引擎 |
| 22 | `physical_state` 文本不统一 | 不改 | 第二阶段 |
| 23 | `cas_number` 无索引 | 不改 | 第二阶段（覆盖扩大时一并） |
| 24 | 别名瑕疵（丙三醇自指、氧气瓶） | 不改种子 | 第二阶段 |
| 26 | 硫化氢→二氧化硫标兼容 | 不改种子 | 第二阶段规则引擎 |
| 28 | 禁忌 `regulation_ref` 近半为空 | 不改 | 第二阶段 |
| 29 | 苯↔丙酮双向冗余、无 UNIQUE | 不改 | 第二阶段 |
| 32 | 安全距离 `facility_pair` 无 UNIQUE | 不改 | 第二阶段 |
| 33 | 安全距离覆盖稀疏 | 不扩种子 | 第二阶段 |
| 34 | 丙三醇无危害类别 | 不改 | 第二阶段 |
| 35 | 危害类别无 UNIQUE | 不改 | 第二阶段 |
| 37 | 法规编号无 UNIQUE | 不改 | 第二阶段 |
| 38 | 8 部法规仅 3 部有全文入库 | 国标全文不进 Git | 第二阶段只扩展**结构化**种子；全文仍由使用方自备 |
| 39 | `refresh_tokens` 过期堆积 | 不改 | 另开（运维清理） |
| 40 | `search_logs` 幽灵表零写入 | 不改 | 第三阶段若做检索观测再接线 |
| 41 | `drift_details.probe_key` 历史空 | 不改 | 另开 |
| 42 | 漂移探针仅 demo 量、无处置闭环 | 不改 | 另开 |

---

## 文档口径债（本阶段只声明，不重写长文）

| 来源 | 摘要 | 第一阶段动作 |
|------|------|----------------|
| [测试总纲.md](测试总纲.md) | 同一页 665 / 712 / 1478 等历史条数；SSH `:15001` 旧路 | 文首声明：分层定义仍有效；条数与隧道段落是历史；现网以 CI 与模块说明书为准。不重写千行正文 |
| [系统血谱.md](系统血谱.md) | 器官表曾写 API 5001 | 改为 **5000**；隧道 15000/15001 标明为历史远程路 |
| 运维默认口 | 裸机脚本 / `health-check.ps1` / Vite / Real Playwright 曾默认 `:5001` 或隧道 `:15000`/`:15001` | **本轮已对齐**：默认 `:5000`；隧道须显式参数 |
| [skills/](skills/) | 混沌工程 / git hook 铁网 | 标为方法底稿，非现网门禁。不删 |

同源拷贝组 1 见 [同源拷贝联动手册.md](同源拷贝联动手册.md)。第二阶段扩种子前必须先打开该手册。
