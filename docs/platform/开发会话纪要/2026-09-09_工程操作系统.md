# 2026-09-09 · 工程操作系统落地

## 做了什么

- 新增 `AGENTS.md`、目录地图、运行时端口登记、测试台账、开发会话纪要模板。
- 仓库 `.cursor/rules/` 六条、`.cursor/skills/` 五个；`.cursor/mcp.json` 只声明 GitHub（读环境变量）。
- 非规范脚本迁入 `scripts/_legacy/`；根 `start_services.sh` 转发；`_scratch` gitignore。
- 未改 `Agent1` / `Agent1.Api` / `agent1-web/src` / 种子 / CI job。

## 脚本去向

- 未在 `_scratch` 新建脚本。
- AutoDL / 一次性脚本：`git mv` 到 `scripts/_legacy/`（见该目录 README）。
- 通用裸机启动：`scripts/start_services.sh`（读仓库根 `.env`，`PROJECT_DIR` 默认仓库根）。

## 踩坑

- GitHub MCP（`user-github`）本会话 `mcp_auth` 两次超时。降级：Cursor Settings 里登录，或用 `gh`。不要把 token 写入仓库。
- 根目录旧 `start_services.sh` 若直接挪到 `scripts/` 而不改路径，会在 `scripts/` 下找 `.env`。已改为 `ROOT=scripts/..`。

## 台账

- [x] 端口登记
- [x] 测试台账「最近变更」
- [x] 目录地图 / AGENTS.md
- [x] CHANGELOG（仅仓库可见变更）
