# 飞致云 RTX 4090 `gpu-full` 深度分析对比报告

> **日期**：2026-09-07  
> **SUT**：飞致云按量 RTX 4090 · 离线包 `容器化部署/gpu` · 模型 `qwen3:8b`（Qwen3-8B Q4_K_M）+ vision `qwen2.5-vl-7b-instruct`  
> **入口**：公司电脑 `scripts/gpu-five-layer.ps1 -Profile gpu-full`（SSH 端口当时 `65037`）  
> **本轮产物**：`eval_reports/20260907_165051/`  
> **对照基线**：[测试总纲.md](./测试总纲.md) v2.2 §4.2 / Task 11（工具 85.7% · 参数 82.5% · 结论 65.1% · 忠实度 23.9%）  
> **方法**：总纲三层漏斗 + 七大类目 + D1–D6；不跑 AutoDL `int-test-task11.sh`。  
> **总判定**：**不能写「GPU 全量通过」**（`exitCode=1`）。否决项只有 L2 缓存 0.12s。L3 / L4 评测 / L4 识图 HTTP 已绿。指标相对历史**上升**，无 `alerts[]`。

交互对照看板（可在 Cursor 里打开）：[featurize-4090-gpu-full-analysis.canvas.tsx](C:/Users/lcy/.cursor/projects/d-agent/canvases/featurize-4090-gpu-full-analysis.canvas.tsx)

---

## 1. 本轮对应总纲哪一层

总纲旧脚本是 Part A→B→C→D。飞致云改成**同一条编排、两档**：

| 总纲说法 | 本轮怎么跑 | 本轮结果 |
|----------|------------|----------|
| Part A 基础设施 | Gate1：8080/8081/8083/5000/8088 | **PASS**（含 vision） |
| Part B API 全链路 | 登录 + Playwright 九 spec | 登录 200；L3 **44/44 PASS** |
| Part C 63 条评测 | L4 `POST /api/Eval/run` `taskId=bc3b18e3` | **completed**，0 error |
| Part D RAG 召回 | 镜像 `GetStatus` 未投影 P@K / MRR | **N/A**（不是 0） |
| 视觉 | L4 `POST /api/Multimodal/analyze` | HTTP **200 · 墙钟 3.4s**（见 §6 正文口径） |
| 白盒 L0/L1 | 本机 ArchitectureTest / xUnit / vitest | vitest **FAIL**（不挡 GPU） |
| L2 集成问句 | 非关键词快路径安全距离 | **FAIL 0.12s**（命中 `gpu-quick` 同句缓存） |

`gpu-quick`（同日更早）Gate1 + L2 3.6s `GetSafetyDistance` + L3 36 条（无 llm-quality）已通过。本轮是 **full**：补上 llm-quality、63 条、识图。

---

## 2. 总判定怎么读（不要和 Playwright 绿混在一起）

`http://localhost:9323/` 的 **44 passed** 只覆盖 **L3 页面**。五层 summary：

| 层 | 状态 | 含义 |
|----|------|------|
| Gate1 | PASS | 打的是飞致云（本机改绑 18088/15000/18080/18081/18083） |
| L0/L1 | FAIL vitest | 与 GPU 正交，不否决 |
| **L2** | **FAIL 0.12s** | **硬失败**。`toolsUsed=[GetSafetyDistance]` 但亚秒 = API 内存缓存，不是 GPU |
| L3 | PASS | 9 spec 全量，含 llm-quality |
| L4 评测 | PASS | 63 条 completed |
| L4 识图 | PASS（门禁） | 200 且墙钟 ≥3s |
| L4 分析 | PASS | `comparison.json` + `analysis.md`；SSH docker 日志 `exit=1` 因容器无 `/app/Data/eval_report.json`，评测分以 HTTP `eval.json` 为准 |

硬卡规则：Gate1 / L2 / L3 / Eval 未 completed / 识图失败 → 整轮 exit 1。指标退化只进 `alerts[]`。本轮 **alerts 为空**。

L2 已改为问句带一次性编号，避免 quick 之后 full 再撞缓存。未 recreate 飞致云容器。

---

## 3. Part C — 63 条评测：对照 Task 11 漏斗

### 3.1 核心三率

```
Task 11（总纲历史）     飞致云 4090 本轮
工具  85.7% (54/63)  →  93.7% (59/63)   +7.9 pp
参数  82.5% (52/63)  →  88.9% (56/63)   +6.4 pp
结论  65.1% (41/63)  →  85.7% (54/63)   +20.6 pp
异常  —              →  0
```

漏斗变窄：

```
历史: 85.7% ── -3.2pp ──→ 82.5% ── -17.4pp ──→ 65.1%
本轮: 93.7% ── -4.8pp ──→ 88.9% ── -3.2pp ──→ 85.7%
```

Task 11 的主瓶颈是 **结论层掉 17pp**（幻觉 GB、忠实度 23.9%）。本轮结论 85.7%，主瓶颈改到 **属性查询工具选错** 和 **安全距离参数/结论**（见 §3.3）。

总纲理想线「结论 ≥90% / Top-5 ≥85%」仍未作为死门槛；结论距 90% 差 4.3pp。P@5 / 忠实度本轮镜像未投影，**不能写成 0，只能写未测**。

### 3.2 七大类目（按期望工具；总纲旧表是 15/20/8/7/5/5/3，本轮用例切分略变，合计仍 63）

| 类目（期望工具） | n | 工具 | 参数 | 结论 | vs Task 11 印象 |
|------------------|---|------|------|------|-----------------|
| 危险类别 `CheckHazardCategory` | 16 | 94% (15) | 94% | **88% (14)** | 历史结论仅 **26.7%**，本轮拉起来了 |
| 储存兼容 `CheckStorageCompatibility` | 25 | **96% (24)** | 96% | 88% (22) | 历史工具触发仅 **65%** |
| 安全距离 `GetSafetyDistance` | 9 | **100%** | 78% (7) | 78% (7) | 历史结论 87.5%，本轮略回落在参数/结论 |
| 重大危险源 `GetMajorHazardThreshold` | 5 | 100% | 100% | **100%** | 与历史双百一致 |
| 法规版本 `CheckRegulationVersion` | 3 | 100% | 100% | **100%** | 与历史双百一致 |
| 化学品属性 `LookupChemicalProperties` | 5 | **60% (3)** | 40% (2) | 60% (3) | 仍是最弱类目 |
| 综合审核（旧表 7 条） | — | — | — | — | 本轮未单独成组，多并入储存/类别 |

工具调用次数（实际，含选错）：`CheckStorageCompatibility` 25 · `CheckHazardCategory` 16 · `GetSafetyDistance` 9 · `GetMajorHazardThreshold` 5 · `LookupChemicalProperties` 4 · `CheckRegulationVersion` 3 · 无工具 1。

### 3.3 失败模式（总纲 A/B/C；12 条未三绿，有重叠计数见 analysis.md）

**A 工具选错 / 未调（4）**

| 问句 | 期望 | 实际 |
|------|------|------|
| 硫酸的危险特性是什么？ | CheckHazardCategory | LookupChemicalProperties |
| 甲苯和二甲苯可以同库吗？ | CheckStorageCompatibility | （无工具） |
| 苯的CAS号是多少？闪点多少度？ | LookupChemicalProperties | CheckHazardCategory |
| 查询硫酸的储存禁忌有哪些？ | LookupChemicalProperties | CheckStorageCompatibility |

属性 vs 类别 vs 储存禁忌，IntentRouter / FC 仍会抢工具。储存类只剩 1 条完全没调工具（历史这类是 7/20）。

**B 工具对、参数错（3）** — 全是距离或属性：氨罐–厂外道路、氯气–居住区、双氧水 CAS+类别。

**C 工具参数对、结论错（5）** — 氨水类别；高锰酸钾+甘油、环氧乙烷+氨气同库；乙炔气柜–办公楼、甲类工艺装置间距。

代码映射仍按总纲：A → IntentRouter / SK FC；B → FactExtractor；C → EvalEngine / ReflectionVerifier。本轮**不改评测集、不改 EvalEngine**。

---

## 4. Part B / L3 — Playwright Real（localhost:9323）

`chromium-real` · 2026-09-07 16:53 · **44 expected / 0 unexpected** · 3.7 min · 已归档 `eval_reports/20260907_165051/playwright-report-real/`。

| spec | 条数 | 结果 | 对应总纲页面 |
|------|------|------|----------------|
| assets.spec.ts | 4 | 全过 | P4 资产台账 |
| audit-log.spec.ts | 5 | 全过 | P5 审计 + 哈希链 |
| compliance-check.spec.ts | 4 | 全过 | P1 合规自查 |
| dashboard.spec.ts | 3 | 全过 | P3 看板 + 扫描 |
| emergency-response.spec.ts | 4 | 全过 | 应急 |
| eval-flow.spec.ts | 5 | 全过 | 评测页三场景 |
| inspection-flow.spec.ts | 5 | 全过 | P2 巡检 |
| knowledge-base.spec.ts | 6 | 全过 | 知识库 GB / 三模式 |
| llm-quality.spec.ts | 8 | 全过 | **quick 去掉、full 才跑** 的七类目质量门 |

页面里仍有 `160ms < 3000ms 跳过耗时下限`：合规/应急问句带「苯/甲醇/安全距离」，走关键词快路径。**L3 不把这当失败**；GPU 证据看 L2（本轮缓存失败）和 L4 评测墙钟（约 15 min running→completed）。

---

## 5. D1–D6 摘要（自动切片 + 本报告补全）

| 维 | 结论 |
|----|------|
| D1 格式 | 63 条、三率齐全、0 error；RAG 字段 N/A；docker 日志切片因 SSH 末条 `cat eval_report.json` 失败未灌满 |
| D2 业务 | A4 / B3 / C5；属性类最差；类别结论相对 Task 11 大幅好转 |
| D3 链路 | Login → Eval/run → EvalEngine ×63 → llama-server :8080；报告经隧道落到 `eval.json` |
| D4 设计 | 8B Q4 仍是上限；关键词快路径与评测 FC 路径分离；本轮未改架构 |
| D5 异常 | 无 P0；结论相对历史 **+20.6pp** 不是骤降；工具率 93.7% > 80% 阈值 |
| D6 性能 | 评测段约 15 min / 63 条（约 14s/例量级）；识图墙钟 3.4s（含 2.3MB 上传） |

---

## 6. L4 识图：门禁 PASS ≠ 标签识别成功

| 项 | 值 |
|----|----|
| 文件 | `大佐胜哥.png`（约 2.3MB） |
| HTTP | 200 |
| 墙钟 | 3.4s（编排门禁：200 且 ≥3s） |
| 模型耗时 `elapsedMs` | 1298 |
| 模型正文 | **「并非化学品标签，无法提取 GHS」** |

门禁只证明 vision 容器在跑、请求没有秒回假成功。**不能**据此写「GHS 标签识别通过」。下次 `-VisionImage` 换真实危化标签或储罐图。

---

## 7. 与 Task 11 深度分析的差异（经验怎么用在这一轮）

Task 11 报告当时的误报模式（jq 未装、curl 格式、B1 有 Token 却判失败）本轮**没有再犯**：登录、评测 `completed`、Playwright 44 绿都是编排直打 HTTP，不经旧 `int-test-task11.sh`。

沿用的分析骨架：

1. 先总判定（哪一层硬失败）再看三率  
2. 三率漏斗定位瓶颈从「结论幻觉」换成「属性路由 + 距离参数」  
3. 七类目表对照，而不是只看总分  
4. A/B/C 失败映射到具体文件  
5. 亚秒合规问句单独剔除，不当 GPU 证据  

本轮新坑（写入两档说明）：`AUTH_ACCOUNTS_JSON` YAML 剥引号 → 登录 500；L2 同句缓存 → full 假失败；识图图不是标签。

---

## 8. 下一步（分析结论，不是再发明测试）

1. 不能对外写 GPU 全量通过。  
2. 重新跑 `gpu-full`（问句已带编号）才能把 L2 硬卡补绿。飞致云不要 `down -v`、不要 rebuild-llama。  
3. 识图换真标签图。  
4. 属性类 3 条路由失败：若要抬 `LookupChemicalProperties`，改 IntentRouter/FC prompt，不改 63 条用例。  
5. RAG P@K / 忠实度：等 API 镜像把 GetStatus 字段投影出来再比总纲 50.5% / 23.9%。  

### 本轮文件

| 文件 | 路径 |
|------|------|
| 五层 summary | `eval_reports/20260907_165051/five-layer-summary.json` |
| 评测 | `eval.json` |
| 指标对照 | `comparison.json` |
| D1–D6 草稿 | `analysis.md` |
| Playwright HTML/JSON | `playwright-report-real/` |
| 识图正文 | `vision-body.json` |
