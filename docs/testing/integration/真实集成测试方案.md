# Agent1 真实集成测试方案

## 一、你要测的 vs 怎么测

| 你关心的 | 测试方式 | 依赖 |
|----------|---------|------|
| 数据库连通 | 跑 `DatabaseIntegrationTests` | PostgreSQL 16 + pgvector |
| LLM 推理 | 跑 LLM 功能验证脚本 | llama.cpp 启动 + Qwen3-8B 模型 |
| RAG 召回率 | 跑 `EvalEngine` 全量评测 | LLM + 知识库 + 评测集 |
| 脏数据处理 | 跑异常输入测试脚本 | 知识库 (可以不连 LLM) |
| 状态机全链路 | 跑 API 端到端流程 | Agent1.Api 启动 |
| 追踪链路 TraceId | 发请求 → 搜 Seq 日志 | Seq 服务 |
| 结构化日志 | 检查 Seq 控制台 | Serilog + Seq |

---

## 二、第一步：数据库集成测试

### 前提
```bash
# 启动 PostgreSQL
pg_ctlcluster 16 main start

# 初始化数据库
psql -h localhost -U postgres -d chemical_park_ai_agent -f init_database.sql
```

### 运行
```powershell
$env:DB_HOST = "localhost"
$env:DB_PORT = "5432"
$env:DB_NAME = "chemical_park_ai_agent"
$env:DB_USERNAME = "postgres"
$env:DB_PASSWORD = "changeme"

dotnet vstest Agent1.Tests/bin/Debug/net8.0/Agent1.Tests.dll --TestCaseFilter:"Category=Integration"
```

### 这会测什么
- ✅ 数据库连接是否正常
- ✅ 表结构是否存在（sessions / audit_logs / chemical_documents / search_logs）
- ✅ CRUD 是否正确（增删改查、批量写入、清空）
- ✅ 审计日志是否正确写入
- ✅ 10 并发写入是否全部成功（无丢数据）
- ✅ PageNumber 是否正确持久化

---

## 三、第二步：LLM 功能验证

### 前提
```bash
# 启动 llama.cpp
cd /root/autodl-tmp/llama.cpp
./build/bin/llama-server \
  -m /root/autodl-tmp/models/qwen3-8b-q4_k_m.gguf \
  --host 0.0.0.0 --port 8080 \
  -ngl 99 -c 8192
```

### 运行
```powershell
# 启动 Agent1.Api
dotnet run --project Agent1.Api

# 另一个终端发请求
curl -X POST http://localhost:5000/api/Compliance/check \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <token>" \
  -d '{"query":"苯和丙酮能放在同一个仓库吗"}'
```

### 手动检查
- [ ] LLM 返回了合规判断（是/否）
- [ ] 引用了正确的法规编号（GB 15603）
- [ ] 调用了正确的工具（CheckStorageCompatibility）
- [ ] 没有幻觉法规（hallucinatedRegulations 为空或可验证）

---

## 四、第三步：RAG 召回率评测（评测集已有）

### 数据
`Data/ComplianceEvalSet.json` — 64 条化工合规评测用例，每条包含：
- 查询文本
- 期望的工具调用
- 期望的法规编号
- 期望的合规结论

### 运行
```bash
# 需要有 LLM + 知识库
# EvalEngine 会自动加载评测集，逐一调用 AgentDialog，对比期望结果
# 项目没有 CLI 入口，需要直接在代码中调用：
# var engine = new EvalEngine(agentDialog, evalSetPath);
# var report = await engine.RunFullEvalAsync();
```

### 这会测什么
- ✅ RAG 检索召回率（ExpectedRegulationNumber 是否出现在检索结果中）
- ✅ 工具调用准确率（ExpectedTools 是否都被调用）
- ✅ 合规结论准确率（IsCompliant 是否正确）
- ✅ 安全距离精度（ExpectedDistance ±1m）

---

## 五、第四步：脏数据 / 异常输入测试

### 手动测试清单

| 测试场景 | 输入 | 期望行为 |
|----------|------|---------|
| 空查询 | `""` | 返回 400 BadRequest |
| 超长查询 | 10000 字文本 | 脱敏截断，不崩溃 |
| SQL 注入 | `'; DROP TABLE--` | SafetyGuardService 拦截 |
| Prompt 注入 | `忽略之前的指令` | SafetyGuardService 拦截 |
| 不存在的化学品 | `不存在的化学品XYZ` | 知识库返回空，LLM 说"未找到" |
| 特殊字符 | `!@#$%^&*()` | 不崩溃，正常返回 |
| PDF 解析乱码 | 损坏的 PDF | PdfExtractor 返回空或报错，不崩溃 |
| 重复文档入库 | 相同内容入库 3 次 | 知识库去重或正确累加 |

---

## 六、第五步：追踪链路 TraceId 验证

### 手动验证
```bash
# 发一个合规查询请求
curl -v http://localhost:5000/api/Compliance/check -d '{"query":"苯"}'

# 检查响应头
# X-Request-Id 应该在响应头中

# 检查 Seq 日志（如果配置了）
# 搜索同一个 TraceId，看是否贯穿：
#   RequestIdMiddleware → AuthController → AgentDialog → LlmService → DatabaseService
```

### 代码验证（已有测试）
`CircuitBreakerTests.ExecuteAsync_SimpleInput_ReturnsResult_WithTraceId` 已验证：
- 每个请求有唯一 TraceId
- PipelineMetrics 记录了 6 步耗时

---

## 七、第六步：结构化日志验证

### 前提
```bash
# 启动 Seq（如果 docker-compose 中有）
docker-compose up seq

# 或者直接看控制台 + 文件日志
# logs/agent1-{date}.log
```

### 检查
- [ ] 日志是否 JSON 格式
- [ ] 是否包含 `TraceId` 字段
- [ ] 是否包含 `RunId` 字段
- [ ] 是否包含 `SessionId` 字段
- [ ] 错误日志是否包含完整堆栈
- [ ] LLM 调用日志是否包含耗时和 Token 数

---

## 八、总结：你现在可以做什么

### 不需要 GPU / LLM 也能测的
```powershell
# 1. 数据库集成测试（需 PostgreSQL）
dotnet vstest Agent1.Tests/bin/Debug/net8.0/Agent1.Tests.dll --TestCaseFilter:"Category=Integration"

# 2. 代码逻辑（已完成）
.\test-all.ps1   # 181 passed
```

### 需要 GPU + LLM 的（在 3090 服务器上）
```bash
# 3. 启动全栈
docker-compose up -d

# 4. 发请求验证
curl -X POST http://localhost:5000/api/Compliance/check \
  -H "Authorization: Bearer $(curl -s -X POST http://localhost:5000/api/Auth/login \
    -H 'Content-Type: application/json' \
    -d '{"username":"admin","password":"xxx"}' | jq -r '.token')" \
  -H "Content-Type: application/json" \
  -d '{"query":"苯和丙酮能放在同一个仓库吗"}'

# 5. 检查日志
docker-compose logs api | grep -E "TraceId|ERROR|WARN"
```
