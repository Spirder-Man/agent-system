# 系统日志解读与排障实战指南

> **适用项目**: Agent1 化工园区危化品合规审核 AI Agent
> **创建日期**: 2026-06-25
> **关联文档**: [Task11 集成测试部署指南](./integration/Task11-集成测试部署指南.md) | [故障排查文档](../_archive/troubleshooting/故障排查文档.md) | [架构收敛专项测试说明](./架构收敛专项测试说明.md)

---

## 一、日志体系全景

本项目采用 **三层日志架构**：

```
Serilog 结构化日志 ──→ *.log 文件 (JSON/纯文本)
       │
       ├── Console.WriteLine ──→ 控制台实时输出
       │
       └── PipelineEvent ──→ 内存事件流 (结构化指标)
```

**日志文件分布**：

| 目录 | 文件 | 内容 |
|------|------|------|
| `logs/` | `agent1-YYYYMMDD.log` | Serilog 结构化日志 (生产格式) |
| `logs/` | `full-YYYYMMDD.log` | 诊断双写日志 (Console + Serilog) |
| `logs/linux/` | `自动化脚本测试输出/` | Linux 自动化测试输出 |
| `Agent1.Api/logs/` | `agent1-api-*.log` | API 服务日志 |

---

## 二、如何阅读日志中的关键信息

### 2.1 日志格式解析

以 `agent1-20260623.log` 中的一条典型日志为例：

```
[2026-06-23 00:23:04.689 INF] LAPTOP-40G212RC RunId=0509e705 [Pipeline] 开始 | TraceId=ce1bc382 | 输入长度=10 | 输入=苯和丙酮能一起储存吗
```

拆解后的字段含义：

```
┌── 时间戳 (精确到毫秒)
│                          ┌── 日志级别 (INF/WRN/ERR/FATAL)
│                          │     ┌── 机器名
│                          │     │               ┌── 运行实例ID (每次启动唯一)
│                          │     │               │            ┌── 模块标签 ──┐
│                          │     │               │            │              │
[2026-06-23 00:23:04.689 INF] LAPTOP-40G212RC RunId=0509e705 [Pipeline] 开始 | TraceId=ce1bc382 | 输入长度=10 | 输入=苯和丙酮能一起储存吗
                                                                                        │                 │
                                                                              TraceId=ce1bc382      结构化字段
                                                                              (本次请求全程一致)
```

**关键识别技巧**：

| 你要找的 | 看什么 | 示例 |
|----------|--------|------|
| 某次请求的完整链路 | 相同的 `TraceId` | `ce1bc382` 从开始到完成的所有日志 |
| 某次运行的所有日志 | 相同的 `RunId` | `0509e705` |
| 某个模块的行为 | `[模块名]` 标签 | `[Pipeline]`, `[IntentRouter]`, `[Memory]` |
| 错误/异常 | 日志级别 `ERR`/`WRN` | `WRN` 安全拦截, `ERR` 服务异常 |

### 2.2 日志级别含义与应对策略

```
INF (Information)  → 正常业务流程，无需处理
WRN (Warning)      → 非致命问题，需关注但系统可继续
ERR (Error)        → 功能受损，需排查修复
FATAL (Fatal)      → 系统级崩溃，需立即处理
```

**实战案例**：从 `agent1-20260623.log` 第 6 行：

```
[2026-06-23 00:22:58.446 WRN] [Pipeline] 安全拦截 | TraceId=0c68ec2f | 原因=检测到潜在注入攻击: 匹配模式 "忽略之前的指令"
```

- **级别是 WRN 不是 ERR**：说明这是**预期行为**——安全模块正常工作了
- **不需要修复**：系统正确地拦截了注入攻击
- **需要关注**：如果有大量此类 WRN，说明有人在尝试攻击

### 2.3 测试结果日志格式

以 `summary.txt` 为例：

```
Agent1 自动化测试报告
══════════════════════
时间: 2026-06-24 00:26:20
通过: 22
失败: 0
跳过: 3
总计: 25
总耗时: 338s
LLM: 200     ← LLM 服务 HTTP 状态码 (200=正常)
Embed: 200   ← Embedding 服务 HTTP 状态码

各测试日志:
  T1_CoT标准推理: PASS
  T12_工具调用诊断: PASS
  T13_合规评测集: PASS
```

**快速判断**：
- `PASS` = 测试通过
- `FAIL` = 需要排查 (去找对应 `T*.log` 文件)
- `SKIP` = 前置条件不满足而跳过 (如 API 未启动)

---

## 三、通过日志排查系统运行问题

### 3.1 排查流程：从现象到根因

```
现象 → 定位时间窗口 → 找到 TraceId → 追踪完整链路 → 定位故障点
```

**实战案例 1：向量嵌入失败**

从 `full-20260605.log` 第 314-315 行：

```
⚠️ 向量请求失败 [InternalServerError]: {"error":"the input length exceeds the context length"}
⏭️ 向量生成失败，跳过向量库写入（BM25 已写入）
```

**排查步骤**：

| 步骤 | 操作 | 发现 |
|------|------|------|
| 1. 识别现象 | 看到 `⚠️` + `InternalServerError` | 向量服务返回 500 错误 |
| 2. 读错误详情 | `"the input length exceeds the context length"` | **根因**：文本块超过嵌入模型的上下文窗口 |
| 3. 看降级行为 | `⏭️ 跳过向量库写入（BM25 已写入）` | 系统有降级：向量跳过但 BM25 仍写入 |
| 4. 判断影响 | 搜索同类错误次数 | 全文出现 15 次同类错误 |
| 5. 修复方向 | 调整分块策略 | 减小 `SemanticChunker` 的 `MaxChunkSize`，或增加嵌入服务的 `-c` 参数 |

> 📖 关联记忆：`llama-server嵌入服务需同步调大-c和-b参数` — 嵌入服务上下文窗口需要与分块大小匹配

**实战案例 2：知识库质量问题**

从 `full-20260605.log` 第 2500-2558 行：

```
🚫 脏数据拦截: 来源=分析检验组班组长与员工责任书（分析）, 质量=未知, 内容长度=0
⚠️  部分:  2
❌ 失败:   0
⏭️  跳过:  1100 (空表单/模板)
```

**阅读技巧**：日志用符号前缀表示状态

| 符号 | 含义 | 是否需要处理 |
|------|------|:---:|
| ✅ | 操作成功 | 否 |
| ⚠️ | 警告/降级 | 关注 |
| ❌ | 错误/失败 | 是 |
| 🚫 | 主动拦截 | 看情况 |
| ⏭️ | 跳过 | 否 |
| 📦 / 🔗 / 📝 | 信息标记 | 否 |

这里 `🚫 脏数据拦截` + `内容长度=0` 说明**系统正确拦截了空文件**（企业制度模板的 DOCX 文件提取后无有效文本），这不是 Bug。

**质量报告解读**：

```
文件总数: 1158    ← 知识库目录下所有文件
✅ 成功:   56     ← 成功入库 56 个文档
⚠️  部分:  2     ← 2 个文档部分成功（向量失败但 BM25 成功）
❌ 失败:   0     ← 0 个完全失败
⏭️  跳过:  1100  ← 1100 个是空表单/模板，被脏数据拦截
总块数:    1241  ← 最终 1241 个语义块可用于检索
```

**判断标准**：如果 `❌ 失败 > 0`，需要排查；如果 `⏭️ 跳过` 占比高且都是空表单，属于正常现象。

### 3.2 排查清单速查表

| 现象 | 查什么日志 | 关键搜索词 | 参考 |
|------|-----------|-----------|------|
| API 启动失败 | `agent1-api-*.log` | `FATAL`, `Exception`, `JWT_KEY` | [故障排查文档#环境变量](../_archive/troubleshooting/故障排查文档.md) |
| 数据库连不上 | 启动日志 | `数据库连接`, `Npgsql` | [Task11 A1 检查](integration/Task11-集成测试部署指南.md) |
| LLM 无响应 | `full-*.log` | `超时`, `被取消`, `canceled` | [故障排查文档#错误9](../_archive/troubleshooting/故障排查文档.md#-高频错误-9-deepseek-r1调用被取消超时) |
| 工具调用失败 | `T12_工具调用诊断.log` | `未知工具`, `调用失败` | [故障排查文档#错误2](../_archive/troubleshooting/故障排查文档.md#-高频错误-2工具名不匹配) |
| 安全拦截异常多 | `agent1-*.log` | `[Pipeline] 安全拦截` | 检查是否误拦截正常请求 |
| 结论幻觉 | 评测日志 | `忠实度`, `幻觉` | [Task11 Part C 分析](#) |
| 服务挂了 | 最后几行日志 | `FATAL`, `crash`, `killed` | `journalctl -u agent1` |

### 3.3 TraceId 全链路追踪实战

在 `agent1-20260623.log` 中，同一个 `TraceId=ce1bc382` 贯穿了完整流程：

```
行7:  [Pipeline] 开始 | TraceId=ce1bc382 | 输入=苯和丙酮能一起储存吗
行8:  [IntentRouter] 关键词 "储存" 命中 → ChemicalCompliance
行9:  [Pipeline] 完成 | TraceId=ce1bc382 | 总耗时=9508ms | 意图=ChemicalCompliance | 工具调用=0
```

**从这里能读到什么？**

1. **用户输入**：`苯和丙酮能一起储存吗` (10 个字符)
2. **路由决策**：`"储存"` 命中合规关键词 → `ChemicalCompliance`
3. **总耗时**：9508ms（约 9.5 秒），`执行=9400ms` 占绝对主导
4. **工具调用=0**：⚠️ **这是一个异常信号！**——对于"储存兼容性"问题，应该调用 `CheckStorageCompatibility` 工具

> **排障线索**：如果 `工具调用=0` 但意图是 `ChemicalCompliance`，需要检查工具调度的 Prompt 是否正确引导了工具选择。

**完整链路查询技巧**：

```bash
# Linux 上提取某个 TraceId 的全部日志
grep "ce1bc382" agent1-20260623.log

# 统计某次运行中每种意图的数量
grep "意图=" agent1-20260623.log | sed 's/.*意图=//' | sed 's/ |.*//' | sort | uniq -c
```

---

## 四、验证业务逻辑是否按预期执行

### 4.1 六步线性流水线验证

从 [AgentDialog.cs](../../Agent1/Services/Dialog/AgentDialog.cs#L75-L170) 的代码，每条请求必须依次经过 6 个步骤并在日志中体现：

```
[1/6] 预处理完成
  ↓
[安全检测] Prompt 注入检测 → 不通过则返回 "安全拦截"
  ↓
[2/6] 意图归类完成: ChemicalCompliance / SimpleChat
  ↓
[3/6] 上下文加载完成
  ↓
[4/6] 业务执行完成 (Xms)
  ↓
[安全检测] 输出高危断言检测
  ↓
[5/6] 会话保存完成
  ↓
[6/6] 结果输出完成
  ↓
[Pipeline] 完成 | TraceId=... | 总耗时=... | 意图=... | 工具调用=...
```

**验证方法**：选取一条 `[Pipeline] 开始` 日志，顺次检查后续步骤是否全部出现，确认无步骤遗漏。

### 4.2 架构收敛专项验证 (7 项检查)

参考 [架构收敛专项测试说明](./架构收敛专项测试说明.md)，测试套件检查 7 个验证点：

| # | 验证点 | 如何从日志确认 | 通过标志 |
|---|--------|---------------|:---:|
| 1 | 文件验证 | 确认旧文件已删除、新文件存在 | 7/7 PASS |
| 2 | IntentRouter 纯归类 | `[IntentRouter] 关键词 "XX" 命中 → ChemicalCompliance` | 只返回意图枚举 |
| 3 | 统一调度 | `moduleFactory.Create(type)` 日志 | 7 个 ModuleType 全覆盖 |
| 4 | 6 步流水线 | `[Pipeline] 开始` → `[1/6]` → ... → `[6/6]` → `完成` | 每步有标记 |
| 5 | 统一基础设施 | `AgentDialog注入: ISessionService, IMemoryService, ILlmService, IToolService` | 4 项服务全注入 |
| 6 | 无硬编码 | 搜索日志中的固定回复 vs LLM 动态生成 | 无硬编码 Prompt |
| 7 | 四大范式集成 | 每个模块 `实现IInferenceModule` + `使用统一服务注入` | 全部 PASS |

**实战案例**：如果测试输出显示：

```
[PASS] CoT推理(标准输出) 实现IInferenceModule
[PASS] CoT推理(标准输出) 使用统一服务注入
[FAIL] CoT推理(标准输出) 属于统一ModuleType体系   ← 失败！
```

说明 `CoTSolidModule` 虽然实现了接口，但 `ModuleType` 枚举中可能遗漏了它，或者 `ModuleFactory` 中没有对应的创建分支。

### 4.3 评测集业务验证 (Part C)

从 Task 11 测试的 `C-eval-full.log` 中，每条用例的日志格式：

```
[C001] 查询: 丙酮属于危险化学品吗？哪一类？
  工具触发: CheckHazardCategory ✅
  参数: substanceName=丙酮 ✅
  结论: 丙酮属于易燃液体，类别2... ⚠️ 幻觉
  忠实度: 1/3 声明可验证
```

**逐条解读**：

| 字段 | 含义 | 判断标准 |
|------|------|:---:|
| `工具触发` | LLM 是否选择了正确的工具 | ✅/❌ |
| `参数` | 传递给工具的参数是否正确 | ✅/❌ |
| `结论` | 最终回答是否准确 | ✅/⚠️/❌ |
| `忠实度` | 结论中有多少声明可追溯到工具数据 | 越高越好 |

**典型异常模式诊断**：

```
模式 A: 工具触发=❌ → 问题在工具调度 Prompt 或意图路由
模式 B: 工具触发=✅, 参数=❌ → 问题在参数提取逻辑
模式 C: 工具触发=✅, 参数=✅, 结论=❌ → 问题在结论生成(幻觉/忠实度)
模式 D: 忠实度=0/3, 结论=✅ → 结论碰巧正确但不可信
```

### 4.4 RAG 召回率验证 (Part D)

从 `D-rag-recall-report.txt`：

```
查询: GB 30000.7
  Precision@5:  100%    ← Top-5 全部相关
  Precision@10: 90%     ← Top-10 中 9/10 相关
  Recall@5:     250%    ← 覆盖了 2.5 倍的相关文档
  Recall@10:    450%
  MRR:          1.000   ← 第一个相关文档排在第 1 位
```

**判断标准**：

| 指标 | 优秀 | 合格 | 需改进 |
|------|:---:|:---:|:---:|
| P@5 | ≥80% | ≥50% | <50% |
| R@5 | ≥100% | ≥60% | <60% |
| MRR | ≥0.8 | ≥0.5 | <0.5 |

**零召回排查**（如 `硫酸储存禁忌` P@5=0%）：

1. 检查知识库中是否有相关文档
2. 检查向量模型是否正确加载（维度=768）
3. 检查混合检索权重配置（BM25 vs Vector）
4. 检查查询预处理是否过度（如错误分词"硫酸储存"）

---

## 五、通过日志追踪代码业务逻辑

### 5.1 Pipeline 日志 ↔ 代码映射

日志中的每一条都对应代码中的具体位置。以 `agent1-20260623.log` 第 7-9 行映射到 [AgentDialog.cs](../../Agent1/Services/Dialog/AgentDialog.cs)：

```
日志: [Pipeline] 开始 | TraceId=ce1bc382 | 输入长度=10 | 输入=苯和丙酮能一起储存吗
      ↓ 对应代码
代码: AgentDialog.cs:75-76
      Serilog.Log.Information("[Pipeline] 开始 | TraceId={TraceId} | 输入长度={Len} | 输入={Input}", ...)

日志: [IntentRouter] 关键词 "储存" 命中 → ChemicalCompliance
      ↓ 对应代码
代码: IntentRouter.cs:66-67
      Serilog.Log.Information("[IntentRouter] 关键词 \"{Keyword}\" 命中 → ChemicalCompliance | 输入: {Input}", ...)

日志: [Pipeline] 完成 | TraceId=ce1bc382 | 总耗时=9508ms | 意图=ChemicalCompliance | 工具调用=0 | ...
      ↓ 对应代码
代码: AgentDialog.cs:167-174
      Serilog.Log.Information("[Pipeline] 完成 | TraceId={TraceId} | 总耗时={TotalMs}ms | ...", ...)
```

### 5.2 业务逻辑追踪法

**场景**：你想理解"用户输入 → 合规审核 → 返回结果"的完整流程。

**方法**：

1. **起手式**：在新日志中找到一条 `[Pipeline] 开始` 记录
2. **顺藤摸瓜**：提取 `TraceId`，`grep` 该 TraceId 的所有日志行
3. **对照代码**：每看到一个新的 `[模块名]` 标签，去对应的 `.cs` 文件找到 `Serilog.Log` 调用
4. **画时序图**：

```
用户输入: "苯和丙酮能一起储存吗"
  │
  ├─ [0ms] AgentDialog.ExecuteAsync()           ← Pipeline 开始
  │   ├─ PreprocessAsync()                       ← [1/6] 预处理
  │   ├─ SafetyGuardService.ValidateInput()      ← 安全注入检测
  │   ├─ IntentRouter.Route()                    ← [2/6] 意图归类
  │   │   └─ 匹配关键词 "储存" → ChemicalCompliance
  │   ├─ LoadContextAsync()                      ← [3/6] 上下文加载
  │   │   ├─ SessionService.GetFormattedHistory()
  │   │   └─ MemoryCoordinator.RetrieveAsync()   ← RAG 检索
  │   ├─ ExecuteBusinessWithResultAsync()        ← [4/6] 业务执行
  │   │   ├─ ModuleDispatcher.Dispatch(intent)
  │   │   ├─ ComplianceCheckModule.Execute()
  │   │   │   ├─ ToolService.ExecuteAsync()      ← 工具调用
  │   │   │   └─ LlmService.InvokeStreamAsync()  ← LLM 推理
  │   │   └─ ReflectionModule.Verify()           ← 反思验证
  │   ├─ SafetyGuardService.ValidateOutput()     ← 输出安全检测
  │   ├─ SaveSessionAsync()                      ← [5/6] 会话保存
  │   └─ FormatOutput()                          ← [6/6] 格式化
  │
  └─ [9508ms] 返回 CliExecutionResult           ← Pipeline 完成
```

### 5.3 定位瓶颈：耗时分析

从 `[Pipeline] 完成` 日志中的耗时分解字段：

```
路由=84ms | 上下文=2ms | 执行=9400ms | 输入安全=10ms | 输出安全=0ms
```

```
总耗时 9508ms
├─ 路由: 84ms (0.9%)      ← 很快，不是瓶颈
├─ 上下文: 2ms (0.02%)    ← 非常快
├─ 执行: 9400ms (98.9%)   ← 🔴 绝对瓶颈！需要深入分析
├─ 输入安全: 10ms (0.1%)  ← 正常
└─ 输出安全: 0ms          ← 正常
```

**深入分析"执行"阶段**：9400ms 花在哪里？

- 如果 `工具调用=N`，则 LLM 推理时间 ≈ 执行时间
- 在 GPU 模式下，单次推理通常 3-5 秒
- 9400ms 约 2 次 LLM 调用（规划 + 执行 + 反思），属于正常范围
- 如果执行 > 30 秒，需检查：
  - 是否触发了多次工具调用（ReAct 循环）
  - LLM 服务是否有排队
  - Prompt 是否过长

---

## 六、错误模式速查表

基于 [故障排查文档](../_archive/troubleshooting/故障排查文档.md) 的历史经验，17 个已知错误按根因分为四类：

### 类别 1：Prompt 设计缺陷 (7 个)

| 错误# | 日志特征 | 根因 | 修复方向 |
|-------|---------|------|---------|
| #1 | `NeedsTools=false` 但实际需要工具 | 假设 LLM 输出"是/否" | 容错解析 |
| #4 | ToC/反思日志缺失 | 简化时误删 | 恢复功能 |
| #6 | `CS1002: 应输入;` | 引号嵌套冲突 | 双引号转义 |
| #8 | 结论输出 JSON 格式 | 未禁止非自然语言 | Prompt 加约束 |
| #12 | `工具列表: 空` → 调用工具"空" | 未过滤无效值 | 扩展过滤词 |
| #13 | AI 自称用户的名字 | 角色边界模糊 | Prompt 明确区分 |
| #14 | 同类问题模式判断不一致 | 分类规则粗糙 | 精细化分类 |

### 类别 2：模型特性不兼容 (3 个)

| 错误# | 日志特征 | 根因 | 修复方向 |
|-------|---------|------|---------|
| #7 | 输出包含 `<think>` 标签 | DeepSeek-R1 特有 | 流式过滤 |
| #9 | `was canceled` | 超时/取消 | 重试机制 |
| #15 | 每次返回相同自我介绍 | Chat Completion 格式不兼容 | 改用 IChatCompletionService |

### 类别 3：代码逻辑缺陷 (4 个)

| 错误# | 日志特征 | 根因 | 修复方向 |
|-------|---------|------|---------|
| #2 | `未知工具: CurentTime` | 拼写错误+严格匹配 | 模糊匹配 |
| #5 | 单环节崩溃→整体失败 | 异常未捕获 | 逐层 try-catch |
| #11 | `未知工具: Calculate` | 参数提取不完善 | 括号匹配 |
| #17 | 专业问题走快速模式 | 长度检查门槛低 | 领域关键词优先 |

### 类别 4：架构断层 (3 个)

| 错误# | 日志特征 | 根因 | 修复方向 |
|-------|---------|------|---------|
| #10 | 重复调用工具查已缓存数据 | 无结构化记忆 | 新增记忆检索 |
| #16 | 硬编码 `new Kernel()` | 脱离 DI 架构 | DI 注入 |
| #17 | 历史保存含 `<think>` 残渣 | 未清理就保存 | 保存前清理 |

---

## 七、常用排障命令

### Linux 服务器命令

```bash
# 查看最近的错误
grep -E "ERR|FATAL|❌" /root/autodl-tmp/agent-system/logs/agent1-*.log | tail -20

# 提取某个 TraceId 的完整链路
grep "ce1bc382" /root/autodl-tmp/agent-system/logs/agent1-*.log

# 统计 Pipeline 完成耗时分布
grep "\[Pipeline\] 完成" logs/agent1-*.log | grep -oP '总耗时=\K\d+'

# 查看 API 日志
tail -f /root/autodl-tmp/logs/agent1-api.log

# 服务健康检查
curl -s http://localhost:8080/health      # LLM
curl -s http://localhost:8081/health      # Embedding
curl -s http://localhost:5000/health      # API
```

### 评测结果分析命令

```bash
# 提取评测关键指标
grep -E "工具触发率|参数准确率|结论准确率|综合评级" test-results/*/C-eval-metrics.txt

# 统计各类目通过率
grep "✅\|⚠️\|❌" test-results/*/C-eval-full.log | sort | uniq -c

# 查看失败用例
grep "❌\|FAIL" test-results/*/C-eval-full.log
```

---

## 八、总结：日志阅读心法

1. **先看级别再看内容**：`ERR`/`FATAL` 优先处理，`WRN` 判断是否误报，`INF` 用于追踪
2. **TraceId 是金线**：一个 TraceId 贯穿一次完整请求，grep 它就能看到全貌
3. **对照代码**：日志里的 `[模块名]` 对应源码中的 `Serilog.Log` 调用，找得到出处
4. **符号即状态**：`✅⚠️❌🚫⏭️` 是快速判断的信号灯
5. **数据说话**：不要猜，用 `grep -c` 统计频率，用 `awk` 计算分布
6. **先看最后再看最前**：排查故障时，先看崩溃前最后几行，再回溯到开头

---

> **关联文档**：
> - [Task11 集成测试部署指南](./integration/Task11-集成测试部署指南.md) — 如何运行测试
> - [故障排查文档](../_archive/troubleshooting/故障排查文档.md) — 17 个已知错误的完整修复记录
> - [架构收敛专项测试说明](./架构收敛专项测试说明.md) — 7 项架构验证点
