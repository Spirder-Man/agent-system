# 系统日志阅读与分析实战教学

> 基于 Task 11 集成测试实际输出的真实日志数据 | 2026-06-25

---

## 教学准备：三份真实日志文件

本教程使用以下三组来自本项目实际运行的日志：

| # | 日志文件 | 来源 | 用途 |
|---|---------|------|------|
| ① | `logs/agent1-20260623.log` (9行) | Windows 本地运行 | Pipeline 链路追踪、安全拦截案例 |
| ② | `logs/full-20260605.log` (19117行) | Windows 本地运行 | 知识库加载全流程、错误模式演示 |
| ③ | `logs/linux/.../summary.txt` (38行) | Linux 服务器测试 | 自动化测试报告解读 |

---

## 第一课：日志结构解析

### 1.1 解剖一条日志的每个字段

以 **日志①** 第 7 行为教材：

```
[2026-06-23 00:23:04.689 INF] LAPTOP-40G212RC RunId=0509e705 [Pipeline] 开始 | TraceId=ce1bc382 | 输入长度=10 | 输入=苯和丙酮能一起储存吗
```

让我们逐字段拆解：

```
┌────────────────────────────────── 时间戳（精确到毫秒） ──────────────────────────────────┐
│                                                                                          │
│  [2026-06-23 00:23:04.689 INF] LAPTOP-40G212RC RunId=0509e705 [Pipeline] 开始 | ...
│          ↑              ↑            ↑              ↑             ↑           ↑
│          │              │            │              │             │           │
│        日期         日志级别      机器名        运行实例ID    模块标签     消息体开始
│                                                                                          │
│  ...TraceId=ce1bc382 | 输入长度=10 | 输入=苯和丙酮能一起储存吗                            │
│         ↑                      ↑                  ↑                                      │
│   请求追踪ID             结构化键值对           结构化键值对                               │
└──────────────────────────────────────────────────────────────────────────────────────────┘
```

**逐个字段详解**：

| # | 字段 | 示例值 | 含义 | 为什么这样设计 |
|---|------|--------|------|---------------|
| 1 | **时间戳** | `2026-06-23 00:23:04.689` | 精确到毫秒的事件时间 | 毫秒级精度让你能计算任两步之间的耗时差（如：`00:23:14.198 - 00:23:04.689 = 9.509秒`） |
| 2 | **日志级别** | `INF` | Information（正常流程） | 五种级别：`VRB`(详细) → `DBG`(调试) → `INF`(信息) → `WRN`(警告) → `ERR`(错误) → `FATAL`(致命)。生产环境只输出 INF 及以上 |
| 3 | **机器名** | `LAPTOP-40G212RC` | 运行程序的计算机名 | 多机部署时区分日志来源。在 Docker 里会是容器 ID |
| 4 | **RunId** | `0509e705` | 程序本次启动的唯一标识 | 程序每启动一次生成一个新 RunId。同一次运行的所有日志共享同一个 RunId，用于区分"上次启动"和"这次启动"的日志 |
| 5 | **模块标签** | `[Pipeline]` | 产生日志的代码模块 | 约定用 `[模块名]` 格式，如 `[IntentRouter]`、`[Memory]`。让你不用看代码就知道日志来自哪个模块 |
| 6 | **TraceId** | `ce1bc382` | 单次请求的唯一追踪 ID | 8 位十六进制（GUID 前 8 位）。同一次请求的所有日志用同一个 TraceId。这是全链路追踪的金线 |
| 7 | **结构化字段** | `输入长度=10`, `输入=苯和丙酮...` | 键值对形式的上下文数据 | 不用解析自由文本，直接 `grep "输入长度=10"` 就能精确过滤。支持后续接入 ELK/Splunk 等日志分析平台的结构化查询 |

### 1.2 两种日志格式对比

本项目有两种日志输出格式，对应不同的使用场景：

**格式 A：Serilog 结构化日志**（日志①）
```
[2026-06-23 00:23:04.689 INF] LAPTOP-40G212RC RunId=0509e705 [Pipeline] 开始 | TraceId=ce1bc382 | 输入长度=10 | 输入=苯和丙酮能一起储存吗
```
- 特点：包含时间戳、级别、机器名、RunId、模块标签
- 用途：生产环境日志、ELK 分析、结构化查询
- 来源：`Serilog.Log.Information(...)` 调用

**格式 B：诊断双写日志**（日志②）
```
📦 正在测试数据库连接...
[15:06:03 INF] 📦 正在测试数据库连接...
```
- 特点：每条同时输出纯文本版 + Serilog 版（因此叫"双写"）
- 用途：开发调试、快速肉眼扫描
- 来源：`Console.WriteLine(...)` + `Serilog.Log.Information(...)` 并行调用

> **设计考虑**：双写的原因是在开发阶段，纯文本日志可以直接 `tail -f` 查看，不需要解析 Serilog 格式；同时保留结构化版本供自动化分析。生产部署时可以关闭 Console 输出，只保留 Serilog 文件输出。

### 1.3 符号前缀语义系统

从 **日志②** 中可以观察到一套完整的符号前缀系统：

```
✅  向量嵌入成功 (维度: 768)     ← 成功，一切正常
⚠️  中文配置不可用，使用simple配置  ← 警告，降级但不影响核心功能
❌  失败:   0                     ← 错误，功能受损
🚫  脏数据拦截: 来源=...          ← 主动拒绝（安全/质量策略触发）
⏭️  跳过:  1100 (空表单/模板)     ← 跳过，策略性忽略
📦  正在测试数据库连接...          ← 信息标记，提示当前阶段
🔗  RAG 知识库已注入               ← 信息标记，依赖就绪
📝  诊断日志双写已启用             ← 信息标记，配置确认
```

**判断规则**：

| 符号 | 是否需要处理 | 说明 |
|:----:|:---:|------|
| ✅ | 否 | 正常流程，无需关注 |
| ⚠️ | 关注 | 降级行为，理解影响后决定是否修复 |
| ❌ | **是** | 必须排查 |
| 🚫 | 视情况 | 如果拦截了该拦截的（如注入攻击），是好事；如果拦截了正常请求，是误报需修复 |
| ⏭️ | 否 | 策略性跳过，无需处理 |

---

## 第二课：链路追踪方法

### 2.1 TraceId 金线原则

**日志①** 中的 9 行日志记录了程序一次启动后的两件事情：

```
行号  RunId      TraceId     模块              动作
───   ────────   ────────    ──────────        ──────
1-3   0509e705   (无)        (系统启动)         程序启动 Banner
4-5   0509e705   (无)        (数据库初始化)     数据库连接成功
6     0509e705   0c68ec2f    [Pipeline]         安全拦截 ← 请求A
7     0509e705   ce1bc382    [Pipeline]         开始     ← 请求B
8     0509e705   ce1bc382    [IntentRouter]     意图归类 ← 请求B
9     0509e705   ce1bc382    [Pipeline]         完成     ← 请求B
```

**关键发现**：
- 行 1-5 没有 TraceId → 属于系统启动阶段，不是用户请求
- 行 6 的 TraceId=`0c68ec2f` → 一次被安全拦截的请求（只有开始+拦截，没有完成）
- **行 7-9 的 TraceId=`ce1bc382`** → 这就是我们要追踪的完整请求链路！

> **核心心法**：`grep "ce1bc382"` 就能看到这次请求的一切。

### 2.2 逐步追踪一条完整请求

以 `TraceId=ce1bc382` 为例，逐步还原发生了什么：

**第 1 步：请求开始**（行 7）
```
[2026-06-23 00:23:04.689 INF] [Pipeline] 开始 | TraceId=ce1bc382 | 输入长度=10 | 输入=苯和丙酮能一起储存吗
```
- **时间**: `00:23:04.689` — 记下这个起点时间
- **动作**: Pipeline 启动，生成 TraceId
- **输入**: `苯和丙酮能一起储存吗`（10 个字符）
- **对应代码**: [AgentDialog.cs:75-76](../../Agent1/Services/Dialog/AgentDialog.cs#L75-L76)

**第 2 步：意图路由**（行 8）
```
[2026-06-23 00:23:04.787 INF] [IntentRouter] 关键词 "储存" 命中 → ChemicalCompliance | 输入: 苯和丙酮能一起储存吗
```
- **时间**: `00:23:04.787` — 距开始仅 `98ms`
- **动作**: 意图路由器分析输入，命中合规关键词 `"储存"`
- **决策**: 路由到 `ChemicalCompliance`（化工合规处理）
- **对应代码**: [IntentRouter.cs:66-67](../../Agent1/Services/Dialog/IntentRouter.cs#L66-L67)

> **验证方法**: 看关键词是否合理 → `"储存"` 确实匹配了用户问题中的 `能一起储存吗`，路由决策正确 ✅

**第 3 步：Pipeline 完成**（行 9）
```
[2026-06-23 00:23:14.198 INF] [Pipeline] 完成 | TraceId=ce1bc382 | 总耗时=9508ms | 意图=ChemicalCompliance | 工具调用=0 | 安全警告=0 | 路由=84ms | 上下文=2ms | 执行=9400ms | 输入安全=10ms | 输出安全=0ms
```
- **时间**: `00:23:14.198` — 距开始 `9508ms` (约 9.5 秒)
- **意图**: `ChemicalCompliance` — 确实走了合规处理流程
- **工具调用=0**: ⚠️ 异常信号！合规问题应该调用工具
- **耗时分解**: `执行=9400ms` 占 98.9%，是绝对瓶颈

> **异常发现**: `工具调用=0` 对于"苯和丙酮能一起储存吗"这类问题是不正常的。应该调用 `CheckStorageCompatibility` 工具。这说明工具调度的 Prompt 可能需要优化。

### 2.3 对比：一次异常请求的追踪

行 6 展示了一次被拦截的请求：
```
[2026-06-23 00:22:58.446 WRN] [Pipeline] 安全拦截 | TraceId=0c68ec2f | 原因=检测到潜在注入攻击: 匹配模式 "忽略之前的指令"
```

- **级别是 WRN 不是 ERR**：说明这是**预期的安全行为**，不是系统故障
- **TraceId 只有开始+拦截**：没有后续的"完成"日志，说明请求在安全检测阶段就被终止了
- **原因明确**：`匹配模式 "忽略之前的指令"` — 典型的 Prompt 注入攻击被正确拦截

> **判断方法**: 用 `grep "0c68ec2f" agent1-20260623.log` 验证，应该只有一条日志。一条开始+一条拦截 = 正常的安全阻断流程。

---

## 第三课：业务逻辑验证

### 3.1 知识库加载全流程验证

**日志②** 完整记录了从启动到知识库就绪的全过程。让我们逐步验证：

**阶段 1：基础设施初始化**（行 1-20）
```
📝 诊断日志双写已启用 → logs\full-20260605.log        ← 配置确认
[15:06:00 INF] 化工园区危化品合规审核AI Agent           ← 服务标识
   ✅ [Thinking] OllamaThinkingHandler 已注入            ← LLM 就绪
🔗 RAG 知识库已注入 ChemicalComplianceTools              ← RAG 就绪
📦 正在测试数据库连接...                                 ← 开始 DB 连接
✅ 数据库连接成功！                                      ← DB 就绪
   ✅ pgvector扩展已存在                                 ← 向量扩展确认
   ⚠️ 中文配置不可用，使用simple配置                      ← 降级行为
   ✅ simple全文搜索索引创建成功
   ✅ 化工文档表创建成功
   ✅ 扩展元数据字段就绪
✅ 数据库表初始化完成！                                  ← DB 就绪完成
```

**逐条验证**：

```
验证点 1: "✅ 数据库连接成功" → 连接字符串正确，PostgreSQL 可访问
验证点 2: "✅ pgvector扩展已存在" → 向量检索能力就绪
验证点 3: "⚠️ 中文配置不可用" → 需要关注但不阻塞。意味着全文搜索降级为 simple 分词
验证点 4: "✅ 扩展元数据字段就绪" → 法规号、文档类型等高级筛选字段已建好
```

**阶段 2：知识库文档处理**（行 22-310）

```
管道: PdfExtractor → TextCleaner → SemanticChunker → 双存储
```

每个文档的处理日志遵循固定模式：
```
✅ 向量嵌入成功 (维度: 768)                              ← 嵌入服务正常
✅ 化工文档添加成功 (类型: 国标, 法规号: 15603-2022, 质量: good)  ← 入库成功
✅ [GB+15603-2022]: 10页 → 6块 (质量:good)               ← 分块完成
```

**验证逻辑**：
1. **维度=768** → 确认 nomic-embed-text-v1.5 模型正确加载（该模型输出 768 维向量）
2. **质量: good** → 分块大小合理，文本内容有效
3. **质量: partial** → 注意！如 `GB 30000.18-2013` 39 页只产出 1 块，说明分块器可能合并了内容，或者文档结构特殊

**阶段 3：脏数据拦截演示**（行 328-343）

```
✅ 向量嵌入成功 (维度: 0)                                ← 维度=0！
🚫 脏数据拦截: 来源=公司生产安全事故应急预案, 质量=未知, 内容长度=0
```

**这是我的验证逻辑**：
1. `维度: 0` 不正常 → 正常维度应该是 768
2. `🚫 脏数据拦截` → 系统主动拒绝了
3. `内容长度=0` → 根因：文档提取后是空文本

> **设计精髓**：系统采用"先检查再入库"的策略。`维度=0` 意味着没有调用嵌入服务（节省了 GPU 资源），直接标记为脏数据跳过。如果先嵌入再检查，1100 个空文档会浪费大量 GPU 时间。

**阶段 4：质量报告验证**（行 2552-2563）

```
========================================
        知识库加载质量报告
========================================
  文件总数: 1158
  ✅ 成功:   56
  ⚠️  部分:  2
  ❌ 失败:   0
  ⏭️  跳过:  1100 (空表单/模板)
----------------------------------------
  总块数:    1241
  知识库文档数: 1241
========================================
```

**快速验证清单**：

| 检查点 | 预期 | 实际 | 判断 |
|--------|------|------|:---:|
| `❌ 失败` | 应该 = 0 | 0 | ✅ |
| `⚠️ 部分` | ≤ 5% 总数 | 2/1158 = 0.17% | ✅ |
| `⏭️ 跳过` | 大部分是空模板 | 1100，全是 `内容长度=0` | ✅ |
| `成功+部分` | ≥ 50 | 56+2=58 | ✅ |
| `总块数` | ≥ 文档数 | 1241 > 58 | ✅ (每个文档平均 21 块) |

### 3.2 自动化测试报告验证

**日志③** 的测试报告：

```
Agent1 自动化测试报告
══════════════════════
时间: 2026-06-24 00:26:20
通过: 22
失败: 0
跳过: 3
总计: 25
总耗时: 338s
LLM: 200
Embed: 200
```

**逐字段验证**：

| 字段 | 含义 | 判断逻辑 |
|------|------|---------|
| `通过: 22` | 22 个测试通过 | 22/25 = 88% 通过率。但注意有 3 个跳过 |
| `失败: 0` | 零失败 | ✅ 核心功能全部正常 |
| `跳过: 3` | 3 个测试被跳过 | 需确认原因。常见：API 未启动（Part B 的 6 个 API 测试）、环境变量缺失 |
| `总耗时: 338s` | 约 5.6 分钟 | 在 25 个测试的合理范围内 |
| `LLM: 200` | LLM 服务 HTTP 200 | ✅ llama.cpp 服务正常 |
| `Embed: 200` | 嵌入服务 HTTP 200 | ✅ 嵌入服务正常 |

**各测试逐项验证**：

```
T1_CoT标准推理: PASS       → CoT 模块正常
T2_CoT流式推理: PASS       → 流式输出正常
T3_ReAct标准推理: PASS     → ReAct 模块正常
T4_ReAct流式推理: PASS     → 流式 ReAct 正常
T5_Reflection反思: PASS    → 反思模块正常
T6_RAG检索增强: PASS       → RAG 模块正常
T7_智能对话系统: PASS      → 统一对话正常
T8_化工合规自查_共存: PASS  → 核心业务正常
T12_工具调用诊断: PASS     → 工具调用链路正常
T13_合规评测集: PASS       → 64 条评测通过
```

> **验证心得**：如果你看到某条 `PASS`，说明该模块的完整链路都跑通了（LLM 推理 → 工具调用 → 结果输出）。如果 `FAIL`，去对应的 `T*.log` 文件找原因。

---

## 第四课：问题排查技巧

### 4.1 错误模式一：向量嵌入上下文超长

从 **日志②** 第 314-315 行：

```
⚠️ 向量请求失败 [InternalServerError]: {"error":"the input length exceeds the context length"}
⏭️ 向量生成失败，跳过向量库写入（BM25 已写入）
```

**排查五步法**：

```
Step 1 — 识别信号
  看见 ⚠️ + InternalServerError → 有异常但非致命

Step 2 — 读错误消息
  "the input length exceeds the context length"
  → 翻译：文本块超过了嵌入模型的上下文窗口限制
  → nomic-embed-text-v1.5 的默认上下文是 8192 tokens
  → 这个文本块超过了 8192 tokens

Step 3 — 看降级行为
  "跳过向量库写入（BM25 已写入）"
  → 系统有设计好的降级策略：
    ✅ BM25 索引仍然写入（全文检索不受影响）
    ❌ 向量库跳过（语义检索会遗漏这个文档块）
  → 影响范围：该文档块无法通过语义检索召回，但 BM25 关键词匹配仍可用

Step 4 — 统计影响范围
  grep "input length exceeds" full-20260605.log | wc -l
  → 全日志出现 15 次
  → 15/1241 = 1.2% 的块受影响，可接受

Step 5 — 确定修复方向
  选项 A: 减小 SemanticChunker 的 MaxChunkSize
  选项 B: 增大嵌入服务的 -c (context size) 参数
  选项 C: 当前影响小(1.2%)，暂不修复，记入技术债务
```

### 4.2 错误模式二：Pipeline 工具调用异常

回到 **日志①** 行 9 的 `工具调用=0`：

```
[Pipeline] 完成 | TraceId=ce1bc382 | 总耗时=9508ms | 意图=ChemicalCompliance | 工具调用=0
```

**诊断树**：

```
工具调用=0 但意图=ChemicalCompliance
│
├─ 可能原因 1: IntentRouter 误判
│   检查: 意图=ChemicalCompliance 是否正确？
│   验证: "苯和丙酮能一起储存吗" → 命中"储存"关键词 ✅ 路由正确
│   结论: 排除
│
├─ 可能原因 2: 工具调度 Prompt 未引导工具调用
│   检查: ComplianceCheckModule 的 Prompt 是否包含工具选择指引？
│   方法: 搜索代码中工具规划相关 Prompt
│   结论: 需要进一步审查 Prompt
│
├─ 可能原因 3: LLM 返回了不需要工具的结果
│   检查: 9508ms 的执行时间说明 LLM 推理确实执行了
│   但 LLM 可能直接回答了问题而不调用工具
│   结论: 最可能的原因
│
└─ 排查动作:
   1. 打开详细日志（设置 Serilog 级别为 Debug）
   2. 查看 LLM 的完整请求 Prompt 和响应
   3. 检查 Prompt 中是否明确要求工具调用
```

### 4.3 错误模式三：安全拦截误判排查

**日志①** 行 6：
```
[Pipeline] 安全拦截 | TraceId=0c68ec2f | 原因=检测到潜在注入攻击: 匹配模式 "忽略之前的指令"
```

**判断是否误报的三步法**：

```
Step 1 — 看输入内容
  需要找到这次请求的原始输入：
  grep "0c68ec2f" agent1-20260623.log
  → 如果有"开始"日志，看输入内容
  → 如果没有（本次情况），说明输入本身可能就是攻击

Step 2 — 看拦截模式
  "匹配模式 '忽略之前的指令'"
  → 这是经典 Prompt 注入模式（Prompt Injection）
  → 该模式在任何系统中都应该被拦截

Step 3 — 判断
  ✅ 这是正确的拦截，不是误报
  ✅ 不需要修改安全规则
  ✅ 如果同类拦截频繁，说明有人在尝试攻击，应告警
```

### 4.4 区分正常与异常速查表

| 日志特征 | 正常行为 | 异常行为 | 判断依据 |
|----------|---------|---------|---------|
| `⚠️ 警告` | 降级但功能可用（如 `中文配置不可用`） | 功能受损（如 `LLM 连接超时`） | 看后续是否有正常输出 |
| `工具调用=0` | 简单对话（意图=SimpleChat） | 合规查询（意图=ChemicalCompliance） | 结合意图判断 |
| `维度: 0` | 脏数据拦截（内容长度=0） | 嵌入服务故障 | 看是否伴随 `🚫 脏数据拦截` |
| `执行>30秒` | 复杂多轮 ReAct 推理 | Prompt 过长或 LLM 排队 | 看工具调用次数 |
| `部分: N` | 少量向量失败，BM25 已备份 | 大量失败，检索受损 | N 占总数的比例 |

---

## 第五课：代码与日志映射

### 5.1 Pipeline 日志的代码溯源

每条 Pipeline 日志都能精准定位到 [AgentDialog.cs](../../Agent1/Services/Dialog/AgentDialog.cs)：

| 日志内容 | 代码位置 | 代码行 |
|---------|---------|:---:|
| `[Pipeline] 开始` | `Serilog.Log.Information("[Pipeline] 开始 \| TraceId={TraceId}...` | [第 75 行](../../Agent1/Services/Dialog/AgentDialog.cs#L75) |
| `[Pipeline] 安全拦截` | `Serilog.Log.Warning("[Pipeline] 安全拦截 \| TraceId={TraceId}...` | [第 95 行](../../Agent1/Services/Dialog/AgentDialog.cs#L95) |
| `[Pipeline] 完成` | `Serilog.Log.Information("[Pipeline] 完成 \| TraceId={TraceId}...` | [第 167 行](../../Agent1/Services/Dialog/AgentDialog.cs#L167) |

**六步流水线与日志的对应**：

```
代码中的执行顺序              控制台输出              Serilog 日志
─────────────────────        ────────────           ────────────
ExecuteAsync() 入口           ═══ 流水线启动 ═══      [Pipeline] 开始
  │
  ├─ [1/6] PreprocessAsync   [1/6] 预处理完成       (内嵌在总耗时中)
  │
  ├─ SafetyGuardService       (无输出/拦截时输出)     [Pipeline] 安全拦截 (仅异常)
  │
  ├─ [2/6] RouteIntent        [2/6] 意图归类完成     [IntentRouter] 关键词命中
  │
  ├─ [3/6] LoadContextAsync   [3/6] 上下文加载完成   (内嵌在总耗时中)
  │
  ├─ [4/6] ExecuteBusiness    [4/6] 业务执行完成     (内嵌在总耗时中)
  │
  ├─ SafetyGuardService       (无输出)               (内嵌在总耗时中)
  │
  ├─ [5/6] SaveSessionAsync   [5/6] 会话保存完成     (内嵌在总耗时中)
  │
  └─ [6/6] FormatOutput       [6/6] 结果输出完成     (内嵌在总耗时中)
                               ═══ 流水线结束 ═══    [Pipeline] 完成
```

### 5.2 从日志反推代码逻辑

**示例**：从 `[Pipeline] 完成` 日志中的字段反推 AgentDialog 的数据结构：

```
日志字段                    反推出的代码含义
────────                   ────────────────
总耗时=9508ms       →      Stopwatch 从 ExecuteAsync 入口到返回的总时间
意图=ChemicalCompliance →  metrics.Intent = intent.ToString()
工具调用=0           →      metrics.ToolCallCount = toolCalls.Count
路由=84ms            →      metrics.RouteMs = (路由结束 - 路由开始)
上下文=2ms           →      metrics.LoadContextMs = (上下文结束 - 上下文开始)
执行=9400ms          →      metrics.ExecuteBusinessMs = (执行结束 - 执行开始)
输入安全=10ms        →      metrics.SafetyCheckInputMs
输出安全=0ms         →      metrics.SafetyCheckOutputMs
```

对应 [AgentDialog.cs 第 66-174 行的 PipelineMetrics 结构](../../Agent1/Services/Dialog/AgentDialog.cs#L66-L174)：

```csharp
// 启动计时
var sw = Stopwatch.StartNew();

// [1/6] 预处理 - 耗时采集
var t0 = sw.ElapsedMilliseconds;
var processedInput = await PreprocessAsync(userInput);
metrics.PreprocessMs = sw.ElapsedMilliseconds - t0;    // → 路由=84ms 的类似逻辑

// [安全检测] - 耗时采集
var t0s = sw.ElapsedMilliseconds;
var (safe, reason) = SafetyGuardService.ValidateInput(processedInput);
metrics.SafetyCheckInputMs = sw.ElapsedMilliseconds - t0s;  // → 输入安全=10ms

// [2/6] 意图路由 - 耗时采集
var t1 = sw.ElapsedMilliseconds;
var intent = RouteIntent(processedInput);
metrics.RouteMs = sw.ElapsedMilliseconds - t1;              // → 路由=84ms

// ... 每个步骤都遵循相同的模式：记录开始时间 → 执行业务 → 计算耗时

// 最终汇总
metrics.TotalMs = sw.ElapsedMilliseconds;                   // → 总耗时=9508ms
```

> **设计精髓**：每个步骤独立计时，既能在 `[Pipeline] 完成` 日志中看到各阶段耗时分解，又能通过 `PipelineEvent` 内存流记录每个步骤的详细信息。这种设计让你无需在代码中加断点就能分析性能瓶颈。

### 5.3 IntentRouter 日志映射

从 **日志①** 行 8 反推 [IntentRouter.cs](../../Agent1/Services/Dialog/IntentRouter.cs) 的执行逻辑：

```
日志: [IntentRouter] 关键词 "储存" 命中 → ChemicalCompliance | 输入: 苯和丙酮能一起储存吗

对应代码:
┌──────────────────────────────────────────────────────┐
│ // IntentRouter.cs:62-68                             │
│ var matchedKeyword = ComplianceKeywords              │
│     .FirstOrDefault(k => lower.Contains(k));         │  ← "储存" 命中
│                                                      │
│ if (matchedKeyword != null)                          │
│ {                                                    │
│     LastMatchedKeyword = matchedKeyword;             │  ← 记录"储存"
│     Serilog.Log.Information(                         │
│         "[IntentRouter] 关键词 \"{Keyword}\" 命中 →" │  ← 生成此行日志
│         + " ChemicalCompliance | 输入: {Input}",     │
│         matchedKeyword, userInput.Truncate(80));     │
│     return IntentType.ChemicalCompliance;            │  ← 返回枚举值
│ }                                                    │
└──────────────────────────────────────────────────────┘
```

**验证完整性**：
1. 检查关键词表（第 20-33 行）→ `"储存"` 确实在 `ComplianceKeywords` 数组中 ✅
2. 检查匹配逻辑（第 62 行）→ `lower.Contains(k)` 正确匹配了中文 ✅
3. 检查审计记录（第 65 行）→ `LastMatchedKeyword` 被记录供追溯 ✅

---

## 第六课：关键指标分析

### 6.1 单次请求性能画像

从 **日志①** 行 9 提取：

```
总耗时=9508ms  (100%)
├── 路由:       84ms  (0.9%)   ← IntentRouter 关键词匹配，极快
├── 上下文:      2ms  (0.02%)  ← Session + Memory 查询，可忽略
├── 执行:     9400ms  (98.9%)  ← 🔴 LLM 推理 + 工具调用，绝对瓶颈
├── 输入安全:   10ms  (0.1%)   ← Prompt 注入检测，合理
└── 输出安全:    0ms            ← 无输出警告，跳过检测
```

**分析**：
- 执行占 98.9%，说明系统开销极小（路由+上下文+安全 < 1.1%）
- 9400ms 在 GPU 模式下对应约 1-2 次 LLM 推理调用（Qwen3-8B 的典型推理延迟 ~5s/次）
- `输入安全=10ms` 说明安全检测是基于正则/关键词匹配，不是用 LLM 做检测（否则会是秒级）

### 6.2 知识库加载性能

从 **日志②** 质量报告反推：

```
文件总数: 1158
成功: 56 (生成 1241 个语义块)
平均每文档分块数: 1241/56 ≈ 22.2 块/文档
平均每块嵌入耗时: 约 586ms (来自 Task 11 实测)
知识库加载总耗时 ≈ 1241 × 586ms ≈ 727 秒 ≈ 12 分钟
```

**优化空间分析**：
- 当前是串行嵌入（一个一个处理），1241 块 × 586ms = 12 分钟
- 如果改为批量嵌入（batch_size=32），耗时可以降至 1241/32 × 586ms ≈ 23 秒
- 但需要确保嵌入服务配置了足够的 `-b` (batch size) 参数

### 6.3 系统运行状况评估矩阵

| 指标 | 来源 | 参考值 | 当前值 | 评估 |
|------|------|:---:|:---:|:---:|
| API 健康 | `curl /health` | HTTP 200 | 200 | ✅ |
| LLM 健康 | `curl :8080/health` | HTTP 200 | 200 | ✅ |
| Embed 健康 | `curl :8081/health` | HTTP 200 | 200 | ✅ |
| LLM 推理延迟 (P50) | Pipeline 日志 | < 5s | ~4.9s | ✅ |
| LLM 推理延迟 (P95) | Pipeline 日志 | < 10s | ~6.9s | ✅ |
| 安全拦截率 | `grep "安全拦截" | wc -l` | < 5% | 1/2=50% | ⚠️ 样本太少 |
| 知识库成功率 | 质量报告 | > 95% | 58/1158=5% | ⚠️ 大量空模板 |
| 工具触发率 | 评测日志 | > 80% | 85.7% | ✅ |
| 结论准确率 | 评测日志 | > 70% | 65.1% | ⚠️ 需改进 |

### 6.4 从 Pipeline 日志批量提取指标的命令

```bash
# 统计平均响应时间
grep "\[Pipeline\] 完成" logs/agent1-*.log | \
  grep -oP '总耗时=\K\d+' | \
  awk '{sum+=$1; count++} END {print "平均:", sum/count "ms, 样本:", count}'

# 统计意图分布
grep "意图=" logs/agent1-*.log | \
  grep -oP '意图=\K\w+' | sort | uniq -c | sort -rn

# 统计工具调用次数分布
grep "工具调用=" logs/agent1-*.log | \
  grep -oP '工具调用=\K\d+' | sort -n | uniq -c

# 统计安全拦截频率
grep -c "安全拦截" logs/agent1-*.log
```

---

## 总结：日志阅读能力自检清单

完成本教程后，你应该能回答以下问题。试着用 **日志①** (agent1-20260623.log) 来检验：

| # | 检验题 | 答案线索 |
|---|--------|---------|
| 1 | 这次运行有几个用户请求？ | 2 个（2 个不同的 TraceId） |
| 2 | 哪个请求被拦截了？为什么？ | TraceId=0c68ec2f，Prompt 注入攻击 |
| 3 | 另一个请求走了什么意图？ | ChemicalCompliance |
| 4 | 那个请求花了多长时间？ | 9508ms |
| 5 | 那个请求有没有调用工具？ | 没有（工具调用=0） |
| 6 | 性能瓶颈在哪里？ | 执行阶段（9400ms，占 98.9%） |
| 7 | 如果让你修复"工具调用=0"的问题，你会从哪里入手？ | 检查 ComplianceCheckModule 的工具调度 Prompt |

---

> **进阶阅读**：
> - [系统日志解读与排障实战指南](./系统日志解读与排障实战指南.md) — 日志体系全景 + 错误速查表
> - [故障排查文档](../_archive/troubleshooting/故障排查文档.md) — 17 个已修复错误的完整记录
> - [Task11 集成测试部署指南](./integration/Task11-集成测试部署指南.md) — 如何运行测试并解读结果
