# models 说明书

GGUF 权重的目录约定与文件名清单。llama.cpp 源码在 `third_party/llama.cpp/`，推理服务由 [infra](../infra/说明书.md) 的 compose 启动。

规范正文：

**→ [../../models/README.md](../../models/README.md)**

## 边界（摘要）

| 负责 | 不负责 |
|------|--------|
| 文件名必须与 compose 一致 | 把 `.gguf` 提交进 Git |
| 下载/校验脚本入口 | 训练或量化新模型 |
| 缺文件时的故障含义（8080/8081/8083） | 业务禁配逻辑 |

校验（默认不下载）：`scripts/download-models.ps1` / `.sh`。设置 `AGENT1_MODEL_BASE_URL` 才按文件名拉取。

3070 实测（vision `--mmproj` 拒参）见 [../infra/2026-09-06_RTX3070容器实测记录.md](../infra/2026-09-06_RTX3070容器实测记录.md)，不要把缺 VL 文件说成那次 8083 失败的主因。
