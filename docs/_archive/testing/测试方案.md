# 工业 AI Agent 线性架构 - 完整测试方案

> **文档版本**：v1.1（深度扩展版 — 2026-06-24）
> **关联修复文档**：[P0-P1修复详细技术文档](../troubleshooting/P0-P1修复详细技术文档.md) | [RAG工程Bug修复笔记](../troubleshooting/RAG工程Bug修复笔记_2026-05-26.md)

## 📋 测试目标
1. 验证模块9（SmartAutoRouter）和模块8（SmartDialogIndustrial）的功能
2. 检测这两个模块是否正确调用四大范式
3. 测试简单对话模式和专业诊断模式的切换逻辑
4. 验证记忆机制是否正常工作

---

## 🏗️ 架构分析

### 模块定义 (ModuleType)
```
1 - CoT (Solid Output)          - 思维链推理
2 - CoT (Stream Output)         - 思维链流式输出
3 - ReAct (Solid Output)        - 推理-行动循环
4 - ReAct (Stream Output)       - 推理-行动流式输出
5 - Reflection                  - 反思纠错
6 - RAG                         - 检索增强生成
7 - Smart Dialog (General)      - 通用智能对话
8 - Smart Dialog (Industrial)   - 工业智能对话
9 - Smart Auto Router           - 自动语义路由 ★
```

### **关键发现：模块8/9 不调用四大范式！**

- 模块9（SmartAutoRouter）和模块8（SmartDialogIndustrial）是独立系统
- 它们使用自己的Handler，不经过CoT/ReAct/Reflection/RAG
- 工业诊断使用ToolService调用工具，不是通过四大范式

---

## 🧪 测试场景

### 场景1：模块9 - 自动语义路由 - 简单对话模式
**目标**：验证简单对话模式下的记忆和功能
**输入序列**：
```
1. "你好，我叫李工，你叫王工"  → 提取双方名字
2. "我是谁？"                  → 从记忆回答
3. "你叫什么？"                → 从记忆回答
4. "你能做什么？"              → 简单对话生成
```
**预期结果**：
- ✅ 显示"⚡ 识别为：简单对话模式"
- ✅ 记忆正确保存和回答
- ✅ 无 `</think>` 标签输出

---

### 场景2：模块9 - 自动语义路由 - 专业诊断模式
**目标**：验证工业关键词触发诊断模式
**工业关键词**：`温度`、`机床`、`主轴`、`异常`、`故障`、`阈值`、`诊断`、`检查`、`传感器`、`数据`、`参数`

**输入序列**：
```
1. "检查机床主轴温度"       → 触发工业诊断
2. "温度超过阈值了吗？"    → 继续工业诊断
3. "帮我诊断异常原因"      → 继续工业诊断
```
**预期结果**：
- ✅ 显示"📋 识别为：专业诊断模式"
- ✅ 调用ToolService（分析中 → 调用工具 → 生成答案）
- ✅ 保存工业相关事实到记忆

---

### 场景3：模块9 - 混合模式切换
**目标**：验证两种模式可以无缝切换
**输入序列**：
```
1. "你好，我叫测试员"          → 简单对话
2. "检查主轴温度"             → 切换到专业诊断
3. "现在温度多少？"           → 继续专业诊断
4. "谢谢你的帮助"             → 切换回简单对话
5. "我是谁？"                → 从记忆回答（记忆应该跨模式保持）
```
**预期结果**：
- ✅ 模式正确切换
- ✅ 记忆在模式切换中保持一致

---

### 场景4：模块8 - 工业智能对话
**目标**：验证模块8的功能
**输入序列**：
```
1. "检查机床运行状态"
2. "查看传感器数据"
3. "诊断故障原因"
```
**预期结果**：
- ✅ 自动使用IndustrialDiagnosticHandler
- ✅ 正确调用工具
- ✅ 记忆正常工作

---

### 场景5：边界测试 - 关键词边界
**目标**：验证路由的准确性
**测试用例**：
```
✅ 应该走工业诊断：
   - "温度多少？"
   - "机床有异常吗？"
   - "检查传感器数据"
   - "诊断故障"
   
✅ 应该走简单对话：
   - "你好"
   - "谢谢"
   - "我叫XX"
   - "为什么？"
   - "怎么样？"
   - (不含工业关键词的任意问题)
```

---

### 场景6：记忆功能测试
**目标**：验证记忆提取和查询
**输入序列**：
```
1. "我叫张工，你叫助手"
2. "主轴温度是85℃"
3. "安全阈值是80℃"
4. "我叫什么？"            → 记忆回答：张工
5. "你叫什么？"            → 记忆回答：助手
6. "刚才温度多少？"       → 记忆回答：85℃
7. "安全阈值是多少？"     → 记忆回答：80℃
```

---

### 场景7：命令功能测试
**目标**：验证特殊命令
**测试命令**：
```
- "history" 或 "历史" → 显示对话历史
- "clear" 或 "清空"   → 清空对话和记忆
- "exit" 或 "退出"    → 退出模块
```

---

## 🔍 检测四大范式调用的方法

### **结论：模块8/9 不调用四大范式！**

| 模块 | 是否调用四大范式 | 使用的技术 |
|------|----------------|-----------|
| 1-6  | ✅ 是 | CoT/ReAct/Reflection/RAG 核心逻辑 |
| 7-8  | ❌ 否 | SmartDialogSystem（独立实现） |
| 9    | ❌ 否 | IntentRouter + 两个Handler |

### 验证方法
1. 检查代码：`SmartAutoRouterModule` → `SimpleChatHandler` / `IndustrialDiagnosticHandler`
2. 检查代码：`IndustrialDiagnosticHandler` → 使用 `ToolService`，不调用范式模块
3. 运行时观察：没有CoT/ReAct等特定输出格式

---

## 📊 测试检查清单

| 测试项 | 模块9 | 模块8 | 状态 |
|--------|-------|-------|------|
| 简单对话模式 | ☐ | ☐ | |
| 专业诊断模式 | ☐ | ☐ | |
| 记忆提取 | ☐ | ☐ | |
| 记忆回答 | ☐ | ☐ | |
| 工具调用 | ☐ | ☐ | |
| 模式切换 | ☐ | N/A | |
| 命令功能 | ☐ | ☐ | |
| 无think标签 | ☐ | ☐ | |

---

## 🚀 自动化测试

见 `agent_test.py` 自动化测试脚本

---

## 双层评测架构 (Phase 2a+, 2026-06-04)

### 架构背景

menu 13「合规评测集」原本将两个不同层面的评测耦合在一起，导致 0% 评分时无法诊断根因。现拆分为双层架构：

### Layer 1: FC 就绪性检查（前置条件）

- **目的**: 验证 Function Calling 管线是否正常工作
- **实现**: `LlmService.RunFcReadinessCheckAsync()` — 使用与评测相同的非流式调用路径
- **用例**: 5 条明确触发指定工具的测试用例
- **通过标准**: 至少 1 条触发了工具调用（3 条以上为"就绪"）
- **失败处理**: 阻断 Layer 2，生成诊断报告，提示用户先排查 FC 管线
- **[#10] 非关键工具取舍**: `GetCurrentTime` 为通用时间工具，不参与合规结论推导；领域化 system prompt 下其触发率低属预期行为，就绪性判定将其标记为**非关键工具**，主业务工具 4/4 触发即视为 FC 管线就绪；不通过修改 prompt 追求 5/5（概率性手段，违反确定性优先原则，且可能损害主业务工具路由精度）

### Layer 2: 业务准确度评测

- **目的**: 在 FC 管线就绪的前提下，评测模型业务能力
- **三维度**: 工具触发率 / 参数准确率 / 结论准确率
- **用例**: 50 条业务场景（危险类别查询 / 储存兼容性 / 安全距离 / 综合审核）
- **报告**: `eval_report.json` 包含 `fc_readiness` 字段记录 Layer 1 状态

### 关键设计决策

1. **同路径验证**: Layer 1 使用 `InvokeNonStreamingWithRetryAsync`，与 Layer 2 完全一致，避免了流式/非流式路径差异导致的假阳性
2. **Prompt 工具引导**: `EvalFastPrompt` 显式列出可用工具并强制要求"回答前必须先调用工具"，防止模型绕过 FC 直接编造答案
3. **阻断机制**: Layer 1 不通过则不跑 Layer 2，避免 50 条全量无效跑测浪费 CPU 时间

### 评测入口

```
menu 13 选择
  -> Layer 1: FC 就绪检查
    -> 失败 -> 诊断报告 -> 退出
    -> 通过 -> Layer 2: 50 条业务评测 -> 完整评测报告
```
