# 项目分析报告与下一步计划

**生成日期**: 2026-05-20  
**项目**: AgentSystem - 化工园区危化品合规审查 AI Agent  
**当前阶段**: P1 开发中

---

## 一、项目现状总结

### 1.1 整体架构

项目采用 .NET 8 + Semantic Kernel + Ollama 构建，是一个面向化工园区危化品合规审查的企业级 AI Agent 系统。核心架构分为四层：

| 层级 | 组件 | 状态 |
|------|------|------|
| **模块层** | 8 个推理模块（CoT/ReAct/RAG/Reflection/Compliance/UnifiedDialog） | 已实现 |
| **服务层** | LLM、知识库、数据库、审计、会话、集成服务 | 已实现 |
| **配置层** | 统一配置中心 AppConfig（化工行业专用） | 已实现 |
| **数据层** | PostgreSQL + pgvector 混合检索 | 已实现 |

### 1.2 已完成功能

- CoT 链式推理（Zero-Shot 模式，同步/流式）
- ReAct 推理+工具调用（同步/流式）
- Reflection 自反思推理
- RAG 检索增强生成（BM25 + 向量混合检索）
- 化工专用 RAG（三级知识体系：国标 > 园区规则 > 历史案例）
- 合规审查模块（危化品存储/动火作业/安全距离检查）
- 会话管理与多轮对话
- 审计日志（等保三级标准，180天留存）
- 数据库初始化与向量索引（HNSW）
- 统一模块调度器（ModuleDispatcher + ModuleFactory）
- 意图路由（IntentRouter）
- 单元测试（xUnit + Moq）
- 架构收敛测试

### 1.3 技术栈依赖

| 依赖 | 版本 | 用途 |
|------|------|------|
| Microsoft.SemanticKernel | 1.74.0 | LLM 编排 |
| Microsoft.SemanticKernel.Connectors.Ollama | 1.74.0-alpha | Ollama 连接 |
| Npgsql | 8.0.5 | PostgreSQL 驱动 |
| Dapper | 2.1.35 | 轻量 ORM |
| Pgvector | 0.3.2 | 向量类型支持 |
| deepseek-r1:local7b | - | 本地推理模型 |
| nomic-embed-text:latest | - | 向量嵌入模型 |

---

## 二、现存问题与不足

### 2.1 架构层面

| # | 问题 | 严重程度 | 说明 |
|---|------|----------|------|
| A1 | **缺少依赖注入容器** | 高 | 服务通过手动 new 创建，未使用 DI 容器，难以测试和替换实现 |
| A2 | **配置硬编码** | 高 | 数据库密码、端口、模型名等硬编码在 AppConfig.cs 中，应迁移到 appsettings.json 或环境变量 |
| A3 | **Program.cs 职责过重** | 中 | 入口文件承担了菜单、初始化、测试等过多职责，需拆分 |
| A4 | **集成服务为空壳** | 低 | IntegrationService（ERP/WMS/EHS）仅有接口定义，无实际实现 |
| A5 | **AuditService 仅内存存储** | 中 | 审计日志未持久化到数据库，重启即丢失，不满足等保三级要求 |

### 2.2 代码质量

| # | 问题 | 严重程度 | 说明 |
|---|------|----------|------|
| C1 | **异常处理不统一** | 中 | 各模块异常处理方式不一致，缺少全局异常处理机制 |
| C2 | **日志系统缺失** | 高 | 未集成结构化日志框架（如 Serilog），调试信息通过 Console.WriteLine 输出 |
| C3 | **测试覆盖率低** | 中 | 仅有 KnowledgeBaseService 和 ToolService 的单元测试，核心模块缺少测试 |
| C4 | **安全隐患** | 高 | 数据库密码明文存储在代码中（changeme） |

### 2.3 功能层面

| # | 问题 | 严重程度 | 说明 |
|---|------|----------|------|
| F1 | **向量嵌入生成未完全集成** | 中 | 文档入库时嵌入生成流程不完整 |
| F2 | **Milvus 尚未接入** | 低 | README 标注为"规划中"，当前仅依赖 pgvector |
| F3 | **知识库更新无增量机制** | 中 | 每次需全量重载知识库，无增量更新/版本管理 |
| F4 | **无 Web API 接口** | 中 | 仅支持控制台交互，缺少 HTTP API 对外暴露能力 |

---

## 三、下一步计划（建议优先级排序）

### Phase 1: 基础设施加固（紧急，1-2周）

#### 1. 引入依赖注入（DI）
- 使用 `Microsoft.Extensions.DependencyInjection`
- 将所有 Service 注册到 DI 容器
- Program.cs 改为 Host Builder 模式
- **收益**: 解耦服务依赖，提升可测试性

#### 2. 配置外部化
- 创建 `appsettings.json` + `appsettings.Development.json`
- 敏感信息迁移到环境变量或 User Secrets
- 使用 `IConfiguration` 绑定配置类
- **收益**: 消除安全隐患，支持多环境部署

#### 3. 引入结构化日志
- 集成 Serilog + Console/File Sink
- 替换所有 `Console.WriteLine` 调试输出
- 添加请求追踪（CorrelationId）
- **收益**: 生产环境可观测性

### Phase 2: 核心功能完善（重要，2-3周）

#### 4. 审计日志持久化
- 将 AuditService 日志写入 PostgreSQL `audit_logs` 表
- 实现 180 天自动清理策略
- **收益**: 满足等保三级合规要求

#### 5. 向量检索完整链路
- 完善文档嵌入生成流程（调用 nomic-embed-text）
- 实现知识库增量更新机制（基于文件哈希去重）
- 优化 HNSW 索引参数（基于实际数据量调优）
- **收益**: RAG 检索质量提升

#### 6. 扩展测试覆盖
- 为 ComplianceCheckModule 添加集成测试
- 为 HybridKnowledgeBaseService 添加混合检索测试
- 为 ModuleDispatcher 添加端到端测试
- 目标: 核心路径测试覆盖率 > 70%
- **收益**: 回归保护，重构信心

### Phase 3: 对外能力建设（中期，3-4周）

#### 7. 暴露 Web API
- 使用 ASP.NET Core Minimal API 或 Controller
- 提供 `/api/chat`（对话）、`/api/compliance/check`（合规检查）等端点
- 添加 Swagger/OpenAPI 文档
- **收益**: 支持前端对接和第三方集成

#### 8. 会话持久化
- 将 SessionService 从内存迁移到 PostgreSQL
- 支持跨重启的会话恢复
- **收益**: 生产可用性

### Phase 4: 高级特性（远期）

#### 9. Milvus 向量数据库接入
- 替代或补充 pgvector，提升大规模检索性能
- **前置条件**: Phase 2 完成

#### 10. ERP/WMS/EHS 系统对接
- 实现 IntegrationService 的实际连接逻辑
- **前置条件**: Web API 层就绪

#### 11. 多模型支持
- 支持在线模型（如 Claude API、通义千问）作为备选推理引擎
- 模型路由策略（本地模型 → 在线模型降级）

---

## 四、架构演进建议

```
当前架构（控制台应用）:
┌─────────────────────────────────────────┐
│  Program.cs (Console Menu)              │
│    ├── ModuleDispatcher                 │
│    │   ├── CoT/ReAct/RAG/...Modules    │
│    │   └── ComplianceCheckModule        │
│    ├── Services (手动创建)              │
│    └── AppConfig (硬编码)               │
└─────────────────────────────────────────┘

目标架构（Web API + DI）:
┌─────────────────────────────────────────┐
│  ASP.NET Core Host                      │
│    ├── API Controllers / Minimal API    │
│    ├── DI Container                     │
│    │   ├── ILlmService → LlmService    │
│    │   ├── IKnowledgeBaseService → ...  │
│    │   └── IDatabaseService → ...       │
│    ├── ModuleDispatcher (DI注入)        │
│    ├── Middleware (Auth/Logging/Error)   │
│    └── IConfiguration (appsettings)     │
└─────────────────────────────────────────┘
```

---

## 五、关键指标

| 指标 | 当前值 | 目标值 |
|------|--------|--------|
| 核心模块数 | 8 | 8（稳定） |
| 服务接口数 | 13 | 13+（按需扩展） |
| 单元测试数 | 2 个文件 | 10+ 个文件 |
| 测试覆盖率 | ~10% | >70% |
| 配置外部化 | 0% | 100% |
| 日志结构化 | 无 | Serilog 全覆盖 |
| API 端点 | 0 | 5+ |
| 文档完整度 | 高 | 持续维护 |

---

## 六、建议立即执行的操作

1. **创建 `.gitignore`** - 当前缺少 gitignore，应排除 `bin/`, `obj/`, `.vs/`, `*.user` 等
2. **提交当前代码** - git status 显示大量未跟踪文件，建议整理后做一次完整提交
3. **从 Phase 1 第 2 项开始** - 先将数据库密码从代码中移除（安全优先）

---

*本报告由 Claude 基于项目源码分析自动生成，建议结合团队实际情况调整优先级。*
