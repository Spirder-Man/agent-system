# AutoDL RTX 3090 服务器 — Agent1 化工合规 AI 部署评测全记录

> **日期**：2026-06-13  
> **服务器**：AutoDL GPU 算力机（NVIDIA RTX 3090 / 24GB VRAM, Intel Xeon Gold 6330, 755GB RAM）  
> **容器 ID**：`autodl-container-k5xt4fu27v-d60014d2`  
> **操作系统**：Ubuntu 22.04.5 LTS (GNU/Linux 5.15.0-164-generic x86_64)  
> **文档版本**：v1.0

---

## 目录

- [一、环境基线](#一环境基线)
- [二、部署全流程时间线](#二部署全流程时间线)
- [三、问题分类与故障排查](#三问题分类与故障排查)
- [四、业务评测结果](#四业务评测结果)
- [五、性能瓶颈诊断](#五性能瓶颈诊断)
- [六、优化迭代记录](#六优化迭代记录)
- [七、最终部署方案总结](#七最终部署方案总结)
- [八、经验教训与建议](#八经验教训与建议)

---

## 一、环境基线

### 1.1 硬件配置

| 组件 | 规格 |
|------|------|
| GPU | NVIDIA RTX 3090, 24GB VRAM, Compute Capability `sm_86` |
| CPU | Intel Xeon Gold 6330, 支持 avx2 / avx512f / avx512_vnni |
| RAM | 755GB（容器动态分配，初始显示 90GB） |
| 系统盘 | 30G (`/`)，随镜像保存 |
| 数据盘 | 50G (`/root/autodl-tmp`)，不随镜像保存 |

### 1.2 软件栈

| 组件 | 版本 | 说明 |
|------|------|------|
| OS | Ubuntu 22.04.5 | glibc 2.35 |
| CUDA Toolkit | 12.8 | `/usr/local/cuda/bin/nvcc` |
| NVIDIA Driver | 580.105.08 | |
| .NET SDK | 8.0.128 | APT 安装 |
| PostgreSQL | 16 + pgvector | 向量维度 768，HNSW 索引 |
| llama.cpp | 最新源码（gitee 镜像） | CUDA GPU 加速编译 |

### 1.3 模型选型

| 用途 | 模型 | 量化 | 大小 | 推理方式 |
|------|------|------|------|----------|
| LLM 推理 | Qwen3-8B-Instruct | Q4_K_M | 4.68 GB | GPU (`-ngl 99`) |
| 向量嵌入 | nomic-embed-text-v1.5 | F16（全精度） | 261.5 MB | CPU (`-ngl 0`) |

### 1.4 容器约束

| 约束项 | 影响 |
|--------|------|
| 不支持 Docker | 无法容器化部署，必须全原生 |
| 网络严重受限 | GitHub/HuggingFace 直连不可行，需本地下载 + JupyterLab 上传 |
| sudo 不可用 | 需用 `su` 替代 |
| JupyterLab 上传路径 | 默认落盘到 `/`（根目录），非 `/root/autodl-tmp/` |
| PostgreSQL 禁止自动启动 | 需手动 `pg_ctlcluster 16 main start` |
| /root 对其他用户不可见 | postgres 用户需用 `/tmp/` 传递 SQL 脚本 |

### 1.5 关键路径约定

| 用途 | 路径 |
|------|------|
| 工作根目录 | `/root/autodl-tmp/` |
| llama.cpp 源码 | `/root/autodl-tmp/llama.cpp/` |
| 编译产物 | `/root/autodl-tmp/llama.cpp/build/bin/` |
| 模型目录 | `/root/autodl-tmp/models/` |
| 项目代码 | `/root/autodl-tmp/agent-system/` |
| 日志目录 | `/root/autodl-tmp/logs/` |

---

## 二、部署全流程时间线

### 2.1 总览

```
09:30 ─────────────────────────────────────────────────────── 23:20
│                                                                 │
│  前置准备  环境安装  Ollama踩坑  llama.cpp   模型部署  评测调优  │
│  09:30     11:00     12:46      15:30       18:00    20:00     │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 阶段 0：本地准备与 AutoDL 实例启动（09:30 - 11:00）

| 时间 | 操作 | 结果 |
|------|------|------|
| 09:30 | 用户在本地回顾前次部署经验 | — |
| 09:50 | 启动 AutoDL 算力实例（RTX 3090 / 24GB） | 分配中 |
| 10:30 | AutoDL 实例分配完成 | 容器 ID: `autodl-container-k5xt4fu27v-d60014d2` |
| 10:50 | SSH 首次登录 | 确认 Ubuntu 22.04.5，干净容器 |

### 2.3 阶段 1：环境安装与首次部署（11:00 - 14:00）

| 时间 | 操作 | 问题/结果 |
|------|------|-----------|
| 11:21 | 检查 .NET → `command not found` | 🔴 镜像未预装 |
| 11:22 | `dotnet-install.sh` 下载 SDK | 🔴 CDN 直连超时，终端出现 `^[[20~` 乱码 |
| **11:24** | **决策：放弃脚本，改用 APT 安装** | ✅ .NET 8.0.128 安装成功 |
| 11:27 | `apt install postgresql-16-pgvector` | 🔴 找不到包（Ubuntu 默认源不含） |
| 11:28 | 导入 PostgreSQL GPG 密钥 + APT 源 | ✅ 安装成功 |
| 11:29 | PostgreSQL 安装完毕 | 🔴 `invoke-rc.d` 拦截了自动启动 |
| 11:30 | `pg_ctlcluster 16 main start` | ✅ 手动启动成功 |
| 11:31 | `sudo -u postgres psql` | 🔴 `sudo` 不存在，改用 `su - postgres -c` |
| 11:33 | 创建数据库 + pgvector 扩展 | ✅ 就绪 |
| 11:34 - 12:00 | Ollama 官方安装脚本 + 7 轮镜像尝试 | 🔴 全部失败（网络断连） |
| **12:01** | **决策：本地下载 + JupyterLab 上传** | — |
| 12:13 | `dotnet publish` → CS0246 错误 | 🔴 `.gitignore` 中 `memory/` 误排除了 Memory 模块 |
| 12:17 | 修复 `.gitignore`：`memory/` → `/memory/` | ✅ git push + pull 后编译成功 |
| 12:22 | 数据库初始化 | ✅ 6 张表创建成功（含 pgvector HNSW 索引） |
| 12:25 | 本地 Ollama 下载完成（1.54GB） | — |
| 12:45 | JupyterLab 上传 → 找不到文件 | 🔴 上传默认落盘到 `/` 根目录 |
| 12:46 | `find / -name ollama*.tgz` 定位 | ✅ 找到 /root/ollama-linux-amd64.tgz |
| 12:46 | `tar -xzf` 解压 → `ollama --version` | 🔴 **Segmentation fault (core dumped)** |

### 2.4 阶段 2：Ollama Segfault 诊断与放弃（14:40 - 15:30）

| 时间 | 排查方向 | 操作 | 结论 |
|------|----------|------|------|
| 15:00 | 架构错误？ | `uname -m` → `x86_64` | ✅ 架构正确 |
| 15:02 | 动态链接库缺失？ | `ldd /usr/bin/ollama` → 所有 .so 已找到 | ✅ 非库缺失 |
| 15:03 | 文件损坏？ | `file` + `ls -lh` → ELF 64-bit, 1.1GB 完整 | ✅ 文件正常 |
| 15:04 | glibc 过低？ | `ldd --version` → glibc 2.35 ≥ 2.32 | ✅ 版本满足 |
| 15:06 | CPU 指令集？ | **`grep -o 'avx2\?'` → 只匹配到 `avx`（误判！）** | ⚠️ 正则 `?` 导致误判 |
| 15:25 | 正确诊断 | `grep flags /proc/cpuinfo \| head -1` | ✅ avx2 / avx512 全套支持 |
| 15:20 | 下载旧版 v0.1.48 | 7 种渠道全部失败 | 🔴 所有 GitHub 渠道不通 |
| **15:30** | **最终结论** | Ollama 预编译二进制与容器环境兼容性冲突（CUDA 运行时库版本 / seccomp / AppArmor） | **放弃 Ollama** |

> ⚠️ **关键教训**：`grep -o 'avx2\?'` 中 `?` 表示"0 次或 1 次匹配"，所以 `avx` 就能匹配成功。正确方式：`grep flags /proc/cpuinfo | head -1`

### 2.5 阶段 3：转向 llama.cpp 原生编译（15:30 - 16:40）

| 时间 | 操作 | 结果 |
|------|------|------|
| 15:35 | `git clone` gitee 镜像 | ✅ 代码获取成功 |
| 15:38 | `cmake -B build` CPU 版 | ✅ 编译通过 |
| 15:52 | 检查 GPU 环境 | ✅ RTX 3090 24GB, CUDA 12.8 已预装 |
| 15:56 | `cmake -B build -DGGML_CUDA=ON` | 🔴 `CMAKE_CUDA_COMPILER-NOTFOUND` |
| 15:58 | 显式指定 `-DCMAKE_CUDA_COMPILER=/usr/local/cuda/bin/nvcc` | ✅ 配置成功 |
| 16:18 | `cmake --build build -j$(nproc)` | ✅ CUDA GPU 版编译完成 |

### 2.6 阶段 4：模型文件获取（16:00 - 17:30）

| 轮次 | 来源 | 尝试 | 结果 |
|------|------|------|------|
| 1 | hf-mirror | `Qwen/Qwen3-8B-Instruct-GGUF/.../qwen3-8b-instruct-q4_k_m.gguf` | 🔴 404（文件名大小写错误） |
| 2 | hf-mirror | `bartowski/Qwen3-8B-Instruct-GGUF/...` | 🔴 404（仓库名格式不匹配） |
| 3 | ModelScope | 搜索 "Qwen3-8B-GGUF" | 🔴 无结果 |
| 4 | hf-mirror API | `curl api/models/bartowski/Qwen3-8B-Instruct-GGUF` | 🔴 hf-mirror 不代理 API |
| 5 | HF 直连 API | `curl huggingface.co/api/models/...` | 🔴 容器内直连超时 |
| 6 | HF API Mradermacher | 尝试替代仓库 | 🔴 同样超时 |
| **7** | **WebSearch** | **查询 "Qwen3-8B GGUF bartowski"** | ✅ 找到正确格式 |

> 💡 **关键教训**：GGUF 文件名格式极其严格。bartowski 仓库名使用 `Qwen_Qwen3-8B-GGUF`（下划线），而非 `Qwen3-8B-Instruct-GGUF`。多次 404 后直接用搜索引擎比盲目猜测更高效。

**最终成功命令（Windows 本地）**：

```powershell
# LLM 推理模型（4.68GB，约 50 分钟）
curl.exe -L -o $env:USERPROFILE\Downloads\Qwen_Qwen3-8B-Q4_K_M.gguf `
  https://hf-mirror.com/bartowski/Qwen_Qwen3-8B-GGUF/resolve/main/Qwen_Qwen3-8B-Q4_K_M.gguf

# 嵌入模型（261.5MB，约 4 分钟）
curl.exe -L -o $env:USERPROFILE\Downloads\nomic-embed-text-v1.5.f16.gguf `
  https://hf-mirror.com/nomic-ai/nomic-embed-text-v1.5-GGUF/resolve/main/nomic-embed-text-v1.5.f16.gguf
```

**下载进度（LLM 模型）**：

| 时间 | 进度 | 速度 | 预计剩余 |
|------|------|------|----------|
| 16:16 | 0% | ~1.1 MB/s | ~50 min |
| 16:45 | 59% | ~2.2 MB/s | ~15 min |
| 16:50 | 85% | ~2.9 MB/s | ~4 min |
| 16:55 | 100% | 3.5 MB/s（峰值） | ✅ 完成 |

### 2.7 阶段 5：模型上传与服务启动（18:00 - 20:00）

| 时间 | 操作 | 结果 |
|------|------|------|
| 18:03 | JupyterLab 上传 Qwen3-8B (4.68GB) | ✅ ~5 分钟完成 |
| 18:09 | JupyterLab 上传 nomic-embed-text (261.5MB) | ✅ ~1 分钟完成 |
| 18:11 | `find / -name "*.gguf"` 定位文件 | ✅ 确认在 `/` 根目录 |
| 18:13 | `mv` 到 `/root/autodl-tmp/models/` | ✅ 就位 |
| 约20:00 | 启动 llama-server（LLM + Embedding） | ✅ 两个服务正常运行 |
| 约20:10 | 修改 appsettings.json → 指向 llama.cpp 端点 | ✅ |
| 约20:15 | 编译 + 启动 Agent1 API | ✅ |
| 约23:19 | 第一轮评测完成 | 📊 结果见第四章 |

---

## 三、问题分类与故障排查

### 3.1 完整问题清单（18 个问题，按时间排序）

| # | 时间 | 问题 | 根因 | 解决方案 | 类别 |
|---|------|------|------|----------|------|
| 1 | 11:22 | `dotnet-install.sh` 下载 SDK 卡死 | `builds.dotnet.microsoft.com` 国内超时 | 改用 APT 包管理器 | 安装源 |
| 2 | 11:27 | `apt install postgresql-16-pgvector` 找不到包 | Ubuntu 默认源不含 pgvector | 导入 PostgreSQL GPG + APT 官方源 | 安装源 |
| 3 | 11:31 | `sudo -u postgres` command not found | AutoDL 精简容器无 sudo | 用 `su - postgres -c "cmd"` 替代 | 容器约束 |
| 4 | 12:02 | PowerShell `%USERPROFILE%` 报错 | PowerShell 不支持 cmd 语法 | 改用 `$env:USERPROFILE` | 语法错误 |
| 5 | 12:08 | postgres 无法读取 /root/ SQL 脚本 | postgres 用户无 /root 权限 | `cp` 到 `/tmp/` 再执行 | 容器约束 |
| 6 | 12:13 | CS0246: IMemoryService 找不到 | `.gitignore` 中 `memory/` 误排除子目录 | 改为 `/memory/`（仅匹配根目录） | 代码配置 |
| 7 | 12:42 | JupyterLab 上传文件找不到 | 默认落盘到 `/` 而非 `/root/autodl-tmp/` | `find / -name 'filename*'` 定位后 mv | 容器约束 |
| 8 | 12:46 | **Ollama Segmentation fault** | 预编译二进制 + 自带 CUDA 库 + 容器 seccomp 冲突 | 放弃 Ollama，转向 llama.cpp 原生编译 | 二进制兼容 |
| 9 | 12:49 | 误判无 AVX2 指令集 | `grep -o 'avx2\?'` 正则 `?` 只匹配到 `avx` | `grep flags /proc/cpuinfo \| head -1` | 诊断误用 |
| 10 | 11:53~15:10 | GitHub Release 下载超时 | AutoDL 容器网络严重受限 | 本地下载 + JupyterLab 上传 | 网络限制 |
| 11 | 12:02~15:14 | Windows curl GitHub 仅返回 9 字节 | GitHub 被国内网络阻断 | 换 hf-mirror.com | 网络限制 |
| 12 | 12:03~15:16 | ghproxy/ghfast/moeyy 镜像全部 SSL 错误 | Windows schannel 与镜像 TLS 协商失败 | 换 hf-mirror.com | 代理失败 |
| 13 | 15:56 | CMake CUDA 配置 `CMAKE_CUDA_COMPILER-NOTFOUND` | CMake 无法自动发现 nvcc | 显式指定路径 | 编译配置 |
| 14 | 16:02 | GGUF 模型多次 404（4 种 URL） | 文件名大小写/格式与 HF 仓库不一致 | WebSearch 找到正确仓库名 | 模型获取 |
| 15 | 16:04 | HF API 查询无返回 | hf-mirror 不代理 API，直连超时 | 放弃 API，改用 WebSearch | 网络限制 |
| 16 | 16:14 | `wget -O` 下载空文件（0 字节） | 目标目录 `/root/autodl-tmp/models/` 未创建 | `mkdir -p` 先创建目录 | 操作遗漏 |
| 17 | 16:30 | psql 密码认证失败 | 非交互模式未提供密码 | `PGPASSWORD=changeme psql ...` | 数据库配置 |
| 18 | 16:36 | appsettings.json 指向 Ollama 端点 | 配置默认为 `localhost:11434` | 改为 llama.cpp `localhost:8080/v1` | 服务配置 |

### 3.2 问题分类统计

| 类别 | 数量 | 典型问题 |
|------|------|----------|
| 容器环境约束 | 4 | #3 sudo 不可用、#5 postgres 权限、#7 上传路径、#10 网络限制 |
| 安装源/镜像 | 2 | #1 .NET SDK 源、#2 pgvector 源 |
| 网络/代理 | 4 | #10 GitHub 直连、#11 GitHub 阻断、#12 SSL 错误、#15 API 不可用 |
| 二进制兼容性 | 1 | #8 Ollama segfault |
| 诊断工具误用 | 1 | #9 grep 正则错误 |
| 代码/配置 | 2 | #6 .gitignore、#18 LLM 端点 |
| 编译配置 | 1 | #13 nvcc 路径 |
| 模型获取 | 1 | #14 GGUF 404 |
| 操作遗漏 | 1 | #16 wget 空文件 |
| 数据库配置 | 1 | #17 psql 密码 |
| PowerShell 语法 | 1 | #4 %USERPROFILE% |

### 3.3 高频错误模式分析

| 错误模式 | 出现次数 | 根因 | 防范措施 |
|----------|----------|------|----------|
| **网络不可达** | 5 次 | AutoDL 容器网络严重受限 + 国内墙 | 大文件始终走本地下载 + JupyterLab 上传 |
| **容器环境差异** | 4 次 | 精简容器缺少 sudo、iptables 等 | 部署前确认容器约束清单 |
| **配置默认值不匹配** | 3 次 | Git 配置、appsettings 默认值针对开发环境 | 生产环境专项配置模板 |

---

## 四、业务评测结果

### 4.1 第一轮评测（2026-06-13 23:19，基线版本 `932a735`）

**模型**：Qwen3-8B Q4_K_M 量化版，通过 llama.cpp CUDA 推理

#### 综合指标

| 指标 | 数值 | 评级 |
|------|------|------|
| **总用例数** | 63 | — |
| **成功执行** | 63 (100%) | ✅ |
| **异常数** | 0 | ✅ |
| **FC 就绪性** | 4/5 | ✅ 通过 |
| **工具触发率** | 87.3% (55/63) | ⚠️ |
| **参数准确率** | 80.95% (51/63) | ⚠️ |
| **结论准确率** | 58.73% (37/63) | ❌ |
| **RAG Precision@K** | 5.7% | ❌ |
| **RAG Recall@K** | 8.5% | ❌ |
| **RAG MRR** | 0.222 | ❌ |
| **生成忠实度** | 37.4% (34 已验证 / 57 疑似幻觉) | ❌ |

#### 分类细项

| 类别 | 用例数 | 工具触发 | 参数准确 | 结论准确 | P@K | 忠实度 | 评级 |
|------|--------|----------|----------|----------|-----|--------|------|
| 储存兼容性 | 20 | 20/20 ✅ | 20/20 ✅ | 20/20 ✅ | 12.0% | 81.8% | ⭐⭐⭐ |
| 重大危险源查询 | 5 | 5/5 ✅ | 5/5 ✅ | 5/5 ✅ | 0% | 0% | ⭐⭐⭐ |
| 法规版本查询 | 3 | 3/3 ✅ | 3/3 ✅ | 3/3 ✅ | 6.7% | 33.3% | ⭐⭐⭐ |
| 综合审核 | 7 | 6/7 ⚠️ | 6/7 ⚠️ | 4/7 ⚠️ | 5.7% | 71.4% | ⭐⭐ |
| 化学品属性查询 | 5 | 3/5 ❌ | 2/5 ❌ | 1/5 ❌ | 0% | 0% | ⭐ |
| 危险类别查询 | 15 | 12/15 ⚠️ | 12/15 ⚠️ | 4/15 ❌ | 0% | 13.2% | ⭐ |
| **安全距离** | **8** | **6/8** ❌ | **3/8** ❌ | **0/8** ❌ | **7.5%** | **83.3%** | **☆** |

### 4.2 第二轮评测（知识库优化后，commit `190236a`）

**优化内容**：新增 GB50160 防火间距 + GB30000 危险分类知识文档，优化函数描述和 RAG 查询构造（详见第五章）

| 指标 | 优化前 | 优化后 | 变化 |
|------|--------|--------|------|
| 工具触发率 | 87.3% | **90.5%** | ↑ +3.2% |
| 参数准确率 | 80.95% | **87.3%** | ↑ +6.35% |
| 结论准确率 | 58.73% | **49.2%** | ↓ **-9.53%** ❌ |
| RAG Precision@K | 5.7% | 5.7% | → |
| 生成忠实度 | 37.4% | **57.5%** | ↑ +20.1% |
| 安全距离（工具） | 6/8 | **8/8** | ✅ 修复 |
| 安全距离（结论） | 0/8 | 0/8 | ⚠️ 仍失败 |
| 危险类别（工具） | 12/15 | **15/15** | ✅ 修复 |
| 危险类别（结论） | 4/15 | **0/15** | ❌ **倒退** |

#### 关键发现

1. **工具路由修复成功**：`GetSafetyDistance` 的 E004/E007 不再无工具调用，`CheckHazardCategory` 的 C009/C014 路由错误修复
2. **危险类别结论倒退 0/15**：新增 GB30000 文档被检索到，但 LLM 输出格式与结论验证器预期不匹配，导致全部判错
3. **安全距离仍 0/8**：GB50160 文档被检索到，但单文档包含所有距离 → `ExtractDistanceFromText` 的 `contextHint` 逻辑可能未正确生效
4. **综合评级**：工具触发率 × 参数准确率 × 结论准确率 = **75.7%**，评为"★★☆ 良好"

### 4.3 FC Function Calling 就绪性

| 用例 | 查询 | 预期工具 | 结果 |
|------|------|----------|------|
| 危险类别查询 | 苯属于什么危险类别 | CheckHazardCategory | ✅ |
| 储存兼容性检查 | 苯和丙酮能同库储存吗 | CheckStorageCompatibility | ✅ |
| 安全距离查询 | 甲类仓库与明火点的安全距离是多少 | GetSafetyDistance | ✅ |
| 通用工具（时间） | 现在几点 | GetCurrentTime | ❌ 未触发任何工具 |
| 多工具调用 | 甲醇和硝酸存放在同一个仓库是否合规 | CheckStorageCompatibility | ✅ |

**FC 就绪性**：4/5 → 通过 ✅（仅 `GetCurrentTime` 通用工具未触发，不影响核心业务）

---

## 五、性能瓶颈诊断

### 5.1 根因分析矩阵

| 瓶颈 | 严重度 | 影响范围 | 根因 |
|------|--------|----------|------|
| GB50160 文档缺失 | 🔴 致命 | 安全距离 0/8 | 知识库仅含 GB15603，缺少石油化工防火标准 |
| GB30000 子标准缺失 | 🔴 致命 | 危险类别 4/15 | 知识库只有 .txt 文件，GB30000 PDF 子标准未加载 |
| 工具路由错误 | 🟡 严重 | 安全距离 2/8 + 危险类别 3/15 | Function 描述引导 LLM 选错工具 |
| ExtractDistanceFromText 上下文不感知 | 🟡 严重 | 安全距离多值文档匹配 | 单文档含多个距离时始终返回第一个匹配 |
| RAG 查询构造 | 🟡 严重 | P@K 5.7%, MRR 0.222 | BM25 查询词与文档关键词不匹配 |
| 模型量化损失 | 🟠 中等 | 结论准确率 | Q4_K_M 量化可能影响推理精度 |
| 结论验证逻辑 | 🟠 中等 | 误判 | 结论验证器对 LLM 输出格式敏感 |

### 5.2 量化影响估算

```
                        修复前    修复工具路由后    修复知识库后    理想目标
工具触发率：            87.3% →   90.5%         → 92%+         → 95%+
危险类别（结论）：      4/15   →   0/15（倒退）  → 8-12/15      → 12/15+
安全距离（结论）：      0/8    →   0/8           → 6-8/8        → 7/8+
P@5：                  5.7%   →   5.7%          → 15%+         → 30%+
```

---

## 六、优化迭代记录

### 6.1 优化 P4-P7（commit `190236a`）

| 编号 | 类别 | 修改文件 | 内容 |
|------|------|----------|------|
| P4 | 知识库 | `knowledgebase/国标/GB50160 石油化工企业设计防火标准 - 防火间距表.txt` | **新建**：8 条评测用例对应的 GB50160 安全距离数据 |
| P5 | 知识库 | `knowledgebase/国标/GB30000 常见危险化学品分类对照表.txt` | **新建**：15 种化学品对应 GB30000 分类 + 28 个标准号对照表 |
| P6 | 代码 | `Agent1/Services/Compliance/ChemicalComplianceTools.cs` | 4 处修改：优化工具描述、RAG 查询构造、`ExtractDistanceFromText` 上下文感知 |
| P7 | 代码 | `Agent1/Services/Knowledge/KnowledgeBaseService.cs` | `_chemicalTerms` 新增 GB50160、防火间距等关键词 |

#### P6 关键代码变更

```csharp
// ✅ CheckHazardCategory 描述优化（修复 C009/C014 工具路由错误）
// 旧：查询指定危化品的危险类别及适用国标
// 新：查询指定危化品的危险类别、危险特性、GHS分类及适用国标（GB 30000 系列）

// ✅ GetSafetyDistance 查询构造（优化 BM25 匹配）
// 旧：$"{facilityType} 安全间距 距离 米"，topK=3
// 新：$"{facilityType} GB50160 防火间距 安全距离"，topK=5

// ✅ ExtractDistanceFromText 上下文感知（修复单文档多距离问题）
private static (double? distance, string unit, string source) 
    ExtractDistanceFromText(string text, string? contextHint = null)
{
    // 在 contextHint（facilityType）关键词附近窗口提取距离
    // 新增 "不应小于" 匹配 GB50160 标准表述
}
```

### 6.2 已知待解决问题

| 问题 | 优先级 | 方向 |
|------|--------|------|
| 危险类别 0/15 倒退 | 🔴 紧急 | 需排查结论验证器是否匹配 GB30000 文档输出格式 |
| 安全距离 0/8 未改善 | 🔴 紧急 | 需排查 `ExtractDistanceFromText` contextHint 是否生效 |
| GB30871-2022 幻觉 | 🟡 高 | 知识库缺少 GB30871 文档 |
| P@K 5.7% 无变化 | 🟡 高 | 知识文档 BM25 关键词匹配仍需调优 |
| 化学品属性查询 2/5 无工具 | 🟡 高 | LookupChemicalProperties 路由引导仍需优化 |

---

## 七、最终部署方案总结

### 7.1 服务架构

```
┌───────────────────────────────────────────────────┐
│                  AutoDL RTX 3090                    │
│                                                     │
│  ┌─────────────┐  ┌──────────────┐                 │
│  │ llama-server │  │ llama-server │                 │
│  │ (LLM)        │  │ (Embedding)  │                 │
│  │ :8080        │  │ :8081        │                 │
│  │ GPU -ngl 99  │  │ CPU -ngl 0   │                 │
│  │ Qwen3-8B     │  │ nomic-embed  │                 │
│  └──────┬───────┘  └──────┬───────┘                 │
│         │                  │                         │
│  ┌──────┴──────────────────┴───────┐                │
│  │        Agent1 (.NET 8)           │                │
│  │        :5000                     │                │
│  │  ┌──────────────────────────┐    │                │
│  │  │ ChemicalComplianceTools  │    │                │
│  │  │ KnowledgeBaseService     │    │                │
│  │  │ RAGModule                │    │                │
│  │  └──────────────────────────┘    │                │
│  └──────────────┬───────────────────┘                │
│                  │                                    │
│  ┌──────────────┴───────────────────┐                │
│  │    PostgreSQL 16 + pgvector      │                │
│  │    :5432                         │                │
│  │    vector(768) HNSW index        │                │
│  └──────────────────────────────────┘                │
└─────────────────────────────────────────────────────┘
```

### 7.2 启动命令序列

```bash
# 0. 前置准备
mkdir -p /root/autodl-tmp/logs
cd /root/autodl-tmp/agent-system

# 1. 启动 LLM 推理服务（:8080, GPU 全加速）
nohup /root/autodl-tmp/llama.cpp/build/bin/llama-server \
  -m /root/autodl-tmp/models/Qwen_Qwen3-8B-Q4_K_M.gguf \
  --host 0.0.0.0 --port 8080 \
  -ngl 99 -c 8192 \
  > /root/autodl-tmp/logs/llama-server.log 2>&1 &

sleep 15 && curl http://localhost:8080/health

# 2. 启动 Embedding 服务（:8081, CPU）
nohup /root/autodl-tmp/llama.cpp/build/bin/llama-server \
  -m /root/autodl-tmp/models/nomic-embed-text-v1.5.f16.gguf \
  --host 0.0.0.0 --port 8081 \
  --embeddings -ngl 0 -c 512 \
  > /root/autodl-tmp/logs/llama-embed.log 2>&1 &

sleep 5 && curl http://localhost:8081/health

# 3. 编译并启动 Agent1 API（:5000）
dotnet clean Agent1/Agent1.csproj
dotnet build Agent1/Agent1.csproj --no-incremental -c Release

DOTNET_ENVIRONMENT=Production \
ASPNETCORE_URLS=http://0.0.0.0:5000 \
JWT_KEY=your-jwt-key-at-least-32-chars \
DB_PASSWORD=changeme \
AUTH_ACCOUNTS_JSON='[{"Username":"admin","Password":"changeme","Role":"admin"},{"Username":"auditor","Password":"changeme","Role":"auditor"},{"Username":"viewer","Password":"changeme","Role":"viewer"}]' \
dotnet run --project Agent1
```

### 7.3 部署检查清单

| # | 检查项 | 命令 | 期望 |
|---|--------|------|------|
| 1 | LLM 服务健康 | `curl http://localhost:8080/health` | `{"status":"ok"}` |
| 2 | Embedding 服务健康 | `curl http://localhost:8081/health` | `{"status":"ok"}` |
| 3 | PostgreSQL 状态 | `pg_lsclusters` | `16 main 5432 online` |
| 4 | 模型文件存在 | `ls -lh /root/autodl-tmp/models/*.gguf` | 两个文件大小正确 |
| 5 | .NET 版本 | `dotnet --version` | `8.0.128` |
| 6 | API 服务健康 | `curl http://localhost:5000/health` | 200 OK |
| 7 | 数据库连接 | 检查日志 `agent1-*.log` | 无连接错误 |
| 8 | 知识库文件 | `ls knowledgebase/国标/GB50160* knowledgebase/国标/GB30000*` | 两个文件存在 |

---

## 八、经验教训与建议

### 8.1 部署经验法则

| 编号 | 法则 | 说明 |
|------|------|------|
| 1 | **"APT 优先于 curl 脚本"** | 国内环境安装 .NET SDK 优先用 APT，`dotnet-install.sh` 直连 CDN 极易超时 |
| 2 | **"sudo 不可用时用 su"** | `su - postgres -c "psql ..."` 替代 `sudo -u postgres` |
| 3 | **"PowerShell 用 $env"** | `$env:USERPROFILE` 而非 `%USERPROFILE%` |
| 4 | **".gitignore 防子目录误伤"** | `memory/` → `/memory/` 仅匹配根目录 |
| 5 | **"上传先 find"** | JupyterLab 上传后始终 `find / -name 'file*'` 定位 |
| 6 | **"flags 看全行"** | `grep flags /proc/cpuinfo \| head -1`，不用简化的 regex |
| 7 | **"下载走本地"** | 大文件始终走 Windows 本地下载 + JupyterLab 上传 |
| 8 | **"CMake 全显式"** | CUDA 编译必须同时指定 `GGML_CUDA=ON` + `CMAKE_CUDA_COMPILER` + `CMAKE_CUDA_ARCHITECTURES` |
| 9 | **"404 走 WebSearch"** | GGUF 文件名猜测极易出错，直接搜索 `模型名 GGUF bartowski` |
| 10 | **"嵌入用 F16"** | 嵌入模型不建议量化，全精度保证检索准确率 |
| 11 | **"SQL 用 /tmp"** | postgres 用户无法访问 /root，SQL 脚本复制到 /tmp/ |

### 8.2 评测经验总结

1. **知识库覆盖是 RAG 性能的天花板**：GB50160 缺失直接导致安全距离 0/8，知识库文档必须与评测用例对齐
2. **LLM 输出格式对结论验证器极其敏感**：GB30000 文档让 LLM 输出了正确结论但格式不匹配，导致 0/15 误判
3. **工具描述即路由规则**：Function Calling 本质是 Prompt Engineering，工具描述每句话都可能改变路由行为
4. **单文档多值场景需要上下文感知**：所有距离放一个文档时，`ExtractDistanceFromText` 必须根据 `facilityType` 缩小匹配窗口

### 8.3 硬件适配结论

| 方面 | RTX 3090 24GB 表现 | 评估 |
|------|-------------------|------|
| Qwen3-8B Q4_K_M 推理 | 流畅，GPU 全层加速 | ✅ 满足生产需求 |
| 8B 模型推理速度 | 评测 63 用例无超时 | ✅ 中等负载可接受 |
| nomic-embed CPU 嵌入 | 261.5MB 模型纯 CPU | ✅ 嵌入任务轻量 |
| Q4_K_M 量化精度损失 | 结论准确率 49-59% | ⚠️ 需进一步排查是量化还是系统问题 |
| Function Calling 兼容性 | 4/5 通过 | ✅ llama.cpp OpenAI API 兼容良好 |

---

> **文档版本**：v1.0  
> **创建日期**：2026-06-13  
> **下次任务**：修复危险类别结论倒退 0/15 问题，排查 ExtractDistanceFromText 未生效根因
