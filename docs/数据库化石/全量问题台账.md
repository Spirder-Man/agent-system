# 全量问题台账（数据库层）

> **创建日期**：2026-08-12
> **方法**：全量扫描 → 建台账 → 逐个核销 → 回查确认（防幻觉）
> **纪律**：任何问题必须经数据库 L0 回查验证后才能标记"已核销"，禁止仅凭代码推断
> **状态说明**：
> - 🔴 待核销：已发现问题，待全量数据验证
> - 🟡 验证中：已跑 SQL 验证，待代码溯源确认
> - 🟢 已核销：数据验证 + 代码溯源 + 修复完成，L0 回查确认
> - ⚪ 已关闭：确认不是问题（设计如此 / 误判）

---

## 台账总览

| # | 表 | 问题 | 严重性 | 状态 | 发现日期 | 核销日期 | 核销 SQL 证据 |
|---|----|------|--------|------|----------|----------|--------------|
| 1 | audit_logs | module 字段 1189/1189 全空（接口无参数） | P2 | 🔴 待核销 | 2026-08-12 | | |
| 2 | audit_logs | ip_address 1140 空 / 49 有值（全是测试 127.0.0.1） | P2 | 🔴 待核销 | 2026-08-12 | | |
| 3 | audit_logs | 4 条 IntegrationTest 绕过哈希链（chain_hash = null） | P2 | 🟢 已核销 | 2026-08-12 | 2026-08-13 | 测试改走 AuditService + RepairChainAsync 全链重算；本地 PG null=0 |
| 4 | audit_logs | detail 自由格式无法结构化查询 | P3 | 🔴 待核销 | 2026-08-12 | | |
| 5 | audit_logs | 哈希链可绕过（直插 _db.AddAuditLogAsync 不算哈希） | P1 | 🟢 已核销 | 2026-08-12 | 2026-08-13 | AddAuditLogAsync 内部强制补算 + 共享链算法；全链 1192/1226 条 null=0、VerifyIntegrity 通过 |
| 6 | chemical_documents | 表 0 行但序列到 1007（历史写入化石，索引空转） | P3 | 🟡 验证中 | 2026-08-12 | | |
| 7 | chemical_documents | 6 个重型索引（HNSW+GIN）但表空（资源浪费） | P3 | 🟡 验证中 | 2026-08-12 | | |
| 8 | chemical_documents | 作为兜底表但双层表已有数据，路径几乎不会被执行 | P3 | 🟡 验证中 | 2026-08-12 | | |
| 9 | chemical_documents | 退役三步走未执行（先拆代码→验证无引用→删表），回退期已过 26 天 | P2 | 🔴 待核销 | 2026-08-12 | | |
| 10 | knowledge_chunks | 50.9% 块（28/55）为“标准分享网”水印垃圾（PDF 提取失败+闸门盲区） | P1 | 🟡 验证中 | 2026-08-12 | | 闸门已修（黑名单规则④）；存量 28 块清除依赖批次 4 OCR 重灌 |
| 11 | knowledge_chunks | 溯源完整率 0%（无一块同时具备章节+条款+页码） | P2 | 🔴 待核销 | 2026-08-12 | | |
| 12 | knowledge_chunks | 蓝图承诺 UNIQUE(file_hash, chunk_index) 防重复未实现 | P3 | 🔴 待核销 | 2026-08-12 | | |
| 13 | knowledge_chunks | regulation_type 分类不一致（GB30000 总纲/GB15603=国标 vs 分册=化工专业条例） | P2 | 🔴 待核销 | 2026-08-12 | | |
| 14 | knowledge_documents | content_hash 34/34 空（增量更新检测从未生效） | P2 | 🔴 待核销 | 2026-08-12 | | |
| 15 | knowledge_documents | regulation_title / parent_category 34/34 空（幽灵字段） | P3 | 🔴 待核销 | 2026-08-12 | | |
| 16 | knowledge_documents | total_chunks 语义漂移（30871 切 22 块入库 1 块，字段=切块数≠入库数） | P3 | 🔴 待核销 | 2026-08-12 | | |
| 17 | long_term_memories | 重复记忆泛滥（470 条重复占 40.6%，"甲类仓库"重复 99 次，RecordAsync 无去重门禁） | P1 | 🟢 已核销 | 2026-08-13 | 2026-08-13 | Upsert 幂等写入（归一化内容/向量≥0.92 更新旧记忆）；活跃重复组=0 |
| 18 | long_term_memories | 冲突停用级联清空（94.3% 记忆被停用，1157→43，648 个唯一内容全停用） | P1 | 🟢 已核销 | 2026-08-13 | 2026-08-13 | 冲突停用收窄为归一化重复；008 迁移恢复活跃 664/1157（57.4%），活跃重复组=0 |
| 19 | long_term_memories | sessions 表 0 行，1157 条记忆 source_session_id 全部孤儿引用 | P2 | 🔴 待核销 | 2026-08-13 | | |
| 20 | long_term_memories | user_id 100% 空 + 51% 记忆 <20 字符（碎片化，多用户串扰风险） | P3 | 🟢 已核销 | 2026-08-13 | 2026-08-13 | 写入强制归一化 user_id；<16 字符碎片不再入长期记忆 |
| 21 | chemical_substances | 覆盖缺口（35 种 vs GB18218 重点监管 74 种，磷化氢/光气等缺失，查无此物降级 LLM 幻觉风险） | P2 | 🔴 待核销 | 2026-08-13 | | |
| 22 | chemical_substances | physical_state 11 种文本不统一（"气体（压缩/液化）"vs"气体（加压液化）"，无 CHECK 约束） | P3 | 🔴 待核销 | 2026-08-13 | | |
| 23 | chemical_substances | cas_number 无索引（LookupByCas 查询入口无索引，35 行无实害） | P3 | 🔴 待核销 | 2026-08-13 | | |
| 24 | chemical_aliases | 种子瑕疵："丙三醇"自指别名（=标准名）+ "氧气瓶"容器名入别名 | P3 | 🔴 待核销 | 2026-08-13 | | |
| 25 | chemical_incompatibilities | 硝酸→盐酸标"兼容"=王水（实际禁配，回答"可以同库"→错误安全建议） | P1 | 🟢 已核销 | 2026-08-13 | 2026-08-13 | 本地+远程 PG UPDATE 为 false + GB 15603；002/SQLite 种子同步 |
| 26 | chemical_incompatibilities | 硫化氢→二氧化硫标"兼容"（克劳斯反应 2H2S+SO2→3S+2H2O，应隔离） | P2 | 🔴 待核销 | 2026-08-13 | | |
| 27 | incompatible_categories | 82% 类别规则死规则（通俗词 vs GHS 术语词汇表错位，contains 匹配永不触发） | P2 | 🟢 已核销 | 2026-08-13 | 2026-08-13 | 匹配支持具体物质名/别名直命 + 同义词规范化，双向检查 |
| 28 | chemical_incompatibilities | regulation_ref 空 10/21（47.6%，无法规依据） | P3 | 🔴 待核销 | 2026-08-13 | | |
| 29 | chemical_incompatibilities | 苯↔丙酮双向冗余（无 UNIQUE(a,b)，种子双写对称对） | P3 | 🔴 待核销 | 2026-08-13 | | |
| 30 | incompatible_categories | 71 种文本同义不同写（氨/氨水、氯/氯气、碱/强碱）+ 无 UNIQUE 无索引裸表 | P3 | 🟢 已核销 | 2026-08-13 | 2026-08-13 | 007 迁移折叠 6 组同义词 + 去重 + UNIQUE(substance_id, incompatible_with)；重复组=0 |
| 31 | chemical_safety_distances | contains 模糊匹配泛化遮蔽特化（"储罐-建筑"25m 先命中遮蔽"液化烃储罐-办公楼"35m）+ 词序敏感 | P2 | 🟢 已核销 | 2026-08-13 | 2026-08-13 | 精确优先 + 最长条目优先；单测特化 35m 不被泛化 25m 遮蔽 |
| 32 | chemical_safety_distances | 裸表：facility_pair 无 UNIQUE 无索引 | P3 | 🔴 待核销 | 2026-08-13 | | |
| 33 | chemical_safety_distances | 覆盖稀疏（20 条 vs 规范全表几百组合）+ 特殊规则（200m）与通用规则混存 | P3 | 🔴 待核销 | 2026-08-13 | | |
| 34 | chemical_hazard_categories | 丙三醇无类别标注（符合 GHS 事实但兼容推断分支失效） | P3 | 🔴 待核销 | 2026-08-13 | | |
| 35 | chemical_hazard_categories | 裸表：无 UNIQUE(substance_id, category) 无索引 | P3 | 🔴 待核销 | 2026-08-13 | | |
| 36 | chemical_regulation_versions | contains+FirstOrDefault："GB 30000"总纲遮蔽"GB 30000.1-2024"（问 .1 最新版返回 2013） | P2 | 🟢 已核销 | 2026-08-13 | 2026-08-13 | 精确匹配优先 + 最长编号优先；单测 GB 30000.1 返回 2024 |
| 37 | chemical_regulation_versions | 裸表：regulation_number 无 UNIQUE | P3 | 🔴 待核销 | 2026-08-13 | | |
| 38 | chemical_regulation_versions | 档案 8 部法规仅 3 部有全文入库（GB 50160/50016/18218/617 无原文，合规引用源头断料） | P3 | 🔴 待核销 | 2026-08-13 | | |
| 39 | refresh_tokens | 过期令牌无限堆积（427/434 已过期、420 条超 7 天、最老 7-02，无任何清理任务） | P3 | 🔴 待核销 | 2026-08-13 | | |
| 40 | search_logs | 幽灵表：0 行 + 全项目零写入代码（检索观测设计完备从未接线，检索性能/质量分析缺数据） | P3 | 🔴 待核销 | 2026-08-13 | | |
| 41 | drift_details | probe_key 空 7/8（006 增列无历史回填，明细追不到具体探针问题） | P3 | 🔴 待核销 | 2026-08-13 | | |
| 42 | drift_probes | 漂移监测运行量 demo 级（4 批全来自 e2e-demo），probe_a1 抓到漂移率 1.0 无处置闭环 | P3 | 🔴 待核销 | 2026-08-13 | | |

**统计**：P1 = 5 条，P2 = 13 条，P3 = 24 条，总计 42 条 | 已核销 10 条（#3/#5/#17/#18/#20/#25/#27/#30/#31/#36） | 核销率 23.8%（2026-08-13 修正：原统计 43/P3=25 为笔误，明细逐行核对实为 42 条）

---

## 问题详情

### #1 audit_logs.module 字段全空

**发现**：全量分组统计 `count(*) FILTER (WHERE module IS NOT NULL)` = 0/1189

**根因**：`IAuditService.LogOperationAsync`、`IDatabaseService.AddAuditLogAsync` 两个接口签名里都没有 `module` 参数，只有建表 SQL 预留了列

**影响**：审计的"在哪个功能里干的"维度完全缺失，无法按功能模块筛选审计记录

**修复方案**（待讨论）：
- 方案 A：给接口加 `module` 参数，所有调用点补传（工作量大，影响全链路）
- 方案 B：保持现状，从 action/detail 里推断功能归属（不修表结构，接受设计残留）
- 方案 C：标记为废弃列（`COMMENT ON COLUMN audit_logs.module IS 'DEPRECATED: 接口未实现'`）

**核销验证 SQL**（待修复后执行）：
```sql
SELECT count(*) FILTER (WHERE module IS NOT NULL AND module <> '') AS module有值,
       count(*) AS 总行数 FROM audit_logs;
-- 预期：module有值 > 0（证明修复后开始写入）
```

---

### #2 audit_logs.ip_address 半实现

**发现**：全量分组统计 1140 空 / 49 有值（全是 127.0.0.1 = 测试）

**根因**：接口有 `ipAddress` 参数，但 AuditService 注释说"由调用方通过 details 传递"——生产调用点没人传

**影响**：审计的"从哪台电脑干的"能力空转，参照控制点仍未覆盖（如生产 IP）

**修复方案**（待讨论）：
- 方案 A：AuditService 自动从 HttpContext 获取 IP（HttpContextAccessor 注入）
- 方案 B：保持现状，接受 IP 不记录（测试环境够用）
- 方案 C：从日志中间接提取（Nginx 日志 + TraceId 关联）

**核销验证 SQL**（待修复后执行）：
```sql
SELECT count(*) FILTER (WHERE ip_address IS NOT NULL AND ip_address <> '127.0.0.1') AS 生产IP,
       count(*) AS 总行数 FROM audit_logs;
-- 预期：生产IP > 0（证明修复后开始记录真实 IP）
```

---

### #3 4 条测试记录无哈希

**发现**：全量分组统计 `length(chain_hash)` 分布，发现 1185 有哈希 / 4 无哈希

**根因**：DatabaseIntegrationTests.cs 直插 `_db.AddAuditLogAsync(...)`，绕过 AuditService，chainHash 不传 = null

**影响**：哈希链有 4 个缺口；VerifyIntegrityAsync 用 `ChainHash != null` 跳过，不报错

**修复方案**（待讨论）：
- 方案 A：测试改为通过 AuditService 写入（走正常路径）
- 方案 B：验证逻辑对 null 哈希报警（不跳过，而是标记为"未覆盖"）
- 方案 C：给测试专用记录打标记（`action = 'TestAudit'`），验证时排除

**核销验证 SQL**（待修复后执行）：
```sql
SELECT count(*) FILTER (WHERE chain_hash IS NULL OR chain_hash = '') AS 无哈希
FROM audit_logs WHERE action != 'IntegrationTest';
-- 预期：无哈希 = 0（排除测试后全量覆盖）
```

**修复状态**（2026-08-13 批次1）：🟢 已修复
- DatabaseIntegrationTests / DataRetentionTests 已改走 AuditService.LogOperationAsync（方案 A）
- 历史 4 条 NULL 记录已通过 RepairChainAsync 全量修复
- 核销记录：VerifyIntegrityAsync 通过；pgcrypto 独立复刻全链比对 match=1192 / mismatch=0 / null=0

---

### #4 detail 自由格式无法结构化查询

**发现**：detail 是 `\|` 分隔的键值串（`查询:xxx|工具:xxx|TraceId=xxx`）

**影响**：想查"过去一周有多少次工具调用"只能用 `LIKE '%工具:%'`（慢、不准）

**修复方案**（待讨论）：
- 方案 A：拆成结构化列（tool_name, tool_count, trace_id, hallucination_count…）
- 方案 B：保持 text，建 GIN 索引加速全文搜索
- 方案 C：保持现状，接受灵活性优先

**核销验证 SQL**（暂不需要，设计选择问题）

---

### #5 哈希链可绕过（合规风险）

**发现**：直插 `_db.AddAuditLogAsync` 可写入无哈希记录，验证逻辑不报错

**影响**：如果有人恶意插入/删除日志，验证只能发现"这条没哈希"，但无法区分是"测试遗留"还是"恶意篡改"

**修复方案**（待讨论）：
- 方案 A：AddAuditLogAsync 内部强制算哈希（不接受外部传 null）
- 方案 B：验证逻辑对 null 哈希报警（`return (false, log.Id, "未覆盖哈希链")`）
- 方案 C：数据库层加触发器（INSERT 时自动算哈希）

**核销验证 SQL**（待修复后执行）：
```sql
-- 验证：尝试直插无哈希记录，应被拒绝或自动补算
-- 需在代码修复后测试
```

**修复状态**（2026-08-13 批次1）：🟢 已修复（方案 A + B 同时落地）
- 方案 A：AddAuditLogAsync 内部链尾读回 + 强制补算（链算法提取为 AuditChainHash 共享类，杜绝双份漂移）；DatabaseIntegrationTests 新增 `AddAuditLog_DirectInsertWithoutChainHash_InternallyFillsHash` 断言直插后 ChainHash 非空
- 方案 B：VerifyIntegrityAsync 遇 null 哈希报警 `(false, id, "该记录未覆盖哈希链（chain_hash 为空）")`；AuditServiceHashChainTests 新增报警/修复两个测试
- 附带发现并修复：GetAuditLogsAsync 的 LIMIT 1000 截断导致验证/修复仅覆盖最近 1000 条 → 新增 GetAllAuditLogsAsync 全量读回，VerifyIntegrityAsync / RepairChainAsync 已切换
- 核销记录：RepairChainAsync 全链重算后 VerifyIntegrityAsync 通过；pgcrypto 独立复刻全链比对 match=1192 / mismatch=0 / null=0

---

### #9 chemical_documents 退役三步走未执行

**发现**：双层表架构 2026-07-17 上线，至今 26 天；旧表 0 行、序列 1007，两处兜底（VectorSearchAsync L902 / GetAllChemicalDocumentTextsAsync L1043）从未触发（新表一直非空），写入方法 AddChemicalDocumentAsync 已标记 [Obsolete]

**根因**：改造蓝图 P6（2026-07-17）"保留旧表作为回退"只定义了回退，未定义退役条件与回退期——绞杀者模式缺最后一步

**影响**：僵尸表持续占用 6 个索引（HNSW+GIN）+ 两处兜底代码 + 启动计数逻辑；拖累后续架构演进的可读性

**修复方案**（退役三步走，顺序不可反）：
- 步骤 1：拆代码——删除两处兜底（L902-943、L1043-1070）、Obsolete 写入方法、启动计数 GetChemicalDocumentCountAsync
- 步骤 2：验证——全仓 grep 确认无 chemical_documents 引用（代码 + 测试 + SQL）
- 步骤 3：删表——DROP TABLE chemical_documents（6 索引随表消失），同步更新 init_database.sql 与速查手册

**核销验证 SQL**（步骤 3 后执行）：
```sql
SELECT count(*) FROM pg_tables
WHERE schemaname = 'public' AND tablename = 'chemical_documents';
-- 预期：0（表已不存在）；另需 grep 全仓确认无代码引用
```

---

### #10 knowledge_chunks 水印垃圾块 50.9%（P1 全库最严重）

**发现**：全量分组统计 28/55 块（28 个 GB30000.2~.29 文档的唯一一块）内容 = `标准分享网 www.bzfxw.com 免费下载+`（99 字符）

**根因链（五环全通）**：
- ① PDF 扫描版 → 提取器只拿到水印页（99 字符）
- ② SemanticChunker 对 99 字符只切 1 块（chunk_index=0）
- ③ GarbledTextDetector 规则盲区：中文占比 37.5% > 20% 阈值 → 规则③放行（"标准分享网"9 汉字/24 字符；异常字符 4% < 40% → 规则②放行；无连续重复 → 规则①放行）
- ④ RejectGarbledChunk 闸门（ChemicalRAG.cs L822）未拦截 → 入库
- ⑤ documents 层标记 partial（28/28）却未触发拒收/OCR 动作（2026-08-12 documents 精读补齐）

**影响**：检索"苯怎么存"可能命中"标准分享网免费下载"；合规回答引用的是水印文本——RAG 数据底座一半是垃圾；**28 个文档平均 19.3 页 ≈ 540 页 GB 30000 法规内容实际丢失**（提取失败且无 OCR 重灌）

**修复方案**（待讨论）：水印黑名单 / 有效内容长度规则（<200 字符拒收）/ 28 个扫描版 PDF 走 OCR 重灌

**核销验证 SQL**（修复后执行）：
```sql
SELECT count(*) FILTER (WHERE content LIKE '%标准分享网%') AS 水印块,
       count(*) AS 总行数 FROM knowledge_chunks;
-- 预期：水印块 = 0
```

**修复状态**（2026-08-13 批次1）：🟡 闸门已修 / 重灌待批次 4
- GarbledTextDetector 新增规则④水印黑名单（标准分享网 / www.bzfxw.com / 免费下载 子串匹配），命中 reason="水印特征" 拒收；测试正例（水印块=True）+ 反例（仅含"下载"词=False）各 1 条
- "有效内容长度 <200 拒收"未采用——与既有测试契约 `IsGarbled_ShortNormalChineseText_ReturnsFalse` 冲突
- 存量 28 个水印块清除依赖批次 4 OCR 重灌，届时执行本条目核销 SQL
- 2026-08-13 补充：DatabaseIntegrationTests 整表清理曾误删本地知识库（Bug-048），已从 `db/backups/dump_20260811.dump` 恢复 `knowledge_documents=34 / knowledge_chunks=55`，并修复测试为标记化清理；存量水印状态不变。

---

### #11 knowledge_chunks 溯源完整率 0%

**发现**：全量排查 55 块无一块同时具备 chapter_title + clause_number + page_number；有效 27 块中也仅 11 有章节、16 有条款、10 有页码

**根因**：SemanticChunker 的章节/条款解析依赖文本结构（"第X条"/"X.X" 模式），大部分提取文本无清晰结构 → 字段留空；写入时 `c.ChapterTitle ?? ""`（ChemicalRAG.cs L728 等）

**影响**：双层表 7 个核心价值中"溯源"一环名存实亡——合规回答的章节/页码引用能力空转

**修复方案**（待讨论）：
- 方案 A：Chunker 后处理增强（正则回填章节/条款）
- 方案 B：LLM 结构化抽取（成本高）
- 方案 C：接受现状，引用降级为"文件名+条文摘要"

**核销验证 SQL**（修复后执行）：
```sql
SELECT count(*) FILTER (WHERE chapter_title IS NOT NULL AND chapter_title<>'' AND
                          clause_number IS NOT NULL AND clause_number<>'' AND
                          page_number IS NOT NULL) AS 溯源完整
FROM knowledge_chunks;
-- 预期：> 0（逐轮提升）
```

---

### #12 knowledge_chunks 防重复约束未实现

**发现**：commit 72271fae 声称"联合唯一约束 UNIQUE(file_hash, chunk_index) 防止重复写入"，实测约束仅 pkey + document_id_fkey，无 file_hash 列

**根因**：file_hash 属 documents 层（content_hash），跨表无法建唯一约束；实现改为 source_path UNIQUE（文档级幂等），分块级幂等缺失

**影响**：同文件重灌会在 chunks 产生重复行（无 ON CONFLICT 处理）

**修复方案**（待讨论）：方案 A：加 (document_id, chunk_index) UNIQUE；方案 B：重灌前先按 document_id 删除旧块（RemoveFileFromKnowledgeBaseAsync 已有雏形）

**核销验证 SQL**（修复后执行）：
```sql
SELECT document_id, chunk_index, count(*) FROM knowledge_chunks
GROUP BY 1,2 HAVING count(*) > 1;
-- 预期：0 行（无重复）
```

---

### #13 knowledge_chunks 分类不一致

**发现**：GB 30000 系列 28 个分册 = "化工专业条例"，但 GB30000-2013 总纲 + GB15603-2022 = "国标"（同标准家族两套标签）

**根因**：目录×类型交叉实测 1:1 完全一致——代码映射（ChemicalRAG.cs L280-283）忠实执行；**问题在知识库文件物理摆放**：28 个 GB 30000 分册（国家标准）被放进"化工专业条例"目录，GB15603 + GB30000 总纲在"国标"目录，同一标准家族被物理拆到两个目录（2026-08-12 documents 精读钉死）

**影响**：检索过滤 `WHERE regulation_type='化工专业条例'` 会漏掉总纲和 GB15603

**修复方案**（待讨论）：调整知识库物理目录（28 个 GB 30000 分册移入"国标/"）后重灌；或改映射逻辑

**核销验证 SQL**（修复后执行）：
```sql
SELECT regulation_type, count(*) FROM knowledge_chunks GROUP BY 1 ORDER BY 2 DESC;
-- 预期：GB 30000 家族不再分裂在两类
```

---

### #14 knowledge_documents content_hash 全空（增量更新失效）

**发现**：全量排查 34/34 行 content_hash 为空；last_modified / file_size 正常写入

**根因**：InsertDocumentForFileAsync（ChemicalRAG.cs L415-430）构造 KnowledgeDocumentRecord 时未给 ContentHash 赋值——代码从未计算 SHA-256

**影响**：init_database.sql L119 注释"content_hash 用于增量更新检测"从未生效：文件内容变化无法被哈希检测，增量更新只能依赖 last_modified（可能漏判内容改动）

**修复方案**（待讨论）：插入前计算文件 SHA-256 写入；重灌既有 34 文档补哈希

**核销验证 SQL**（修复后执行）：
```sql
SELECT count(*) FILTER (WHERE content_hash IS NULL OR content_hash='') AS 哈希空
FROM knowledge_documents;
-- 预期：0
```

---

### #15 knowledge_documents 幽灵字段（标题/父目录全空）

**发现**：regulation_title 34/34 空、parent_category 34/34 空

**根因**：调用点（ChemicalRAG.cs L715-717 等）从未传 regulationTitle / parentCategory 参数

**影响**：法规标题需从 file_name 推断；H166 层级路径（parent_category）无法追溯文档归属

**修复方案**（待讨论）：方案 A：文件名解析回填 regulation_title；方案 B：接受现状（file_name 已含大部分信息）

**核销验证 SQL**（修复后执行）：
```sql
SELECT count(*) FILTER (WHERE regulation_title IS NOT NULL AND regulation_title<>'') AS 标题有值
FROM knowledge_documents;
-- 预期：> 0
```

---

### #16 knowledge_documents total_chunks 语义漂移

**发现**：30871-2022（id 1294）字段值 22 vs 实际入库 1 块（差 21）；其余 33 文档一致

**根因**：UpdateDocumentChunkCountAsync 写入的是**切块数**（chunks.Count=22），而 RejectGarbledChunk 拒收了 21 块乱码（拒收是正确行为）——计数含被拒块

**影响**：total_chunks 失去"可检索块数"语义；前端展示/统计失真。（对照：30871 的 21 块被拒说明闸门对"乱码"有效——与 #10 水印放行形成对照：闸门拦乱码，不拦"有意义的水印"）

**修复方案**（待讨论）：统计入库块数（AddDocumentAsync 成功数）而非切块数

**核销验证 SQL**（修复后执行）：
```sql
SELECT kd.id, kd.total_chunks - count(kc.id) AS 差值 FROM knowledge_documents kd
LEFT JOIN knowledge_chunks kc ON kc.document_id = kd.id
GROUP BY kd.id, kd.total_chunks HAVING kd.total_chunks - count(kc.id) <> 0;
-- 预期：0 行
```

---

### #17 long_term_memories 重复记忆泛滥

**发现**：470 条重复（40.6%）："甲类仓库" 99 条（99 个不同写入时刻，跨 6-29~7-29 逐次写入）、"GB 50140-2005" 56 条、"GB 50160 §5.2.1" 35 条

**根因**：RecordAsync 直接 INSERT 无幂等/去重检查（对照 knowledge_chunks 有 source_path UNIQUE 文档级幂等）；FactExtractor 无归一化（同一法规多粒度："GB 50140-2005" vs "GB 50140-2005《建筑灭火器配置验收及检查规范》"）；ResolveConflictsAsync 是"事后停用"非"事前防重"且未收敛（99 条停 97 留 2 条活跃）

**影响**：表体积膨胀 40%；语义检索命中重复结果；重要度/命中统计失真

**修复方案**（待讨论）：写入前查重（content 完全匹配或 embedding 余弦 > 0.95 则跳过/更新）；FactExtractor 增加归一化（法规编号提取）

**修复状态**（2026-08-13 批次2）：🟢 已修复
- `UpsertLongTermMemoryAsync` 幂等写入：同用户+同类型下，归一化内容相等或向量相似度 ≥ 0.92 → 更新旧记忆，不新增重复；
- 008 迁移把历史重复折叠为每组最新一条活跃（活跃重复组=0），旧重复行保留 `is_active=false` 作归档，不物理删除。

**核销验证 SQL**（修复后执行）：
```sql
SELECT count(*) AS 总行数, count(DISTINCT content) AS 去重内容数,
       count(*) - count(DISTINCT content) AS 重复条数
FROM long_term_memories;
-- 预期：重复条数 = 0
```

---

### #18 long_term_memories 冲突停用级联清空

**发现**：96.3%（1114/1157）记忆被停用；活跃 43 条全部创建于 7-29 当天；687 个唯一内容中 648 个全部停用，仅 39 个含活跃版本

**根因**：ResolveConflictsAsync → DeactivateConflictingMemoriesAsync 语义相似度阈值过宽——每条新记忆写入都停用大量旧记忆（停用与写入逐日同步：7-20 写 458 停 467、7-24 写 189 停 196），级联清空一个月沉淀；且无"新记忆确实更好"的校验

**影响**：一个月沉淀的事实点 94.3% 阵亡；RetrieveAsync 实际检索范围只剩 43 条；记忆系统名存实亡

**修复方案**（待讨论）：冲突停用改为"合并/更新"而非停用；提高相似度阈值；限制单次停用上限；或引入重要性对比（新记忆重要性 < 旧记忆则不停用）

**修复状态**（2026-08-13 批次2）：🟢 已修复
- `DeactivateConflictingMemoriesAsync` 收窄为“归一化内容完全相同的重复版本”，不再按 `memory_type` 整组停用；
- `RecordAsync` 改走 `UpsertLongTermMemoryAsync`，写入即合并/更新，不再先停用旧记忆；
- 008 迁移恢复每归一化内容最新版为活跃：**总 1157 / 活跃 664（57.4%）**，活跃重复组=0。

**核销验证 SQL**（修复后执行）：
```sql
SELECT is_active, count(*) FROM long_term_memories GROUP BY 1;
-- 预期：活跃占比显著回升（> 50%）
```

---

### #19 long_term_memories sessions 孤儿引用

**发现**：1157 条 source_session_id 全非空，但 350 个不同 session_id 在 sessions 表均不存在（sessions 表存在但 0 行）

**根因**：会话持久化从未接线——短期记忆活在内存 ConcurrentDictionary（MemoryService._sessions），sessions 表"建了但没人写"。**代码级钉死**（2026-08-13 sessions 精读）：AgentDialog.SaveSessionAsync（L927-931）只调 `_sessionService.AddDialogTurn`（内存），从不 INSERT sessions 表；全项目无 `INSERT INTO sessions` 语句

**影响**：记忆不可追溯"哪次对话来的"；会话级隐私删除（记忆随会话删除）无法实现；sessions 表成为幽灵表

**修复方案**（待讨论）：确认 sessions 表是否还需要（方案 A：接线持久化；方案 B：废弃并移除 source_session_id 依赖）

**核销验证 SQL**（修复后执行）：
```sql
SELECT count(DISTINCT lt.source_session_id) AS 记忆引用的会话数,
       count(DISTINCT s.id) AS sessions表会话数,
       count(*) FILTER (WHERE lt.source_session_id IS NOT NULL AND s.id IS NULL) AS 孤儿数
FROM long_term_memories lt LEFT JOIN sessions s ON s.id = lt.source_session_id;
-- 预期：孤儿数 = 0（或方案 B 下迁移清理后无引用）
```

---

### #20 long_term_memories user_id 全空 + 记忆碎片化

**发现**：user_id 1157/1157 全空（默认 default）；51%（591 条）记忆内容 <20 字符（纯法规编号如 "GB 50140-2005"）；62%（720 条）零命中

**根因**：user_id 从未写入（模型默认 "default"，与审计日志 100% 匿名呼应）；FactExtractor 提取事实无最小长度/上下文要求

**影响**：多用户场景记忆串扰（A 用户检索可能命中 B 用户记忆）；碎片化记忆检索价值低

**修复方案**（待讨论）：写入时强制填充 user_id；FactExtractor 增加最小内容长度门槛（如 >15 字符）或自动补全上下文

**修复状态**（2026-08-13 批次2）：🟢 已修复（写入路径）
- `RecordAsync` 强制 `user_id` 非空（默认 `default`），避免多用户串扰；
- 内容 `<16` 字符的碎片事实不再入长期记忆（日志记录跳过原因）；
- 存量碎片保留不删，后续核销以“新写入碎片占比”为准。

**核销验证 SQL**（修复后执行）：
```sql
SELECT count(*) FILTER (WHERE user_id = '' OR user_id IS NULL) AS 空用户,
       count(*) FILTER (WHERE length(content) < 20) AS 碎片记忆
FROM long_term_memories;
-- 预期：空用户 = 0；碎片记忆占比显著下降
```

---

### #21 chemical_substances 覆盖缺口

**发现**：35 种物质 vs GB18218 重点监管危化品 74 种——覆盖不足一半；磷化氢、光气、氰化氢、黄磷、液化石油气等常见重点监管物质缺失

**根因**：002_chemical_knowledge_graph.sql 种子数据固定 35 种（7-28 单事务导入）；AddSubstance 存在但从未被运行时调用，表是静态只读档案

**影响**："查无此物"→ 降级 RAG/LLM，有幻觉风险（用户问"光气怎么存"会走 LLM 自由发挥）；别名兜底有效（液氨→氨、氯气→氯），但只能兜已收录物质

**修复方案**（待讨论）：扩充种子数据至 GB18218 重点监管 74 种；或提供管理端 AddSubstance 写入通道

**核销验证 SQL**（修复后执行）：
```sql
SELECT count(*) AS 物质总数 FROM chemical_substances;
-- 预期：>= 74（覆盖 GB18218 重点监管危化品）
```

---

### #22 chemical_substances physical_state 文本不统一

**发现**：11 种状态表述："气体（压缩/液化）"vs"气体（加压液化）"同义不同写；"液体（甲醛溶液）""液体（氯化氢水溶液）"把物质名混入状态字段；无 CHECK 约束

**影响**：按状态过滤/统计不精确（当前仅展示用途，危害低）

**修复方案**（待讨论）：统一枚举（液体/固体/气体（压缩）/气体（液化）/气体（溶解）等）并加 CHECK 约束

**核销验证 SQL**（修复后执行）：
```sql
SELECT physical_state, count(*) FROM chemical_substances GROUP BY 1;
-- 预期：状态种类 <= 5 且无物质名混入
```

---

### #23 chemical_substances cas_number 无索引

**发现**：LookupByCas 是查询入口（ChemicalSubstanceDatabase.LookupByCas）但 cas_number 无索引（仅 pkey + name UNIQUE）

**影响**：35 行量小无实害；扩展至数百种后全表扫描变慢

**修复方案**（待讨论）：CREATE INDEX idx_substances_cas ON chemical_substances(cas_number)

**核销验证 SQL**（修复后执行）：
```sql
SELECT indexname FROM pg_indexes WHERE tablename='chemical_substances';
-- 预期：包含 idx_substances_cas
```

---

### #24 chemical_aliases 种子瑕疵别名

**发现**：全量 83 条别名中 2 条瑕疵——"丙三醇"→丙三醇（别名 = 标准名，自指冗余）；"氧气瓶"→氧气（容器名当物质别名，"氧气瓶"是设备不是化学品）

**根因**：002_chemical_knowledge_graph.sql 种子数据直接写入（83 条随 35 种物质单事务导入），种子时未做自指/语义校验；运行时 AddSubstance/AddAlias 均受 UNIQUE(substance_id, alias_text) 保护，不会产生此类新数据

**影响**：低——自指别名浪费 1 行且检索时可能自命中；"氧气瓶"命中氧气反而提供便利（查询"氧气瓶怎么存"可命中）；不构成安全风险

**修复方案**（待讨论）：002 种子删除"丙三醇"自指别名；"氧气瓶"可保留（便利性）或删除（严格性）

**核销验证 SQL**（修复后执行）：
```sql
SELECT a.alias_text FROM chemical_aliases a
JOIN chemical_substances s ON s.id = a.substance_id
WHERE a.alias_text = s.name;
-- 预期：0 行（无自指别名）
```

---

### #25 硝酸→盐酸 标"兼容"（王水！）

**发现**：incompatibilities 中 硝酸→盐酸 is_compatible=TRUE，reason="两种强酸可同库分区存放"

**根因**：002 种子人工编写 21 条规则时未复核 GB 15603 配存表——硝酸+盐酸=王水（混合放热、产生氯气/亚硝酰氯剧毒气体、可溶金铂），实际为**禁配**

**影响**：用户问"硝酸和盐酸能一起存吗"→ 系统回答"可以同库"→ **直接给出错误的安全建议**（全库唯一"答错要出事故"的表答错了）；同类目兼容推断（L349 类别重叠）不救场——硝酸与盐酸 GHS 类别不同

**修复方案**（待讨论）：改为 is_compatible=false，reason 填王水反应说明，补 regulation_ref=GB 15603；顺带专家复核全部 21 条规则

**核销验证 SQL**（修复后执行）：
```sql
SELECT sa.name, sb.name, i.is_compatible FROM chemical_incompatibilities i
JOIN chemical_substances sa ON sa.id = i.substance_a_id
JOIN chemical_substances sb ON sb.id = i.substance_b_id
WHERE (sa.name='硝酸' AND sb.name='盐酸') OR (sa.name='盐酸' AND sb.name='硝酸');
-- 预期：is_compatible = false
```

**修复状态**（2026-08-13 批次1）：🟢 已修复（三拷贝点）
- 002 L667 → FALSE + 王水 reason + GB 15603 ✅
- ChemicalDatabaseService L875（SQLite 种子）→ 0 + 同步 reason ✅
- 本地 PG `fix_25_aqua_regia.sql` UPDATE 1 ✅；核销 SQL 回查：`硝酸|盐酸|f|硝酸+盐酸=王水…严禁同库混存|GB 15603` ✅
- 远程 PG（AutoDL）`remote_fix_25c.ps1` 文件传输方案 UPDATE 1 ✅；核销回查：`18	false	GB 15603	硝酸+盐酸=王水…`（hex 解码）✅
- 专家复核全部 21 条规则：待用户化学专家结论（同 #26）

---

### #26 硫化氢→二氧化硫 标"兼容"（克劳斯反应）

**发现**：硫化氢→二氧化硫 is_compatible=TRUE（"同属酸性气体可同库"）

**根因**：002 种子人工编写；未考虑 2H2S+SO2→3S+2H2O（克劳斯工艺核心反应，放热）——两者相遇存在反应性

**影响**：中等——用户可能据此将 H2S 与 SO2 同库；H2S 为易燃有毒气体，反应放热

**修复方案**（待讨论）：待专家复核；倾向改为隔离储存或补充条件说明

**核销验证 SQL**（修复后执行）：
```sql
SELECT sa.name, sb.name, i.is_compatible FROM chemical_incompatibilities i
JOIN chemical_substances sa ON sa.id = i.substance_a_id
JOIN chemical_substances sb ON sb.id = i.substance_b_id
WHERE (sa.name='硫化氢' AND sb.name='二氧化硫') OR (sa.name='二氧化硫' AND sb.name='硫化氢');
-- 预期：专家复核结论（倾向 false）
```

---

### #27 82% 类别规则死规则（词汇表错位）

**发现**：193 条 incompatible_categories 中 ~158 条（82%）永不触发——incompatible_with 是通俗词/具体物质名（硝酸 10 条、氨 7 条、高锰酸钾 6 条、过氧化氢 5 条…），而 CheckCompatibility L332-333 的 contains 匹配对象是 HazardCategories.Category（GHS 术语：氧化性固体/液体/气体等）

**根因**：002 种子两套词汇表未对齐（通俗词 vs GHS 术语）；仅 ~5 词（氧化剂/易燃液体/易燃固体/金属/氧）碰巧可触发（含"氧"字等）；无单元测试覆盖类别级判定路径

**影响**：用户问"高锰酸钾和硝酸能一起存吗"，若精确表无此边→类别级判定漏判→可能给出"无禁忌"结论；规则表形同虚设

**修复方案**（待讨论）：方案 A：incompatible_with 统一改为 GHS 术语词表（对齐 hazard_categories.category）；方案 B：类别级判定改为查表（新增"类别→类别"禁忌映射）；方案 C：将死规则中的具体物质名转为精确表边（193→21 扩充）

**修复状态**（2026-08-13 批次2）：🟢 已修复
- `CheckCompatibility` 新增“具体物质名/别名直命”分支 + 双向检查，不再只对 GHS 类别做 contains；
- 匹配前统一同义词（氨水→氨、氯气→氯、铝粉→铝、氢气→氢、强碱→碱、强酸→酸）；
- 单测：氢氟酸禁忌词“氨水”命中物质“氨”→ 返回禁配。

**核销验证 SQL**（修复后执行）：
```sql
WITH cats AS (SELECT DISTINCT category FROM chemical_hazard_categories)
SELECT count(*) AS 死规则数 FROM chemical_incompatible_categories ic
WHERE NOT EXISTS (
  SELECT 1 FROM cats c
  WHERE c.category LIKE '%'||ic.incompatible_with||'%' OR ic.incompatible_with LIKE '%'||c.category||'%');
-- 预期：0
```

---

### #28 精确规则无法规依据（47.6%）

**发现**：regulation_ref 空 10/21——"苯乙烯→过氧化氢""铝粉→硝酸铵""氰化钠→盐酸"等高风险规则均无法规依据

**根因**：002 种子编写时仅部分规则附 GB 15603 引用（对照 knowledge_chunks 溯源 0% #11——全库法规溯源通病）

**影响**：合规回答无法给出条文引用，监管审计时无法溯源

**修复方案**（待讨论）：补齐 regulation_ref（GB 15603 配存表 / GB 18218）；无法规可依的标注"行业通识"

**核销验证 SQL**（修复后执行）：
```sql
SELECT count(*) FILTER (WHERE regulation_ref IS NULL OR regulation_ref='') AS 空引用
FROM chemical_incompatibilities;
-- 预期：0
```

---

### #29 苯↔丙酮双向冗余 + 无 UNIQUE(a,b)

**发现**：21 行中唯一一对反向重复（苯→丙酮 与 丙酮→苯 同时存在）；incompatibilities 无 UNIQUE(substance_a_id, substance_b_id)

**根因**：002 种子双写对称对；建表时未加复合唯一（代码层 L323-324 双向查找覆盖后加载行，功能无害但数据库冗余）

**影响**：低——内存字典折叠；但运行时若双向写入将持续膨胀

**修复方案**（待讨论）：加 UNIQUE(substance_a_id, substance_b_id) + 删除冗余行；或约定只存单方向（a_id < b_id）

**核销验证 SQL**（修复后执行）：
```sql
SELECT count(*) FROM chemical_incompatibilities i
JOIN chemical_incompatibilities r ON r.substance_a_id=i.substance_b_id AND r.substance_b_id=i.substance_a_id
WHERE i.id < r.id;
-- 预期：0（另需 pg_constraint 确认 UNIQUE 存在）
```

---

### #30 categories 文本规范 + 裸表

**发现**：71 种 incompatible_with 文本同义不同写（氨/氨水、氯/氯气、铝/铝粉、氢/氢气、碱/强碱、酸/强酸）；"水（遇水可能释放HCN）"解释性文本混入对象名；表无 UNIQUE、无 substance_id 索引（仅 pkey）

**根因**：002 种子人工文本无枚举约束；建表只建了 FK

**影响**：统计/去重失真；同义不同写导致未来对齐 GHS 术语（#27 修复）时映射工作量翻倍

**修复方案**（待讨论）：统一词表（随 #27 一起做）；加 UNIQUE(substance_id, incompatible_with)

**修复状态**（2026-08-13 批次2）：🟢 已修复
- 007 迁移：折叠 6 组同义词 → 删除折叠后重复行 → `UNIQUE(substance_id, incompatible_with)`；
- 本地 PG 执行结果：UPDATE 13、DELETE 1、重复组=0、唯一约束已建；
- 002 种子同步改为规范词（硫化氢→氯、氢氟酸→氨、三氯甲烷→碱、氧气→氢）。

**核销验证 SQL**（修复后执行）：
```sql
SELECT incompatible_with, count(*) FROM chemical_incompatible_categories
GROUP BY 1 ORDER BY 2 DESC;
-- 预期：无同义不同写（氨/氨水等成对消失）；另确认 UNIQUE 约束存在
```

---

### #31 安全距离 contains 匹配泛化遮蔽特化

**发现**：GetSafetyDistance（ChemicalKnowledgeGraph L367-375）用双向 contains 模糊匹配 + FirstOrDefault 按表序取第一条——泛化条目"储罐-建筑"（25m，id=2）会先于特化条目"液化烃储罐-办公楼"（35m，id=17）命中：用户问"液化烃储罐和建筑多少米"（key 含子串"储罐-建筑"）→ 返回 25m，少答 10m；词序反转（key 与条目反向）则双向 contains 均 false → 查不到降级 LLM

**根因**：文本 contains 匹配对子串零区分；条目无优先级（泛化/特化同表混存）；与 #27 死规则同款词汇表模式

**影响**：安全距离回答可能偏小（少了 10m 防火间距）或查无此条目——安全数值失真

**修复方案**（待讨论）：方案 A：精确 Equals 优先 + 模糊降级；方案 B：特化条目优先（按文本长度倒序）；方案 C：facility_pair 拆结构化列 + 精确索引

**修复状态**（2026-08-13 批次2）：🟢 已修复
- `GetSafetyDistance` 改为精确匹配优先，模糊命中按“条目长度倒序”取最特化；
- 单测：`液化烃储罐-建筑` 返回 35m（特化），不再被 `储罐-建筑` 25m 遮蔽。

**核销验证 SQL**（修复后执行）：需代码级单元测试——key="液化烃储罐-建筑"应返回 35m 而非 25m

---

### #32 安全距离表裸表

**发现**：仅 pkey——facility_pair 无 UNIQUE、无任何索引、无 FK

**根因**：002 建表只建了 pkey（与 incompatible_categories 同款裸表）

**影响**：种子重复会双写；启动全量加载内存故索引无实害，但与家族裸表通病一致

**修复方案**（待讨论）：加 UNIQUE(facility_pair)

**核销验证 SQL**（修复后执行）：
```sql
SELECT conname FROM pg_constraint
WHERE conrelid='chemical_safety_distances'::regclass AND contype='u';
-- 预期：包含 facility_pair 唯一约束
```

---

### #33 安全距离覆盖稀疏 + 量级混乱

**发现**：20 条 vs GB 50160/50016 防火间距全表（储罐类型×容量×介质×周边设施，几百组合）；"氯气储存区-居住区"200m（重大危险源等级）与 12~35m 通用条目混存，特殊规则与通用规则未分层

**根因**：002 种子抽样简化；与 #21 覆盖缺口同性质（确定性知识层简化）

**影响**：组合外的问题查不到 → 降级 LLM（对照 #21）；200m 异常值混在通用表里可能被误作通用规则

**修复方案**（待讨论）：扩充条目或标注覆盖边界；特殊规则分离或加规则等级列

**核销验证 SQL**（修复后执行）：
```sql
SELECT count(*) FROM chemical_safety_distances;
-- 预期：随扩充逐步增长；200m 条目有明确等级标注
```

---

### #34 丙三醇无类别标注

**发现**：chemical_hazard_categories 中丙三醇唯一 0 类别标注物质

**根因**：甘油无 GHS 危险分类——标注符合事实，非错误；但 CheckCompatibility 同类兼容推断（L347-349）依赖类别集非空，丙三醇参与时该分支失效

**影响**：极低（仅降级到精确边/类别级判定）

**修复方案**（待讨论）：接受现状；或代码层对空类别集显式跳过

**核销验证 SQL**（修复后执行）：
```sql
SELECT s.name FROM chemical_substances s
LEFT JOIN chemical_hazard_categories hc ON hc.substance_id = s.id
WHERE hc.id IS NULL;
-- 预期：丙三醇（符合 GHS 事实）或代码层已显式处理空集
```

---

### #35 hazard_categories 裸表

**发现**：无 UNIQUE(substance_id, category)（运行时 AddSubstance 重复写类别会重复行）；无 substance_id 索引

**根因**：002 建表只建 pkey + FK（家族裸表通病；当前 116 行种子无重复）

**影响**：低——数据质量高（全字段有值零幽灵），仅缺约束保护

**修复方案**（待讨论）：加 UNIQUE(substance_id, category)

**核销验证 SQL**（修复后执行）：
```sql
SELECT conname FROM pg_constraint
WHERE conrelid='chemical_hazard_categories'::regclass AND contype='u';
-- 预期：包含 (substance_id, category) 唯一约束
```

---

### #36 法规编号泛化遮蔽（GB 30000 遮蔽 GB 30000.1-2024）

**发现**：GetRegulationVersion（L392-397）双向 contains + FirstOrDefault 按表 id 序——"GB 30000"（总纲 2013，id=2）先于"GB 30000.1"（2024 修订，id=3）：用户问"GB 30000.1 最新版本"→ 归一化后"gb30000.1".Contains("gb30000")=true → 命中总纲返回 2013，2024 修订被遮蔽

**根因**：与 #31 同款 contains+FirstOrDefault 模式——全库第三处泛化遮蔽（#27 死规则、#31 距离、#36 版本）；短编号是长编号前缀，contains 必然命中且表序在前

**影响**：法规版本回答失真（把 2024 说成 2013）；与 GHS 第 8 修订版接轨的关键更新被隐藏

**修复方案**（待讨论）：精确匹配优先（Equals/最长编号优先）；或按编号长度倒序排序

**修复状态**（2026-08-13 批次2）：🟢 已修复
- `GetRegulationVersion` 改为精确匹配优先，模糊命中按“编号长度倒序”取最长编号；
- 单测：`GB 30000.1` 返回 2024（不再被 `GB 30000` 总纲 2013 遮蔽）。

**核销验证 SQL**（修复后执行）：需代码级单元测试——查询"GB 30000.1"应返回 2024 而非 2013

---

### #37 法规版本表裸表

**发现**：regulation_number 无 UNIQUE

**根因**：002 建表仅 pkey（家族裸表通病；当前 8 行无重复）

**影响**：低——运行时重复登记会双写

**修复方案**（待讨论）：加 UNIQUE(regulation_number)

**核销验证 SQL**（修复后执行）：
```sql
SELECT conname FROM pg_constraint
WHERE conrelid='chemical_regulation_versions'::regclass AND contype='u';
-- 预期：包含 regulation_number 唯一约束
```

---

### #38 法规档案 8 部仅 3 部有全文

**发现**：登记 8 部法规，知识库只有 3 部全文（GB 15603/GB 30000 系列/GB 30871）；GB 50160/50016/18218/JT/T 617 无原文——has_full_text 标记经交叉验证 8/8 准确，问题是覆盖现状

**根因**：知识库建设阶段只上传了部分法规（28 个 GB 30000 分册占绝大多数）

**影响**：合规回答引用这些规范条款（如 #28 的 regulation_ref="GB 50160"）时无原文可查——溯源链条源头断料

**修复方案**（待讨论）：补齐 5 部法规全文入库（GB 50160/50016/18218 优先级高）；或引用时明确标注"无原文"

**核销验证 SQL**（修复后执行）：
```sql
SELECT file_name FROM knowledge_documents
WHERE file_name ~ '50160|50016|18218|617';
-- 预期：逐步出现对应文档
```

---

### #39 refresh_tokens 过期令牌无限堆积

**发现**：434 行中 427 行（98.4%）已过期、420 条过期超 7 天、最老过期令牌躺在 2026-07-02——全项目无任何清理任务（grep 确认只有 INSERT + 验证 DELETE，无定期清理）

**根因**：每次登录 INSERT 新 token（StoreRefreshTokenAsync L1453），但令牌只在"刷新"时被 DELETE（ValidateAndRemoveRefreshTokenAsync L1479）——admin 404 个令牌大多"登录后未刷新"即废弃，永不被清理

**影响**：表体积缓慢膨胀（434 行/48 天）；长期运行无上限；无清理策略违反数据留存纪律（DataRetentionTests 未覆盖此表）。安全设计本身合格：token_hash 哈希存储无明文、PK 天然去重、一次性令牌（刷新即删）、原子验证

**修复方案**（待讨论）：启动或定时任务 `DELETE FROM refresh_tokens WHERE expires_at < now() - INTERVAL '7 days'`；或登录时清理该用户旧令牌

**核销验证 SQL**（修复后执行）：
```sql
SELECT count(*) FILTER (WHERE expires_at < now() - INTERVAL '7 days') AS 过期超7天
FROM refresh_tokens;
-- 预期：≈0（清理任务生效后）
```

---

### #40 search_logs 幽灵表（检索观测从未接线）

**发现**：表 0 行 + 全项目零写入代码——建表 DDL（DatabaseService L384-400）、启动建表调用（L95）、测试断言（DatabaseIntegrationTests L103）全齐，但 0 处 `INSERT INTO search_logs`、0 处埋点方法

**根因**：建表先行、写入未接——与 sessions（#19）同款"幽灵表模式"（设计在 DDL、实现留空）；检索链路（VectorSearchAsync 等）无埋点

**影响**：检索质量分析缺数据（无法回答"RAG 平均耗时多少""双数据源分流比例多少"——source_priority 字段明显为双通道观测设计）；与 #10 水印垃圾呼应（若接线，用户实际检索命中垃圾块比例本可被观测）；检索行为日志缺失

**修复方案**（待讨论）：在检索入口埋点写 search_logs（query/results_count/execution_time_ms/source_priority）；或若观测价值已被 MetricsCollector 替代则废弃此表

**核销验证 SQL**（修复后执行）：
```sql
SELECT count(*) FROM search_logs;
-- 预期：>0（埋点接线后随检索增长）
```

---

### #41 drift_details.probe_key 历史空 7/8

**发现**：8 条明细中 7 条 probe_key 为空——006 迁移增列 `ADD COLUMN IF NOT EXISTS probe_key` 后，006 之前写入的明细无回填，追溯断链（只能追到批次 probe_id，追不到具体探针问题）

**根因**：增列无历史回填（回填需按批次关联反查模板，成本高价值低）

**影响**：历史 7 条明细无法定位"哪条黄金问题测出了漂移"——恰好 probe_a1 的 1.0 漂移明细（id=15）有 probe_key，损失有限

**修复方案**（待讨论）：不回填（新数据天然带 probe_key）；或按 session 反查模板手工回填 7 条

**核销验证 SQL**（修复后执行）：
```sql
SELECT count(*) FROM drift_details WHERE probe_key IS NULL;
-- 预期：不再增长（新写入均带 probe_key）
```

---

### #42 drift 漂移监测运行量 demo 级 + 抓到漂移无处置闭环

**发现**：4 批测量/8 条明细全部来自 8-12 e2e-demo 会话——生产运行量近零；probe_a1（"知识库分块文本和向量存在哪两张表？"）drift_score=1.0000（被测 AI 未答出"动脉A双表"锚点），监测系统抓到了真实漂移但无告警/跟进记录

**根因**：监测子系统"建成未启用"——代码/表/种子齐备，但无生产触发（主动调用或定时任务未开启）。id 序列跳号（1,2,3,8）非缺陷：ON CONFLICT upsert 重放自然消耗序列值

**影响**：漂移监测的预警价值未兑现——AI 认知漂移发生时无人知晓

**修复方案**（待讨论）：启用 DriftProbeScheduler 定时轮询（23 条黄金问题定期问被测 AI）；漂移率超阈值（如 >0.5）告警/落审计

**核销验证 SQL**（修复后执行）：
```sql
SELECT count(*), max(created_at) FROM drift_probes;
-- 预期：批次随生产运行持续增长
```

---

## 核销流程（纪律）

```
1. 发现问题 → 记录到台账（状态 🔴）
2. 分析根因 → 追到代码行号（状态 🟡）
3. 制定修复方案 → 评审选择（状态 🟡）
4. 执行修复 → 代码修改 + 测试
5. 回查数据库 → 跑核销验证 SQL（状态 🟢 或 ⚪）
   - SQL 结果符合预期 → 🟢 已核销
   - SQL 结果证明不是问题 → ⚪ 已关闭（记录原因）
6. 更新台账 → 修改状态 + 填核销日期 + 附 SQL 结果截图
```

**铁律**：
- 禁止仅凭代码推断"修好了"——必须跑 SQL 回查数据库（宪法第三条：真实数据落地）
- 禁止跳过步骤 5 直接标记核销（防幻觉）
- 每次核销必须附验证 SQL 及结果（可追溯）

---

## 待扫描表清单

| 表 | 是否全量扫描过 | 发现问题数 | 备注 |
|----|--------------|-----------|------|
| audit_logs | ✅ 是 | 5 | 2026-08-12 完成三层考古 |
| chemical_documents | ✅ 是 | 4 | 2026-08-12 完成三层考古 |
| sessions | ✅ 是 | 0 | 2026-08-13 完成三层考古（0 行幽灵表，#19 根因代码级钉死） |
| refresh_tokens | ✅ 是 | 1 | 2026-08-13 完成三层考古（#39，安全设计合格/卫生差） |
| search_logs | ✅ 是 | 1 | 2026-08-13 完成三层考古（#40，观测设计完备/零接线） |
| knowledge_documents | ✅ 是 | 3 | 2026-08-12 完成三层考古（#14/#15/#16） |
| knowledge_chunks | ✅ 是 | 4 | 2026-08-12 完成三层考古 |
| long_term_memories | ✅ 是 | 4 | 2026-08-13 完成三层考古（#17/#18/#19/#20） |
| chemical_substances | ✅ 是 | 3 | 2026-08-13 完成三层考古（#21/#22/#23） |
| chemical_aliases | ✅ 是 | 1 | 2026-08-13 完成三层考古（#24，002 家族最健康子表） |
| chemical_incompatibilities | ✅ 是 | 4 | 2026-08-13 完成三层考古（#25/#26/#28/#29，成对精读） |
| chemical_incompatible_categories | ✅ 是 | 2 | 2026-08-13 完成三层考古（#27/#30，成对精读） |
| chemical_safety_distances | ✅ 是 | 3 | 2026-08-13 完成三层考古（#31/#32/#33） |
| chemical_hazard_categories | ✅ 是 | 2 | 2026-08-13 完成三层考古（#34/#35，002 家族质量最高标注表） |
| chemical_regulation_versions | ✅ 是 | 3 | 2026-08-13 完成三层考古（#36/#37/#38，002 家族收官） |
| drift_anchors | ✅ 是 | 2 | 2026-08-13 家族精读（#41/#42，设计标杆/建成未启用） |
| drift_probe_templates | ✅ 是 | 0 | 2026-08-13 家族精读（23 条零孤儿锚定全启用） |
| drift_probes | ✅ 是 | 0 | 2026-08-13 家族精读（4 批 e2e-demo，问题归 #42） |
| drift_details | ✅ 是 | 0 | 2026-08-13 家族精读（probe_key 空归 #41） |

**统计**：已扫描 19/19 表（100%），全库精读收官 ✅（drift 家族 2026-08-13 全库清点后补扫）

---

## 宏观治理路线

```
阶段 1（当前）：audit_logs 完成 → 5 个问题入台账
阶段 1.5（当前）：chemical_documents 完成 → 4 个问题入台账
阶段 1.75（当前）：knowledge_chunks 完成 → 4 个问题入台账
阶段 1.9（当前）：knowledge_documents 完成 → 3 个问题入台账（并钉死 #10/#13 根因）
阶段 2（当前）：long_term_memories 完成 → 4 个问题入台账（记忆空心化/重复泛滥）
阶段 2.5（当前）：chemical_substances 完成 → 3 个问题入台账（覆盖缺口/状态枚举/索引）
阶段 2.55（当前）：chemical_aliases 完成 → 1 个问题入台账（种子瑕疵别名）
阶段 2.6（当前）：禁忌矩阵成对完成 → 6 个问题入台账（王水规则 P1/死规则 82%/克劳斯反应）
阶段 2.65（当前）：chemical_safety_distances 完成 → 3 个问题入台账（泛化遮蔽/裸表/覆盖稀疏）
阶段 2.7（当前）：chemical_hazard_categories 完成 → 2 个问题入台账（002 家族收官）
阶段 2.75（当前）：chemical_regulation_versions 完成 → 3 个问题入台账（编号遮蔽/裸表/全文覆盖）
阶段 2.8（✅ 2026-08-13）：最后 3 表完成 → #39/#40 入台账（sessions 幽灵表钉死 #19 根因），全库 15/15 精读收官
阶段 2.85（✅ 2026-08-13）：全库清点发现 drift 家族 4 表（8-11 后新建）→ 补扫完成 #41/#42 入台账，最终 19/19 表 100% 收官
阶段 2.9（✅ 2026-08-13）：远程库同步修复 → chemical_aliases 补建（DDL 取自 002 原版）+ 本地 83 行全量 name 关联回放，远程 19 表/16 序列/0 视图与本地结构对齐，精确行数 18/19 完全一致（唯一差异 drift_details：本地 8 vs 远程 4，属各自运行时监测记录，无需同步）
阶段 3（当前）：按严重性排序（P1 → P2 → P3） → 逐个核销
阶段 4：全部核销后 → 建立持续监控（新增表自动触发精读）
```

**核心原则**：先见森林再见树——不盲人摸象，不局部优化陷阱。
