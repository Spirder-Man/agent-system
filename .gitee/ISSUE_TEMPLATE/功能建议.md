---
name: 功能建议
about: 新能力或现有模块加深，须说明是否碰到事实通道
title: "[Feature] "
---

主约束：**法规号、储存禁忌、安全距离由确定性代码给出，大模型只解释。**
不要把「让模型自己改规则 / 改源码」当作默认方向。

## 使用场景

谁、在什么情况下需要。验证场仍是化工园区储存审查辅助，不是已交付的园区生产系统。

## 建议做法



## 是否改动事实通道

事实通道包括工具返回值、`RegulationRefs` 白名单、规则引擎、`OutputSanitizer`。

- [ ] 不改（只影响解释、页面或文档）
- [ ] 会改（须有可复现验收，不能只靠模型生成编号）
- [ ] 不清楚

## 怎样算做成

一条可复现的检查（命令、页面或测试名）：
