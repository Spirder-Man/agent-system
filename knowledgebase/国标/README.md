# 国标全文（不进 Git）

把使用方取得的合法副本放在本目录，例如 PDF 或 TXT。文件名建议含标准号，便于检索。

本仓库**不提供** GB 15603、GB 30000 等国家标准正文。公开仓只在数据库种子里保留法规**编号**与结构化禁忌，见 `db/migrations/002_chemical_knowledge_graph.sql`。

克隆后本目录只有这一份说明。没有正文时服务可以启动；全文 RAG 与向量入库需要自备文件，并在有 embedding 服务时加载。
